# 界面体验问题记录

记录日期：2026-08-20

## 已立即处理

- 全部产品页面由繁体中文统一为简体中文，页面语言标记改为 `zh-CN`。
- 中文字体由台湾字形优先级改为简体中文系统字体优先级。

## 下一轮需要重做

- 字号层级不稳定：目前存在 8–11px 的正文和标签，阅读负担较重，同级信息字号也不统一。
- 视觉层级不清楚：标题大小跳跃，页面之间的间距、边框和信息密度缺少共同规则。
- 整体风格缺少一致设计：教师端、学生端、提交页和反馈页需要共用一套字体、字号、间距、颜色和控件规范。

## 重做验收标准

- 常规正文桌面和手机均不小于 14px，辅助文字原则上不小于 12px。
- 每个页面只有一个主标题和一个主要操作，同级信息使用同级字号。
- 教师端与学生端共用同一套基础视觉规范，并在 390px 与 1440px 宽度下实际检查。
- 保持现有权限、提交、反馈和确认流程不变，只调整呈现方式。

## 进行中：UI-206 现代化视觉探索

- 闸 1 已在真实首页、教师仪表盘、学生仪表盘提供三套可切换原型；无 `?visual=` 时保持 UI-201 基线。
- 评审地址：`/teacher?visual=warm-paper|ink-structure|soft-studio`，以及同样参数的 `/` 与 `/student`。
- 三套方向：暖纸工作台、墨结构、柔和教室。选定前不把任一方向扩散到其余工作页。
- 探索圆角层级、柔和表面、统一图标、微交互与页面过渡，但维持教学工作台的清晰信息层级。
- 最多采用 2–3 个有意义的动效：打开确认、状态变化、列表或页面切换；时长 150–300ms，并支持 `prefers-reduced-motion`。闸 1 未引入新动效。
- 禁止大量滚动动画、弹跳、玻璃拟态、渐变堆叠、为动效引入重型框架，以及任何改变发布、提交、反馈业务语义的视觉改造。
