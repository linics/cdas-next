# 界面体验问题记录

记录日期：2026-08-20

## 已立即处理

- 全部产品页面由繁体中文统一为简体中文，页面语言标记改为 `zh-CN`。
- 中文字体由台湾字形优先级改为简体中文系统字体优先级。

## 下一轮需要重做

- 字号层级不稳定：目前存在 8–11px 的正文和标签，阅读负担较重，同级信息字号也不统一。
- 视觉层级不清楚：标题大小跳跃，页面之间的间距、边框和信息密度缺少共同规则。
- 整体风格缺少一致设计：教师端、学生端、提交页和反馈页需要共用一套字体、字号、间距、颜色和控件规范。

## 重做验收标准

- 常规正文桌面和手机均不小于 14px，辅助文字原则上不小于 12px。
- 每个页面只有一个主标题和一个主要操作，同级信息使用同级字号。
- 教师端与学生端共用同一套基础视觉规范，并在 390px 与 1440px 宽度下实际检查。
- 保持现有权限、提交、反馈和确认流程不变，只调整呈现方式。

## UI-206：已按 UI UX Pro Max 落地

- 已安装技能最新版 `ui-ux-pro-max-cli@2.15.0`（技能路径，不进仓库）。
- 已验证并持久化设计系统：`design-system/uipro-max-lms/`（LMS + Flat Design + Accessible & Ethical）。
- 全站前端按该 MASTER 与页面覆盖文件调整：色板、Plus Jakarta Sans、密度 8、2px 扁平半径、无阴影/无渐变、主 CTA 橙、次按钮青绿描边。
- 已删除本分支此前无法使用的 `?visual=` 原型、模板抄写和对比条。
- 业务文案、授权、domain command 与确认链未改。
