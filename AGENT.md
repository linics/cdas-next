# CDAS Next Agent 边界

状态：启动基线（v0.1）；首版官方依据检索已合入主线
日期：2026-08-27

## 定位

CDAS 助手是嵌入产品的上下文助手，不是独立聊天机器人，也不是绕过产品规则的自动操作层。

助手可以理解：

- 当前登录用户及角色
- 当前页面
- 当前班级、草稿、发布实例或提交
- 当前用户依法可调用的工具集合

助手不拥有额外权限。它能做的事情，必须是当前用户通过普通产品界面也有权完成的事情。

## 第一阶段交互形态

- 全局入口：回答“我现在可以做什么”、查找自己的班级或草稿。
- 场景入口：在活动草稿、发布预览和提交查看页面携带当前对象上下文。
- 结构化结果：优先展示草稿卡片、差异预览、班级选择和确认面板，不只返回长文本。
- 页面导航：通过注册的前端工具跳转，不通过模拟鼠标或猜测 DOM 操作页面。

## 工具分层

### 前端工具

只影响界面状态，不直接修改业务数据：

- `navigate_to_activity_draft`
- `navigate_to_release`
- `navigate_to_submission`
- `open_release_preview`
- `focus_activity_section`

### 只读业务工具

- `get_current_context`
- `list_my_classrooms`
- `get_activity_draft`
- `get_release_summary`
- `list_release_submissions`

### 草稿工具

- `create_activity_draft`
- `update_activity_draft`
- `suggest_activity_structure`

草稿工具只能修改当前教师拥有的草稿，并必须返回修改摘要和可撤销依据。

D-030 起，`create_activity_draft` 的新写入只接受完整 schema v2 任务书。原版 CTS/CDAS 是生成质量下限：必须包含合法学段/年级、主学科与融合学科、原版三类作业及条件子类型、探究深度、提交模式、周期、三维目标、具体背景、3–4 个连续阶段、类型化证据和四档评价描述。生成内容应形成真实问题、真实对象、跨学科证据链和可验收成果，不能用“开展活动”“进行探究”等泛词替代学生行动。它们仍是教师可编辑的草稿建议，不是课程标准判断或跨学科质量结论。

D-033 起，`create_activity_draft` 是“理解确认后执行”的 L1 工具。资料不足时助手每轮只问一个会改变设计的必要问题；资料充分后，工具输入同时携带结构化理解摘要、教师已提供要求、明确假设、每个融合学科的必要贡献、知识/过程/情感三条目标—任务—证据—评价链和完整 v2 内容。UI 必须先展示这些参数并等待教师批准；拒绝确认不写入。该批准使用 AI SDK 签名防止客户端伪造，但不建立 ActionIntent，也不替代执行时的服务端授权或后续 L2 发布确认。

理解摘要和设计诊断只说明这份建议内部如何组织，不是课程标准、教育效果或自动评分结论。教师编辑草稿后，旧诊断只能视为对应提案的历史界面内容；本切片不自动重写教师修改，也不持久化完整对话或另建诊断实体。

主线已交付的首版官方依据检索在上述提案之前增加两个 L0 只读工具：`search_knowledge` 只检索教育部课程方案及语文、数学、物理、信息科技 2022 年课程标准的已圈定章节，`read_source_section` 只读取前者返回的精确章节。模型可改写查询并迭代，但不得把 UbD、C-POTE、T/CAET、设计流程、教材、教师案例或 AI 历史产物说成当前语料。命中首版覆盖学科时，提案须包含至少两个不同官方来源的规范引用和采用理由；零命中必须明确说明语料边界。

知识工具结果和提案引用均由服务端白名单校验。服务端按当前消息会话维护“检索命中—已读章节”账本：`read_source_section` 只能读取此前检索命中的章节，`create_activity_draft` 的每条引用都必须已在本次会话读过，审批续传时从重新校验后的消息历史恢复该账本。工具输入不接受文件路径、任意 URL、存储键或私有资源标识；客户端历史中的片段、标题、链接与 ID 必须能由相同输入在当前语料中精确重算。来源引用只说明当前提案使用了哪些依据，不是课程标准映射或合规结论，也不进入 ActivityDraft、Revision 或 Release snapshot。

### 高影响业务工具

- `publish_activity_release`
- `save_teacher_feedback`
- 关闭活动、删除和成员变更工具均留待后续；第一阶段不向 Agent 开放关闭 Release 工具。
- D-025/D-026 的附件预留、上传、内容验证、移除与下载不作为 Agent 工具；学生与教师通过普通界面完成，模型不能取得 storage key、预签名 URL 或附件内容。
- D-027 的名单码预览、成员加入、结束与恢复不作为 Agent 工具；成员变更是 L3 人类管理操作，只能由班级管理员在第一方界面明确确认。
- D-035 的量规评价准备与保存不作为 Agent 工具；教师只通过第一方界面手写并独立确认。D-036、D-037、D-038、D-039、D-040、D-041、D-042 与 D-043 的列表评价状态、覆盖计数、跟进标记、工作台待办计数和评阅名册导出同样只走第一方只读查询。

高影响工具必须暂停，展示完整参数与影响范围，并等待用户明确确认。

D-034 起，`save_teacher_feedback` 的新确认参数除正文外还必须包含形成性下一步（继续或修改重交）与支架层级（基础支持、标准任务、挑战拓展）。AI 只能提出这些字段的建议；教师确认后的共享领域命令才可追加正式反馈修订。该选择不成为分数、阶段解锁状态或长期学生画像，助手也不能自动向学生发布反馈。

D-035 的证据绑定量规评价只走第一方 UI 与共享领域命令，不进入工具清单。长期候选的评价建议仍须先形成用户场景、确认合同和人机差异历史后再决定是否开放 Agent 工具。

## 风险等级

| 等级 | 类型 | 默认策略 |
| --- | --- | --- |
| L0 | 页面导航、读取当前用户有权查看的数据 | 可直接执行 |
| L1 | 新建草稿、修改未发布草稿 | 必须先展示结构化方案或变更摘要；D-033 的 Agent 新建草稿需教师确认，结果仍可继续编辑 |
| L2 | 发布活动、保存最终反馈 | 必须人工确认 |
| L3 | 删除数据、调整成员、批量操作 | 第一阶段不开放给 Agent |

## 每个工具必须具备的合同

- 明确、狭窄的用途
- 结构化输入与输出
- 服务端身份认证
- 服务端资源级权限检查
- 幂等键或重复调用处理策略
- 可理解的错误结果
- 审计记录
- 超时与失败恢复策略
- 是否需要人工确认

前端传来的“已确认”不能代替服务端授权。审批用于防止模型擅自行动，授权仍必须在工具执行时依据当前用户和当前资源重新判断。

## 禁止事项

- Agent 直接访问数据库会话或 ORM 模型
- 用 Prompt 代替权限规则和业务校验
- 将完整工具集合暴露给所有角色
- 自动发布、自动终评或静默修改学生数据
- 让模型生成任意可执行前端代码
- 用浏览器自动点击代替稳定的业务工具
- 把 RAG 当作所有问题的默认解决方案
- 在第一阶段引入多个相互委派的 Agent

## 中断与已提交结果

- 模型在业务工具执行前中断时不得创建草稿、Release 或反馈；AgentRun 必须如实进入 FAILED 或 CANCELLED。
- Agent 草稿或发布领域命令必须把不可变结果、成功审计与对应 AgentRun 的 `RUNNING → SUCCEEDED` 放在同一数据库事务；若运行已经失败、取消或不属于当前教师，整个业务事务回滚。
- 一旦经过签名交互审批与 ActionIntent 重新授权的领域命令已经成功提交，其不可变结果就是权威事实。后续模型说明、HTTP 回应或客户端连接失败不得回滚该结果，也不得把对应 AgentRun 改写成失败。
- 写工具成功后应结束后续模型 loop。响应丢失的重试仍必须经过既有幂等和 provenance 合同；不能为了恢复便利而放松 actor、AgentRun、参数 hash 或资源版本绑定。
- 已成功的 AgentRun 只能精确重放其既有幂等结果，不能以新 key 或新参数继续创建业务历史。

## 第一阶段 Agent 验证任务

只验证一个端到端场景：

> 教师用自然语言描述活动；助手在必要时有限澄清，随后展示结构化理解和设计诊断；教师确认后生成可编辑草稿，进入草稿预览，并在另一项独立确认后调用发布工具。

这个场景的官方 AI SDK message session 只在覆盖 `/teacher/activities/new` 与精确预览页的共享客户端 layout 内存中续接。草稿方案和发布分别使用独立的签名 approval；草稿工具返回的 draft ID 与站内路径精确一致时才导航，预览页继续使用同一 session 提出发布 approval。对话、理解摘要、诊断、prompt、ticket 或 approval 签名不写入 URL、localStorage 或业务数据库。直接进入或刷新预览页不会恢复助手对话，教师仍可使用既有手工发布面板完成闭环。

第一次外部验收必须由独立、受保护且明确批准模型费用的 `staging-agent-acceptance` workflow 完成。它只能使用预留的四个 Clerk test identity、marker 派生 namespace 和固定合成主题；marker 标题必须精确，模型生成的理解、诊断和 schema v2 任务书须满足 D-033/D-030 合同，但不得把生成文本逐字照抄 prompt 当成产品成功条件。每个 ticket 只可在最终 staging origin 精确锁定后签发，关键导航不得跨 origin。任何 deployment、DB、Clerk、模型、approval secret、同 run 证据或人工 attestation 不匹配都必须在模型或业务写入前得到 `NO_GO`。正常发布账本必须证明 marker 标题下只有一份草稿，且四次 `SUCCEEDED` AgentRun 分别对应无业务写入的草稿方案、教师确认后的草稿写入、无业务写入的发布提议和教师确认后的发布执行。随后同一个 Agent 创建的 Release 必须继续由普通第一方 UI 完成主学生正式提交、教师确认反馈、学生读取反馈、教师确认关闭、关闭后陈旧写拒绝和只读访问，并以其他学生与其他教师证明资源隔离；这些后续动作不增加 Agent 工具或 AgentRun。最终证据 schema 与截图索引缺少或畸形同样必须 `NO_GO`。

项目所有者已持续授权 Agent 在开发阶段自动管理测试 GitHub、Vercel Hobby、Neon、Clerk 与固定合成数据的 DeepSeek 验收；已经存在本机忽略凭据和命令所需精确成本确认时，不因同一授权逐次暂停。该持续授权不包含生产资源、真实用户数据、套餐升级或付款资料、不可恢复的删除或重置，以及明显扩大的模型费用或非固定合成调用；遇到这些边界必须停止并报告。凭据只能从本机 Git 忽略文件按需读取，禁止输出到聊天、日志、截图、artifact、文档或 Git。

如果这个场景不能稳定通过正常、拒绝确认、无权限、重复调用和模型不可用测试，不增加第二个 Agent 场景。

## 已接受的设计助手增强

D-033 只把成熟开源 Agent 中适合当前产品的控制模式落入既有助手：先理解再执行、窄工具、结构化结果、人工中断、显式停止和失败降级。它不复制 OpenHands、Cline、SWE-agent、Goose、LangGraph、Mastra、OpenAI Agents SDK 或 PydanticAI 的通用运行时；当前短流程继续使用 Vercel AI SDK 与领域命令。

暂不采用持久 checkpoint、长期记忆、多 Agent handoff、任意网络/浏览器/终端工具、开放式多来源 RAG、自动改稿或自动发布。当前只有上述五份公开官方来源的只读检索，不引入通用 RAG 框架、向量服务或第二后端；其他来源类型仍须先完成治理决策。

## 长期助手方向

`ROADMAP.md` 已记录需求澄清、任务理解摘要、跨学科必要性说明、目标—任务—证据—评价一致性诊断、可选资料依据和证据驱动评价建议等候选能力。D-034 只接受教师审核后的结构化形成性反馈字段；其他候选能力进入工具清单前仍须逐项形成用户场景、领域命令、资源授权、确认与审计合同。

长期方向也必须遵守：

- AI 只提出可解释、可编辑的建议，不直接发布任务、分数或最终评价。自动形成性反馈只作为待决路线项；若未来接受，必须由教师按活动明确启用、限制范围并保留审计和纠正路径。
- 证据不足时明确说明缺失内容，不根据常识或模型推测补齐学生表现。
- RAG 只在明确资料来源、使用范围和访问权限后作为可选增强；模型或检索不可用时人工流程仍可运行。
- 学生开放式 AI 聊天不进入近期路线；任何面向学生的 AI 能力必须先处理代做、隐私、年龄适配和教师可见性。
- 保存 AI 建议与教师最终结果的差异是审计和研究素材，不等于已经证明人机一致性或模型有效性。
