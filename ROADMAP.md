# CDAS Next 功能演进路线

状态：长期产品方向已记录；第一阶段开发与验收顺序不变
日期：2026-08-23

本文把旧版 CDAS 评审后形成的《中小学跨学科任务设计与评价一体化平台 PRD（研创赛重构版 v1.0）》和《新旧版本对比简报》转成可持续维护的产品路线。它记录最终希望具备的能力，但不是当前版本的实现清单、交付日期或验收合同。

当前实施范围仍以 `PRODUCT.md` 的第一条完整闭环、`DOMAIN.md` 的第一阶段模型、`ACCEPTANCE.md` 的第一阶段合同和已有 `DECISIONS.md` 为准。任何路线能力只有在形成明确用户场景、权限与历史规则、独立验收场景和已接受决策后，才能进入开发。

## 不改变当前开发安排

1. 先完成真实 Clerk 教师/学生账号、独立 PostgreSQL 和真实模型供应商条件下的第一阶段外部验收。
2. 不为未来能力预建空实体、通用 JSON、第二套后端、工作流引擎、RAG 系统或多 Agent 运行时。
3. 后续按可独立验收的纵向切片推进；一个切片完成正常、无权限、并发变化、重复操作和外部依赖失败路径后，再接入下一项依赖能力。
4. 路线顺序可以通过新的已接受决策调整，但不能通过页面占位、脚手架默认值或未经验收的数据库字段暗中扩大范围。

## 长期产品目标

CDAS Next 最终面向跨学科教师协作条件不足的真实 K12 场景，以单教师负责制为默认组织方式，帮助教师完成有依据的跨学科任务设计、差异化实施、过程证据采集、形成性反馈和证据驱动评价。AI 负责澄清、建议、诊断和解释；教师始终负责发布、反馈和最终评价。

长期目标闭环为：

> 课程依据 → 教师需求与 AI 澄清 → 受约束的任务设计 → 教师确认 → 学生分阶段实施 → 学习证据 → 关键节点形成性反馈 → 修改与再提交 → 证据驱动评价 → 教师终审 → 看板、导出与复用

这个目标扩展当前闭环，但不替换当前第一阶段完成标准。

## 已接纳的产品方向

### 教师与学校组织

- 默认由一名教师负责活动，多教师复杂协作只保留为更远期方向。
- 长期支持学校数据边界、教师建班和受控批量导入学生；学生账号不采用无约束自主注册。
- 长期支持班级、小组和指定学生三种发布范围。
- 学校租户、管理员权限、账号开通、学生身份匹配、跨班转移和导入回滚必须先形成独立授权与历史合同，不能把“Excel 导入”当成普通表格写入。

### 跨学科任务设计

- 活动明确主学科与融合学科，并说明各学科对问题解决的真实作用；系统可提示装饰性、拼盘式融合，但最终判断由教师确认。
- 创建流程采用“核心结构化输入 + 可选补充 + 必要澄清 + 教师确认”。AI 正式生成前先展示任务理解摘要。
- 结构设计遵循“目标—任务—证据—评价”一致性，支持教师查看诊断理由并继续编辑。
- 候选 `TaskType` 为：探究调查型、设计制作型、社会实践型、综合项目型。它们是待验证的产品词汇，不是当前数据库枚举。
- 学生开放式 AI 聊天不进入近期路线，避免代做和责任边界模糊。

### 语义化阶段

未来阶段应从有语义、可版本化的目录中选择和组合，而不是让模型自由创造不可解释的状态。候选 `StageType` 目录为：

- `problem_context`：情境进入与问题发现
- `problem_definition`：问题或需求界定
- `knowledge_building`：知识与信息获取
- `inquiry_planning`：探究或调查规划
- `evidence_collection`：数据与证据获取
- `analysis_reasoning`：分析、解释与论证
- `solution_design`：方案设计
- `implementation`：制作或行动实施
- `testing_validation`：测试与验证
- `revision_improvement`：修改与完善
- `communication`：成果表达与交流
- `reflection_evaluation`：评价与反思

该目录不能直接成为统一流程状态机。不同 `TaskType` 对阶段、依赖、并行、回溯和完成条件的要求不同，进入领域模型前必须验证真实课堂案例并为目录本身建立版本规则。

### 学习证据与过程反馈

- 在现有不可变 `SubmissionRevision` 基础上，逐步支持文本、图片、Word/PDF/PPT、Excel/CSV、平台表格、版本变化和最终成果。
- 小组任务采用“小组共同成果 + 可识别的轻量个人贡献”，但不得因此让组员读取无权访问的个人草稿或历史数据。
- 教师可指定少量关键反馈节点；候选模式包括自动发布形成性建议、教师审核后发布或关闭。自动模式尚未接受为产品行为，未来若进入开发，必须由教师按活动明确启用、限制反馈范围、完整审计并允许教师纠正；阶段反馈不直接成为正式分数，也不能替学生完成任务。
- 保留“提交—反馈—修改—再次提交”的版本历史，评价引用的证据必须指向不可变修订。

### 差异化与评价

- 差异化维持共同核心目标，通过基础支持、标准任务和挑战拓展调整任务要求、支架强度和拓展深度；AI 可推荐，教师负责分配与确认。
- 最终评价候选结构包括分维度量规、关联证据、AI 建议理由、改进建议和综合结论。
- 每个评价维度必须关联有权读取的实际学习证据；证据不足时必须明确返回“证据不足”，不得凭空补齐事实或强行给分。
- AI 原始建议与教师最终评价可以分别保留，用于审计和后续研究；差异记录本身不能证明 AI 有效，也不能把教师评价自动当作模型训练真值。
- 学生只看到教师确认后的正式评价；AI 不成为最终评价者。

### 诊断、复用与管理

- 轻量过程诊断可关注阶段卡点、量规薄弱项、修改次数和反馈后改善情况。
- 可展示同一班级内的历史趋势，但不建立跨学年的固定能力标签或不可解释画像。
- 长期支持活动归档、复制、AI 适配、导出、平台精选案例和教师个人历史活动；教师个人内容默认私有。
- 轻量管理员方向包括教师账号边界、平台基础资料、精选案例和必要配置，不扩张为复杂学校运营后台。

## AI 与资料依据方向

未来设计助手可以选择性使用课程标准、教材知识结构、跨学科设计规范和教师明确选择的个人资料，并在结果中展示资料名称与关键依据。候选理论参考包括 T/CAET 005—2025、C-POTE、UbD、Constructive Alignment、探究循环、工程设计、综合实践和 PBL。

这些名称是设计输入，不是自动正确的业务规则。进入产品前必须确定适用学段、学科、版本、授权来源和可验证的映射；教师个人资料默认私有，跨学校检索默认禁止。模型运行、Prompt 版本、检索来源和建议结果需要可追溯，但不能用对话日志代替业务审计。RAG 不作为所有问题的默认解法，模型与资料服务失败时手工设计、发布、提交和反馈闭环仍须可用。

## 建议依赖顺序

以下顺序用于保护依赖关系，不代表发布日期或已经接受的实现范围：

1. **当前基线验收**：完成第一阶段真实账号、数据库和模型条件下的闭环验收。
2. **证据附件纵向切片**：私有对象存储、类型和大小检查、恶意文件扫描、所有权与不可变修订关联。
3. **教师成员管理纵向切片**：受控导入、预览、错误报告、幂等、成员有效区间和审计；多租户另行决策。
4. **结构化任务纵向切片**：主学科/融合学科、`TaskType`、目标与证据要求；先保证手工流程，不依赖模型。
5. **多阶段与小组纵向切片**：版本化 `StageType`、依赖与回溯、阶段证据、小组共同成果和个人贡献。
6. **设计助手增强**：需求澄清、理解摘要、跨学科必要性和一致性诊断；知识库仅在来源治理完成后作为可选增强。
7. **形成性反馈与差异化**：关键节点、审核模式、支架方案、修改与再次提交。
8. **证据驱动量规评价**：维度—证据绑定、证据不足、AI 建议与教师终审差异历史。
9. **诊断、复用与学校管理**：过程看板、趋势、导出、案例、租户和轻量管理员能力。

如果某项需求能够形成更小且不依赖后续模型的纵向切片，可以由新的决策调整顺序；不得通过提前引入通用平台或第二套架构来“为未来做准备”。

## 每个未来切片的进入条件

每项路线能力进入开发前必须同时具备：

- 一个真实教师或学生用户场景，以及明确的非目标；
- 资源所有权、班级或小组成员关系和历史可见性规则；
- UI 与 Agent 共用的服务端领域命令边界；
- 对发布、提交、反馈、评价、审计和幂等结果的 append-only 影响说明；
- 任何需要长期保留的 AI 建议、Prompt 版本、检索来源和人机差异记录都具有资源级读取授权，并以不可变快照或只追加版本保存，不能被静默覆盖；
- 正常、无权限、不存在或并发变化、重复操作和外部依赖失败的验收场景；
- 模型、认证服务、对象存储和文档解析不在数据库事务内的实现方案；
- AI 不可用时仍可完成对应人工流程的降级路径；
- 在 `DECISIONS.md` 中记录已选择方案，并同步更新 `PRODUCT.md`、`DOMAIN.md`、`ACCEPTANCE.md`、`AGENT.md` 中真正受影响的部分。

## 明确不采纳的假设

- 不因外部 PRD 的技术建议改为 React + Vite + FastAPI；D-010 的单一 Next.js 模块化单体继续有效，除非出现新的已接受决策。
- 不在没有教师精确确认的情况下发布活动、分数或最终评价；自动形成性反馈只能在未来独立决策接受且教师按活动明确启用后进入产品。
- 不把学生开放式 AI 聊天、多教师复杂协作、资源社区、视频 AI 分析、跨学年固定能力标签或复杂学校运营后台设为近期承诺。
- 不认为保存 AI 与教师差异就已经完成教育效果或人机一致性验证；相关结论需要独立研究设计和证据。
