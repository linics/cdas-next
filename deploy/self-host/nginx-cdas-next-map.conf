# Install as /etc/nginx/conf.d/cdas-next-map.conf
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

upstream cdas_next_upstream {
    server 127.0.0.1:3000;
    keepalive 16;
}
