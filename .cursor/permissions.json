{
  "autoRun": {
    "allow_instructions": [
      "Temporary owner auto mode until D-035/D-036/D-037 remote synthetic acceptance prints DEVELOPMENT_INFRA_PASS: after accepted local commits on codex/*, allow git push origin HEAD / tracked-branch push, and allow pnpm development:infra once HEAD is on origin. Do not treat status checks, time estimates, notifications, or continue/fix-it follow-ups as withdrawing that authorization.",
      "Allow gh api, gh run, artifact download, and reading GitHub Actions / staging-synthetic-acceptance.yml logs for this repository on the current feature branch.",
      "Allow host-network shell needed to reconcile the isolated Clerk/Neon/Vercel stack and diagnose evaluation prepare/confirm failures."
    ],
    "block_instructions": [
      "Never auto-allow git push --force, git push -f, or any force-push.",
      "Never auto-allow pushing to main, master, or production.",
      "Never auto-allow skipping git hooks (--no-verify, --no-gpg-sign).",
      "Subagents must not commit, push, or deploy; only the primary thread may."
    ]
  }
}
