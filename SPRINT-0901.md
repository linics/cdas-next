# 竞赛冲刺计划（2026-08-26 → 2026-09-01）

状态：生效中。冲刺期内本文件优先级高于 AGENTS.md 与 DECISIONS.md 中的常规交付流程；冲刺结束后本文件归档，常规流程恢复。

## 目标重定义

本项目当前唯一目标是 **9 月 1 日交出一个能打动评委的参赛作品**。评委接触作品的方式是演示视频、现场演示与参赛材料，时间 10–20 分钟。因此一切工作按"评委可见性"排序。

**冲刺期内明确不做**：租户/管理员、案例库、成员批量导入、AI 评价建议（除非全线提前）、任何新的合规决策、真实用户相关工作。

## 流程降级（冲刺期专用）

- **停止每切片远程合成验收**。冲刺期内新功能只要求：`pnpm check` 通过 + 本地手动过一遍主路径。不新增 D-0xx 决策记录、不跑 staging-synthetic-acceptance、不更新 STAGING.md 证据表。
- 保留的硬约束（防演示翻车）：不破坏现有闭环测试、不改动发布/提交/反馈/评价的写路径语义、`AI_PROVIDER_DISABLED=1` 下全部手工流程必须照常可用。
- 新功能允许"读路径新查询 + 新页面/新组件"自由添加；写路径改动需谨慎（回归成本高）。

## 工作顺序

按下列顺序推进，做完一项进入下一项。工时是预估，实际快慢都正常；真正不能动的只有文末三个硬锚点。

### 第 0 步：定桩（半小时，唯一需要用户亲手做的事）

- [x] 圈定首版 RAG 语料：只收教育部《义务教育课程方案（2022年版）》及语文、数学、物理、信息科技课程标准的目标、课程内容、学业质量和实施相关章节。UbD、C-POTE、T/CAET、设计规范、教材、教师案例与 AI 生成历史均不进入首版；设计方法留在提示词与 schema。Word 原件来自并列 `cdas/storage/raw/curriculum_standards/`，机械转换文本和白名单清单位于 `corpus/official-standards/`。

### 第 1 步：Agentic RAG（预估 1.5–2 天）

目标：设计助手在生成任务书前后能**检索指定语料并给出可点开的来源引用**。

- 语料索引：构建脚本把 `corpus/official-standards/` 按标题层级和段落稳定切块，记录来源名、章节路径、教育部发布页、原文与内容 hash；Word 不能可靠恢复物理页码时不伪造页码。首版生成只读静态索引，不新增 Prisma 表或第二套入库状态机。
- 检索基线用**确定性词法检索**（中文分词、标题/章节/正文加权、学段/学科过滤和来源多样化），不依赖 embedding、pgvector 或任何外部检索服务。
- Agentic 部分：给现有设计助手新增 2 个只读工具 `search_knowledge`（关键词检索返回带来源的片段）与 `read_source_section`（按目录读取指定章节），允许模型多轮改写查询、迭代检索。
- UI：助手回答中的依据以引用标注呈现，点开显示原文片段 + 来源名 + 章节。任务理解摘要中展示"本次设计参考了哪些依据"。
- 边界：只读工具，不产生业务写入；检索为空时明确说"语料中未找到依据"，不编造。模型关闭时检索入口对教师独立可用（成本极低且是加分演示点）。

**完成标准**：教师在新建活动页提问 → 助手检索 → 回答带 ≥2 条可点开引用。

### 第 1.5 步：任务书情境叙事升级（预估 0.5 天）

问题（2026-08-27）：现有生成结果是"纯作业"口吻——背景设定是摆设，各阶段是三道独立的题，学生看着枯燥。字段已够（真实情境 + 每阶段情境），缺的是内容的叙事性；只改提示词与文案，不动 schema。

- **提示词强制驱动性情境三件套**：学生角色（"你们是……"）、真实受众（成果交给谁）、驱动性问题/最终成果物，写进"真实情境"字段。
- **阶段间叙事衔接**：每阶段情境用第二人称承接上一阶段产出（"你们在上一阶段发现了……现在××希望你们……"），先叙事后要求；三个阶段必须是同一个故事的三集。
- **一致性自检**：助手在任务理解摘要中自查"三阶段是否同一故事、证据是否逐级递进"，并入既有目标—任务—证据—评价诊断。
- **学生端文案去术语化**（随 3a 落地）：任务卡以情境叙事开场；"核心行动/学习支架/评价要点"等教师词汇在学生端改为"这一步做什么 / 可以怎么做 / 老师会看什么"。
- 与第 1 步同属助手提示词改动，可合并施工；RAG 引用的课标依据可作为情境真实性的佐证展示。

**完成标准**：用同一主题重新生成一份任务书，三个阶段读起来是连续剧而不是三道题；学生端当前阶段卡首句是情境而不是标签。

### 第 2 步：教师诊断看板（预估 0.5–1 天）

纯读路径，新页面 `teacher/insights` 或并入工作台：

- 各发布的阶段卡点分布（学生停在哪个阶段）
- 量规维度薄弱项聚合（各维度档位分布）
- 反馈后改善情况（REVISE 后重交率、重交后评价变化）
- 数据全部来自现有表的聚合查询；一律只读，不新增写命令。图表用轻量方案，不引入重型图表框架。

**砍单规则**：若第 1 步严重超时，本步砍成只保留"量规薄弱项聚合"一张卡片，全力保 RAG。

### 第 3 步：信息架构重构 + UI 层级收敛（预估 1–1.5 天）

背景诊断（2026-08-27）：现有页面是按数据模型平铺长出来的——每个纵向切片往页面追加一个区块，叠加后认知负荷过大，学生任务页尤其严重。本步的核心是**减法和重组**，排版收敛只是收尾。

**总原则：每个页面先写下"这页替用户回答什么问题"；与该问题无关的信息一律收进二级入口（抽屉/独立页/折叠），默认不渲染。首屏只服务该问题。**

#### 3a. 学生任务页重构（最高优先级）

这页只回答两个问题："我现在要做什么" 和 "我交的东西怎么样了"。

- **首屏 = 当前阶段任务卡**：本阶段要完成什么、证据要求、截止时间/迟交警示，加一个唯一主按钮（继续编辑草稿 / 提交 / 开始重交，按状态三选一）。阶段步进器压缩为一行，只有当前阶段展开详情，其余点击展开。
- **完整发布快照降级为二级入口**：任务设置、三维目标、跨学科概念、背景设定、总体任务、任务链、评价标准全部移出常驻右栏，收进"查看完整任务书"（抽屉或独立页）。右栏拆除，工作区单列。
- **修订历史与教师反馈合并为一条时间线**："我的提交与反馈"——每个正式版本一个条目，教师对该版的反馈、量规评价直接挂在对应版本条目内（不再是并列区块），默认只展开最新一条，历史折叠。
- **操作跟随对象**："开始重交"等操作出现在它作用的版本条目上，只在相应状态出现，不做独立卡片。

#### 3b. 教师端收束

- 工作台：散落的"待反馈 n / 待评价 n / 待重交 n"徽章聚合为置顶"需要我处理"区块；"我的发布"压缩为单行摘要，细节进详情页。
- 教师提交详情页按同一总原则检查一遍：反馈/评价撰写是主操作，快照全文进二级入口。
- 学生列表按"待重交 → 进行中 → 已关闭"分组。

#### 3c. 全站排版收敛（收尾）

- 字号阶梯统一：正文 ≥14px、辅助文字 ≥12px，全站 4–5 档字号；间距/圆角/边框收敛为一套 token。
- 每页一个主标题、一个主操作；同级信息同级字号。
- 390px 与 1440px 两档宽度实际过一遍。

边界：只动呈现层与页面组织，不碰命令、查询合同与权限逻辑；受影响的页面测试允许同步改写断言。**新增切片只允许往二级入口加内容，不允许往任何页面首屏加区块**——此规矩冲刺后仍然有效。

本步完成即**功能冻结**：此后只修 bug 与文案。

### 第 4 步：演示工程（预估 1 天）

- **演示种子脚本**（`pnpm demo:seed`）：一条命令在开发库重建完整演示态——一个班、3–4 名学生、1 个走完全程的活动（多版提交、反馈、重交、量规评价，看板数据饱满）+ 1 个进行中的活动（现场演示发布→提交→反馈动线）+ RAG 语料就位。两个种子活动必须是情境叙事的最佳范例（第 1.5 步标准），不得使用旧的"纯作业"口吻任务书。
- **完整彩排**两遍，写下演示动线脚本（每一步点哪里、说什么、预期看到什么）。
- **录制备用视频**（完整动线一镜到底 + 分段素材；RAG 环节单独多录一版）。现场 Clerk/DeepSeek/网络任何一环故障都用视频兜底。

### 第 5 步：参赛材料（预估 0.5–1 天）

- 面向评委的说明文档/PPT，核心叙事三条：
  1. **可溯源的 AI**：RAG 引用 + AgentRun 审计 + 教师终审——AI 全程可解释、可追责；
  2. **完整可信的教学闭环**：设计→发布→分阶段实施→证据→反馈→重交→量规评价，全程不可变历史；
  3. **证据驱动评价**：评价必须引用真实学生证据，证据不足明确标注，不凭空给分。
- 视频精修（如需配音/字幕）。
- 处理彩排暴露的 bug 清单。

## 硬锚点（只有这三条卡日期）

1. **8/30 内必须进入第 4 步（演示工程）**——不管前面完成度如何，到点就按现状做种子数据和彩排。
2. **8/31 内必须录出备用视频**并完成材料初稿。
3. **9/1 为纯缓冲日**：不写新代码，只处理突发意外、材料终校、最后一次完整彩排。若有余力，可把 `codex/bootstrap-foundation` 合入 `main` 并打 tag。

前面各步做快了就顺势提前进入下一步；做慢了按各步砍单规则砍范围，硬锚点不动。

## 风险与预案

| 风险 | 预案 |
|---|---|
| RAG 检索质量差 | 语料小而精 + 确定性检索兜底；演示动线只走彩排过的提问 |
| DeepSeek 演示日不可用 | 备用视频兜底；RAG 检索入口在模型关闭时独立可用，仍可演示"可溯源" |
| UI 收敛改出回归 | 只动呈现层；收敛完成后跑全量 `pnpm check` + 手动过闭环 |
| 进度严重滑坡 | 按各步砍单规则砍范围；三个硬锚点任何情况下不得被开发挤占 |
| 语料版权/合规 | 参赛演示场景，语料仅节选并标注来源；不对外提供下载原文全文 |

## 冲刺纪律

- 每完成一步在本文件对应条目打勾并记一行实际情况。
- 任何"顺手做一下"的新想法一律记入文末"冲刺后再说"清单，不进当期。

## 冲刺后再说

- AI 评价建议（AI 起草量规评价 → 教师逐维度终审）
- main 分支合并与里程碑 tag（若 9/1 未做）
- 恢复常规验收流程，补记冲刺期变更的决策记录
- 全局侧边栏助手：把现有活动助手 handler 挂到全局入口，工具注册表按"导航（返回 href）→ 只读查询 → ActionIntent 写操作"三档逐步扩充
- 多模态课本入库管道：教师上传课本照片 → Vision 模型一次性识别（原文转录 + 插图教学含义）→ 持久化为 KnowledgeChunk（sourceType: TEXTBOOK），对话运行时始终读文本
- 教师样例上传进语料：复用同一条入库管道（sourceType: TEACHER_EXEMPLAR），准入门槛为"教师确认此样例可作参考"的显式动作；AI 生成的历史产物一律不得进语料
