# CDAS Next 产品边界

状态：第一阶段 v0.1 已完成外部验收；v0.2 结构化跨学科任务设计、v0.3 顺序阶段执行、D-032 作业小组共享提交、D-034 结构化形成性反馈、D-035/D-036/D-037 证据驱动量规评价、D-038 教师发布页反馈/评价覆盖计数、D-039 待重交/尚未正式提交、D-040 教师工作台待反馈/待重交计数、D-041 教师工作台待评价计数、D-042 学生活动列表待重交与 D-043 教师评阅名册导出均已完成开发期远程验收；D-044 AI 量规评价建议与 D-055 附件预览/自托管/评阅受限读取已完成本地开发门禁，待固定合成远程验收；官方课程标准检索已合入主线并由 D-058 扩展为 14 门课标；D-059 学校组织与管理员端、D-061 schema v3 任务书、D-062 教师建班与名单导入进入本地开发切片；长期方向见 `ROADMAP.md`
第一阶段基线日期：2026-08-18；长期方向记录日期：2026-08-23；官方依据检索合入日期：2026-08-27；D-055 接受日期：2026-08-29

## 一句话定位

CDAS Next 是面向 K12 教师与学生的跨学科学习活动工作台：教师设计并发布活动，学生提交可核验的学习证据，教师给出反馈；AI 作为理解当前业务上下文的助手参与准备、导航和分析，但不替代教师作出发布或评价决定。

## 首要问题

第一阶段只解决一个问题：让一次跨学科学习活动从教师设计走到学生提交，再回到教师反馈，过程清楚、归属明确、结果可追溯。

## 第一条完整闭环

1. 教师创建一份学习活动草稿。
2. 教师预览将要发布的内容与目标班级。
3. 教师确认后，将一个冻结版本发布给一个班级。
4. 班级学生查看活动并提交非空文本证据。
5. 教师查看提交并给出反馈。
6. 学生查看教师反馈。
7. 发布教师且仍为当前班级管理员，在第一方界面明确确认后关闭发布；关闭后学生不能再写入，但既有活动、提交和反馈仍可读取。

## 第一阶段必须成立的验收场景

- 教师只能向自己管理的班级发布。
- 学生只能看到发布给自己所在班级的活动。
- 发布后的内容形成不可变快照；后续修改从新草稿或新版本开始。
- 学生只能读写自己的提交。
- 教师只能查看和反馈自己发布活动下的提交。
- 正式提交至少包含一项有效文本证据。
- 只有发布教师且仍管理目标班级，才能在第一方界面明确确认后关闭发布；关闭不抹除既有历史，也不阻止有权教师继续反馈。
- AI 不可绕过教师确认发布活动，不可替教师提交最终反馈或量规评价。
- AI 服务不可用时，教师、学生的核心闭环仍然可运行。

## 第一阶段明确不做

本节表示“不进入当前验收合同”，不表示永久拒绝。已经接纳的后续方向、依赖顺序和进入开发条件统一记录在 `ROADMAP.md`；在新的决策和验收合同成立前，下列能力不得提前扩大当前实现范围。

- 多 Agent 协作
- 开放式、多来源或教师私有资料 RAG 知识库
- 学生互评与自评
- 班级小组和项目小组
- 多阶段自动流转
- AI 自动评分
- 教师自助调整班级成员（第一阶段由受控 operator CLI 预配置）
- 风险大屏和复杂学习分析
- 通知中心、全局搜索、个人设置等非核心模块
- 旧 `/api/v2` 兼容
- 旧数据库迁移

## 已接受的后续纵向切片

- 证据附件已按 D-025/D-026 完成开发期远程验收，并由 D-055 扩展自托管能力：学生可在文字工作草稿中补充最多 5 个 JPEG/PNG/WebP、PDF、DOC/DOCX 文件，单文件最大 20 MiB；只有已配置私有存储中对象元数据和文件头格式都与声明一致的附件才能进入不可变正式修订。Vercel 使用 Private Blob，自托管使用 release 目录外的持久磁盘。
- D-031 后正式提交可由非空文字、READY 附件或发布阶段内的已确认检查点满足；附件服务不可用时文字/检查点教学闭环仍可运行。
- D-055 允许学生与有权教师在授权下载路径上预览图片与 PDF，Word 保持下载；响应按服务端保存的媒体类型决定 inline，并统一使用 sandbox CSP。它还允许第一方教师评阅页的反馈/评价起草读取当前正式修订附件：图片由隔离视觉子调用转写，DOCX 在服务端做实际展开量受限的文本抽取；PDF 继续可由有权用户预览/下载，但在具备可证明的隔离解析资源边界前不进入 AI 起草。旧 `.doc`、PDF 自动解析/OCR、恶意文件扫描、文档高保真渲染和全局 Agent 附件工具仍不在当前合同内。
- 教师成员管理已按 D-027 完成开发期远程验收：学校或 operator 先为既有学生账号配置唯一名单码，班级管理员教师可在第一方界面批量粘贴名单码预览后明确确认加入，也可明确确认结束当前成员关系；重新加入必须追加新的有效区间，不能删除或改写旧历史。
- 首个成员切片不创建外部身份、不展示全局学生目录，也不做邀请、密码、Excel 文件解析、多租户、小组或 Agent 成员工具。
- D-062 补上教师自助建班与「学号、姓名」两列的 Excel 名单导入：教师逐行预览后确认，系统为本校新学号创建学生账号与本地凭据（初始密码由学号派生，首次登录强制修改），既有账号只被加入班级。原始 Excel 不保存，跨班转移、导入回滚、姓名回写和批量改密不在合同内。
- 结构化跨学科任务设计已按 D-030 完成开发期远程验收：原版 CTS/CDAS 构成能力下限。新活动保留学段/年级、原版学科目录、主学科/融合学科、三类作业及子类型、探究深度、提交模式、周期、跨学科概念、三维目标、背景设定、3–4 阶段任务链、类型化证据与四档评价描述；这些内容进入不可变草稿修订与发布快照，学生读取冻结后的同一任务书。
- D-061 起，既有 v1/v2 草稿、修订、发布快照与 hash 保持原样可读且不迁移；新的手工或 Agent 创建统一产生 v3 内容。既有未封存 v2 草稿仍按 v2 原版本编辑，不由 Agent 或保存命令静默升级。
- D-031 已按原版 CTS/CDAS 的真实阶段行为完成开发期远程验收：新 `phased`/`mixed` 发布按冻结快照顺序形成每阶段独立草稿与不可变正式修订，检查点只能引用发布定义，提交后幂等准备下一阶段；旧发布和 `once` 任务继续使用整项提交。通用流程引擎、条件分支和回溯不混入该切片。
- D-032 已按原版实际小组能力下限完成开发期远程验收：教师按 Release 建组并设置成员角色，同组共享阶段 Submission、附件、反馈与教师进度，开始提交后冻结小组归属；实时协同、贡献度分析、同伴互评和跨作业固定分组不进入本切片。
- D-033 接受受约束的设计助手增强；D-061 将新建提案升级为 schema v3：资料不足时每轮只追问一个会改变任务设计的必要问题，资料充分后展示结构化理解、明确假设与可追查依据，教师确认后才把同一份内容保存为可编辑草稿。诊断是可编辑建议而非课程质量结论，拒绝确认或模型失败均不产生草稿，手工表单与既有发布流程保持独立可用。
- D-033 只借鉴成熟开源 Agent 的“先理解、后确认、再执行”、窄工具、显式停止和可恢复失败模式；不引入通用 Agent 框架、持久工作流、多 Agent、长期记忆、第二后端或自动发布。
- 主线已交付的官方依据检索使用教育部《义务教育课程方案（2022年版）》及 14 门 2022 年课程标准的已圈定章节；综合实践活动暂无独立课标。助手通过两个只读工具先检索、再读取原文；命中已收录学科时，结构化提案至少展示两个不同官方来源及采用理由。引用是当前提案的可追查依据，不是课程标准合规结论，也不随草稿正文持久化。UbD、C-POTE、T/CAET、设计流程、教材、教师样例与 AI 历史产物均不进入生产语料。
- D-034 已完成开发期远程验收：教师在确认每版反馈时，同时选择“继续后续阶段”或“按反馈修改并重交”，并选择基础支持、标准任务或挑战拓展。三档只调整反馈中的支架强度和下一步建议，不改变发布快照中的共同核心目标；学生沿既有不可变修订与重交流程响应，个人与小组继续服从原资源边界。
- D-034 不引入自动反馈、自动评分、教师批准后才解锁阶段的强制闸门或长期学生能力画像。AI 建议即使可用也必须经过现有教师确认链；模型不可用时结构化手写反馈与重交完整可用。
- D-035 已完成开发期远程验收：教师对当前正式修订按冻结的结构化 v2/v3 量规手写确认证据绑定评价。量规评价是独立聚合，不混入形成性反馈；必须覆盖全部冻结维度，等级判断须引用本版文字、READY 附件或已确认检查点，也可明确标记证据不足。schema v1 快照不开放评价；关闭后有权教师仍可评价。本切片不新增 Agent 工具，也不保存 AI 建议与教师终审差异。
- D-036 已完成开发期远程验收：教师 Release 正式提交列表只读展示当前修订的反馈版本与量规评价状态（待评价 / 已评价 vN / 无量规），不展开综评或维度结果，也不把评价写成阶段完成条件。
- D-037 已完成开发期远程验收：学生活动列表只读展示当前正式修订是否已有量规评价，不把综评或维度结果带进首页。
- D-038 已完成开发期远程验收：教师 Release 正式提交页只读展示当前正式修订的反馈与评价覆盖计数（已反馈 n/m、已评价 n/m），不展开综评、趋势或阶段完成条件。
- D-039 已完成开发期远程验收：教师 Release 正式提交页只读展示待重交、重交中，以及班级进度上当前阶段尚未正式提交；不展开工作草稿正文、综评或趋势。
- D-040 已完成开发期远程验收：教师工作台「我的发布」只读展示各发布当前正式修订的待反馈与待重交计数；班级管理权已变更时不显示计数。
- D-041 已完成开发期远程验收：教师工作台「我的发布」只读展示各发布当前正式修订的待评价计数；schema v1 不显示待评价。
- D-042 已完成开发期远程验收：学生活动列表只读展示待重交与重交中，不展开反馈正文或综评。
- D-043 已完成开发期远程验收：教师可从 Release 正式提交页下载当前正式修订评阅名册 CSV，不导出证据正文、综评或学生 UUID。
- D-044 接受第一方教师评阅页上的 AI 量规评价建议，D-055 再把同一当前正式修订中逐件重新授权且成功读取的附件加入证据边界：模型读取冻结量规、可见文字、已确认检查点与受限附件转写，返回覆盖全部维度的可编辑建议；读取失败或不支持的附件不能被引用。建议直接回填既有表单，未经教师逐维修改并走现有独立确认链不会形成评价历史。
- D-044 不保存 Prompt、模型原始建议或人机差异，只以成功 `AgentRun`、绑定当前正式修订与评价版本的 `ActionAudit`，以及最终评价修订上的 `AI_ASSISTED` 来源记录“本版曾由 AI 起草”。模型失败、输出非法、教师放弃或 `AI_PROVIDER_DISABLED=1` 均不产生评价或 ActionIntent，手写评价路径保持完整可用。

## 产品原则

1. 先完成真实闭环，再增加功能宽度。
2. AI 输出默认是建议、草稿或预览，不是已生效业务事实。
3. 发布、评价、删除和成员变更必须由有权限的人明确确认。
4. 页面只展示已经可用的能力，不放无业务行为的占位按钮。
5. 每项能力必须同时考虑正常流程、无权限、数据不存在、重复操作和外部 AI 失败。

## 第一阶段完成标准

只有当上述闭环能够由真实教师和学生账号从头走通，并通过权限反例测试与人工体验检查后，第一阶段才算完成。代码数量、页面数量、框架数量和 AI 工具数量都不是完成指标。

## 长期产品方向

第一阶段完成后，CDAS Next 将在现有可追溯闭环上逐步扩展为“跨学科任务设计与评价一体化平台”：面向单教师承担跨学科设计的真实场景，增加有依据的结构化任务设计、多阶段学习证据、形成性反馈、差异化支持、证据驱动评价、过程诊断和复用能力。

长期方向遵守以下产品约束：

- AI 负责澄清、建议、诊断和解释，教师负责发布、反馈与最终评价。
- 学习评价必须引用实际证据；证据不足时不得强行得出结论。
- 先完成手工可用的业务纵向切片，再增加模型、知识库或自动化增强。
- 学校、班级、小组、教师个人资料和学生证据都必须有服务端资源级授权与可追溯历史。
- 后续功能不得回拨或覆盖已经发布的活动、正式提交、教师反馈、评价、审计或成功幂等结果。

完整的候选功能、设计词汇、依赖顺序与非目标见 `ROADMAP.md`。该路线不会改变本文件前述第一阶段完成标准。
