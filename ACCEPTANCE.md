# CDAS Next 第一阶段验收合同

状态：启动基线（v0.1）；D-060 本地认证、会话与门禁迁移已完成源码收尾
日期：2026-09-01

本文件把 `PRODUCT.md`、`DOMAIN.md` 与 `AGENT.md` 的第一阶段边界转成实现和测试必须共同满足的行为合同。`ROADMAP.md` 记录的是后续候选能力，不属于本文件的当前验收范围；任何路线能力进入开发时必须在同一变更中补充对应的权限矩阵、正常路径和反例场景。

## 权限矩阵

表中的“允许”始终包含服务端资源归属判断，不只判断角色。对调用者无权读取的具体资源，默认返回不存在，避免枚举资源。

| 操作 | 草稿所有者教师 | 发布教师且仍管理班级 | 其他教师 | 当前成员学生本人 | 历史成员学生本人 | 其他学生 | Agent |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 读取、修改未发布草稿 | 允许 | 仅同时为所有者时允许 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 继承当前教师权限，L1 |
| 发布到班级 | 所有者且管理班级时允许 | 允许 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 继承权限，必须人工确认 |
| 关闭发布实例 | 仅同时为发布教师且仍管理班级时允许 | 必须在第一方 UI 明确确认后允许 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 第一阶段不提供关闭工具 |
| 读取发布实例 | 允许预览 | 允许 | 拒绝 | 当前可见时允许 | 成员有效期曾与活动开放期重叠时允许 | 拒绝 | 继承当前用户权限 |
| 保存或正式提交 | 拒绝 | 拒绝 | 拒绝 | 仅自己的 active 活动允许；关闭后只读 | 只读 | 拒绝 | 第一阶段不提供学生提交工具 |
| 读取提交证据 | 仅同时满足右侧发布权限时允许 | 允许读取该发布下的提交 | 拒绝 | 仅自己的允许 | 仅自己的允许 | 拒绝 | 继承当前用户权限 |
| 增加、移除或刷新附件 | 拒绝 | 拒绝 | 拒绝 | 仅自己的 active 工作草稿允许 | 拒绝 | 拒绝 | 当前不提供附件工具 |
| 下载附件 | 仅同时为发布教师且管理班级时可读正式附件 | 允许读取该发布下正式附件 | 拒绝 | 仅自己的工作草稿或正式附件 | 仅自己的既有附件 | 拒绝 | 当前不提供附件工具 |
| 保存结构化形成性反馈 | 仅同时满足右侧发布权限时允许 | 目标仍为当前正式修订时允许；正文、下一步与支架层级须一起确认 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 继承权限，必须人工确认 |
| 起草或保存证据绑定量规评价 | 仅同时满足右侧发布权限时允许 | v2 当前正式修订可请求只读 AI 起草；最终保存仍须覆盖全部冻结维度并由教师确认 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 第一方页面只起草，不提供直接保存评价工具 |
| 调整班级成员 | 仅同时为班级当前管理员时允许 | 班级当前管理员在第一方 UI 明确确认后允许 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 当前不提供成员工具 |
| 建班与按名单导入学生 | 仅本校 ACTIVE 教师为自己建班时允许 | 班级当前管理员在第一方 UI 逐行预览并明确确认后允许 | 拒绝，且他校或他人班级返回不存在 | 拒绝 | 拒绝 | 拒绝 | 当前不提供建班或导入工具 |
| 学校与教师管理 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 拒绝 | 当前不提供学校工具 |
| 检索首版官方课程依据 | 允许 | 允许 | 允许 | 拒绝教师入口 | 拒绝教师入口 | 拒绝教师入口 | 继承当前教师身份，只读 L0 |

Agent 不是新的业务主体。审计中的 actor 始终是登录用户，`agent_run_id` 只表示调用来源。

### 本地认证权限矩阵

| 场景 | ADMIN | ACTIVE 学校的 ACTIVE 教师 | ACTIVE 学校的 ACTIVE 学生 | DISABLED 账号 | DISABLED 学校成员 | 未认证调用者 |
| --- | --- | --- | --- | --- | --- | --- |
| 使用本角色登录入口 | 允许 | 允许 | 允许 | 拒绝 | 拒绝 | 仅可查看登录页 |
| 首次登录后强制改密 | 适用 | 适用 | 适用 | 拒绝 | 拒绝 | 拒绝 |
| 建立、撤销自己的会话 | 允许 | 允许 | 允许 | 不建立新会话 | 不建立新会话 | 拒绝 |
| 进入角色工作台 | `/admin` | `/teacher` | `/student` | 拒绝 | 拒绝 | 重定向登录 |
| 读取或写入教学资源 | 仅管理员命令，不读取教学内容 | 仍按所有权与班级管理关系 | 仍按当前/历史成员关系与资源归属 | 拒绝 | 拒绝 | 拒绝 |

认证只识别 actor，不替代授权。角色登录入口不匹配、跨校 identifier、跨校资源 ID、其他教师或学生的资源都不得因持有有效 session 而变为可见。

```gherkin
场景: ACTIVE 教师正常登录并建立本地会话
  假如教师账号、所属学校和本地凭据均为 ACTIVE
  当教师在教师登录页提交正确学校代码、工号和密码
  那么系统建立 12 小时本地会话并进入教师工作台
  并且后续教学操作仍按草稿所有权和班级管理关系授权
```

```gherkin
场景: 会话 Cookie 与已配置公开 origin 的传输协议一致
  假如部署的公开 origin 为 HTTPS，或生产环境未配置有效公开 origin
  当任一角色使用正确凭据建立、续期或清除本地会话
  那么 cdas_session Cookie 必须带 Secure 属性
  假如受支持的自托管公开 origin 明确为 HTTP
  当任一角色使用正确凭据建立、续期或清除本地会话
  那么 cdas_session Cookie 不得带 Secure 属性，且同一 HTTP origin 的后续工作台请求必须携带该会话
```

```gherkin
场景: 停用账号或学校拒绝登录和既有会话访问
  假如账号或其所属学校已被管理员停用
  当该用户提交正确密码或使用停用前建立的会话访问工作台
  那么系统拒绝访问且不删除任何既有教学历史
```

```gherkin
场景: 跨校身份与资源拒绝
  假如两所学校存在相同工号或学号且各自拥有独立资源
  当甲校用户以乙校代码登录或持甲校会话访问乙校资源
  那么登录失败或资源表现为不存在
  并且不得泄露乙校账号或资源是否存在
```

平台管理员由受控 `bootstrap:admin` CLI 建立；学校、教师和学生账号由本地 provisioning/import 路径建立。CLI 不得覆盖既有角色、显示名称、班级管理员或成员历史。D-027 起，班级当前管理员教师可以管理既有学生账号的成员关系；创建账号、重置密码和身份资料修改仍不是 Agent 工具。

所有写命令的 actor、调用来源、追踪号和时钟都由服务端入口注入。客户端即使提交同名字段也必须被输入合同拒绝，不能覆盖本地会话或回拨命令时间。

工作台导航必须明确显示服务端 `AppUser` 的当前账号名称、业务角色和退出登录入口。教师账号进入 `/student` 或学生账号进入 `/teacher` 时，根工作台只能显示不含业务资源的角色说明、正确工作台入口与退出入口；带具体资源 ID 的越权访问仍返回不存在，不能以角色提示泄露资源是否存在。

## 跨能力反例

每一项业务能力都至少验证以下五类路径：

| 能力 | 正常路径 | 无权限 | 不存在或并发变化 | 重复操作 | 外部依赖失败 |
| --- | --- | --- | --- | --- | --- |
| 草稿编辑 | 创建新版本和变更摘要 | 无写入 | 旧版本返回冲突 | 相同幂等请求返回原结果 | AI 失败时仍可手工编辑 |
| 结构化跨学科任务书 | 新建 v3 保存基本设置、真实情境、可观察目标与核心素养、学科贡献、3–4 阶段证据链和四档量规 | 非所有者不可读写 | 非法核心素养、目标未覆盖或陈旧版本拒绝；既有 v1/v2 历史不改写 | 相同幂等请求返回原版本修订 | AI 不可用时手工 v3 表单仍可完整设计和发布 |
| 受约束设计助手 | 必要澄清后展示结构化理解、假设、学科必要性和官方依据，教师确认后创建 v3 草稿 | 学生、其他教师或伪造审批无写入 | 缺少必要信息、非法 schema 或审批参数变化时不执行；既有 v2 修改保持 v2 | 同一批准调用只创建一份草稿 | 模型不可用、输出失败或教师拒绝时手工 v3 表单仍可用 |
| 官方课程依据检索 | 确定性检索课程方案与 14 门白名单课程标准；模型先检索再读原文，命中已收录学科的提案至少引用两个不同来源 | 学生不能进入教师检索页；工具不得读取文件路径、任意 URL 或私有资料 | 未知来源/章节、伪造片段或不匹配引用拒绝；零命中明确返回无依据 | 相同输入返回稳定排序、稳定 ID 与相同片段 | 模型不可用时教师页面仍可独立检索；检索失败不阻塞手工设计闭环 |
| 顺序阶段执行 | 按冻结任务书保存并正式提交当前阶段，随后准备下一阶段 | 非成员、其他学生和非管理教师不可读写 | 越界、跳阶段、关闭后写入或陈旧草稿拒绝 | 相同阶段提交返回原修订与同一下一阶段草稿 | AI 不可用时文字、已支持附件和检查点仍可提交 |
| 作业小组共享提交 | 教师按 Release 建组并设置角色；同组共享阶段草稿、修订、附件、反馈和进度 | 组外学生、未分组学生和非管理教师不可读写 | 重复入组、已有个人提交、陈旧共享草稿或提交后改组拒绝 | 相同建组或小组阶段提交返回原结果 | AI 或附件存储不可用时同组仍可用文字/检查点完成 |
| 发布 | 产生唯一冻结 Release | 无 Release，记录拒绝 | 草稿变化使确认失效 | 返回同一 Release | AI 不可用时普通 UI 仍可发布 |
| 关闭发布 | 发布教师且当前班级管理员确认后由 active 变为 closed | 非发布者或不再管理班级者无写入 | Release 已关闭、状态变化或确认过期时拒绝 | 返回同一关闭结果 | AI 关闭时第一方 UI 仍可完成关闭 |
| 学生提交 | 产生不可变 Revision | 其他学生不可读写 | 活动关闭或版本过期时拒绝 | 不增加第二份修订 | AI、对象存储等外部服务不可用时文本证据仍可提交 |
| 证据附件 | 私有上传、元数据与文件头验证通过后进入同一正式修订；图片/PDF 可授权预览 | 其他学生和教师无对象 URL、storage key 或元数据 | 工作版本变化、对象元数据或文件头不符时拒绝正式提交；Word/未知类型不可 inline | 相同预留不增加第二个资产 | 配置的附件后端暂不可用时暂停附件，纯文字提交仍可用 |
| 班级成员管理 | 名单码预览后确认加入，或确认结束当前区间 | 非管理员、学生和 Agent 无写入且不能枚举学生 | 名单码、成员状态或班级版本变化时要求重新预览 | 相同幂等请求返回原结果且不增加区间 | 认证会话之外无外部身份调用；既有 AppUser 名单读取与变更仍可完成 |
| 结构化形成性反馈 | 正文、继续/重交指令与基础/标准/挑战支架一起绑定当前提交修订 | 其他教师不可写，其他学生或组外学生不可读 | 学生已重交、反馈版本变化或确认参数变化时要求重新确认 | 相同幂等请求返回原结果，不增加第二个反馈修订 | AI 建议失败时仍可手写、确认并由学生重交 |
| 证据绑定量规评价 | 覆盖全部冻结维度；等级须引用本版证据，或明确标记证据不足 | 其他教师不可写，其他学生或组外学生不可读；v1 快照不可写 | 学生已重交、评价版本变化或确认参数变化时要求重新确认 | 相同幂等请求返回原结果，不增加第二个评价修订 | AI 不可用时仍可手写确认；关闭后有权教师仍可评价 |
| AI 量规评价建议 | 当前正式修订的全部冻结维度回填可编辑建议；可引用可见文字、已确认检查点或本次成功读取的正式附件 | 其他教师、学生与已失去班级管理权的发布者得到资源级不存在；附件逐件复用下载授权且文件名/storage key 不进模型 | v1、无正式修订、陈旧页面、不可读附件引用或非法模型 schema 不起草评价 | pending 时不并发；每次新的明确起草请求留下独立 AgentRun，但不增加评价历史 | 模型或附件读取失败只关闭/降级当前建议并保留手写表单；禁用时不渲染入口、不建 run/provider |
| AI 形成性反馈建议 | 回填引用本版文字、已确认检查点或成功读取附件事实的正文与下一步/支架选择，教师改后独立确认 | 其他教师、学生与已失去班级管理权的发布者得到资源级不存在；附件逐件复用下载授权且文件名/storage key 不进模型 | 学生已重交、反馈版本变化或陈旧页面不起草；最终准备时必须存在绑定同一正式修订与反馈版本的成功建议审计，其他成功 Run 不能冒充来源 | 起草不写反馈历史；每次明确起草留下独立 AgentRun；匹配建议的相同准备幂等请求只返回原 ActionIntent | 模型失败只关闭 AgentRun 并保留手写表单；单附件不可读时明确交给教师核对，禁用时不渲染入口 |
| 教师全局 Agent 会话 | AI 开启且教师已认证时，通过不占页面流的悬浮入口打开独立内存会话，复用既有官方依据、草稿与发布职责 | 学生、未登录、身份不符或上下文不可建立时无入口；API 与工具仍逐请求、逐资源授权 | 关闭再开保留本次内存消息；跨教师页导航保留；刷新、离开教师区或直接深链不恢复消息或审批 | 会话本身不写业务历史；现有签名 approval、命令幂等与 AgentRun 规则不变 | AI 禁用或配置不完整时不渲染、不查询助手班级、不构造 provider，手工页面布局与闭环不变 |
| 教师 Agent 页面上下文与工作区摘要 | 每次请求识别白名单教师页面；列出本人班级、草稿、发布与待办并返回 canonical 站内链接 | 客户端页面提示不授予权限；其他教师资源、伪造 UUID/标题/计数/href 不进入结果或历史；不返回学生、正文、反馈或评价明细 | 动态资源须仍存在于当前授权工作区；失去权限或资源变化时降级为不可用上下文，列表按新结果重算 | L0 查询不写业务历史；相同工作区映射为稳定结果，教师点击链接才导航 | 查询或模型失败时就近失败，AI 禁用时手工工作台与导航不变 |
| 教师 Agent 读取本人草稿正文 | 助手读回本人某一份草稿的当前结构化 v2/v3 任务书，据此逐条指出情境承接与证据链问题并给出可替换文字 | 其他教师的草稿与伪造 UUID 不触发查询，一律资源级不存在；不返回学生、提交、反馈、评价或 Release 标识 | 草稿在两轮之间被改写、删除或转手时按当前授权重算，旧貌不留在模型历史；v1 快照只返回标题与链接 | L0 只读不写业务历史；同一请求对同一草稿只作一次授权判定 | 草稿读取失败时在建立 AgentRun 之前就近失败；AI 禁用时草稿页手工闭环不变 |
| Agent 工具参数修复重试 | 一次非法提案参数经修复后仍作为待确认卡片呈现，教师确认才写入 | 修复使用无 execute 的 schema 注册表，过程本身不执行工具、不写数据，也不放宽授权、通读账本或版本校验 | 修复结果走与首次相同的 schema 与领域校验，草稿在此期间变化仍由 `expectedVersion` 失败关闭 | 每请求最多修复一次，第二次非法参数按原路失败关闭，不产生额外 AgentRun 或审计 | 修复失败、超时或被中断时与从未修复一致，保留"草稿未创建"提示与手工降级 |
| Agent 读取发布提交名册 | 按第一方名册页顺序返回对象的阶段位置、提交与反馈/评价状态、跟进标记和行级评阅链接，全部以匿名序号呈现 | 其他教师、伪造 UUID 与失去班级管理权后不触发查询即资源级不存在；响应不含学生姓名、小组名或任何裸 UUID 字段 | 历史中伪造的姓名、状态与计数按当前授权整块重算；发布关闭后仍可读且与第一方页面一致 | 只读查询，无幂等写入，不创建 ActionIntent 或修订；超过 60 个对象时截断并标注 | 查询或模型失败时就近失败，AI 禁用时名册页与导出的手工闭环不变 |
| Agent 读取发布过程诊断 | 返回与过程诊断页一致的阶段分布、量规各档分布与最弱维度、重交后上升/持平/下降计数，并给出该页链接 | 其他教师的发布与伪造 UUID 不触发查询即资源级不存在；结果不含学生或小组姓名、成员数、提交 ID、证据、反馈或综评 | 历史中伪造的诊断结果按当前授权重算；发布或班级权限变化后不再返回 | 只读查询，无幂等写入，不创建 ActionIntent 或修订 | 查询或模型失败时就近失败，AI 禁用时过程诊断页与手工闭环不变 |
| 教师确认后 Agent 改写草稿 | 读过同一版本后按声明的区域改写，教师确认才写入新版本，原版本保留为历史修订 | 未读、版本不符或非本人草稿在教师看到确认卡之前即被自动拒绝；封存草稿与他人草稿由既有命令失败关闭 | 教师在别处改过这份草稿时 `expectedVersion` 不符，改写失败且原内容一字不变 | 每次请求最多改写一份；相同幂等请求返回原结果，不增加第二个修订 | 模型或命令失败时零写入，AI 禁用时草稿页手工编辑与版本历史不变 |
| 提交列表评价状态 | 发布列表只显示当前修订待评价、已评价版本或无量规；学生列表只显示当前修订是否已有评价 | 其他教师不可读教师列表；无权学生看不到对应活动；不返回综评或维度结果 | 重交后列表跟随新当前修订，旧评价不出现 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 学生列表跟进标记 | 学生活动列表只显示待重交或重交中 | 无权学生看不到对应活动；不返回反馈正文、`nextStep` 或综评 | 开始重交后待重交改为重交中 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 发布页评阅名册导出 | 有权教师下载当前正式修订评阅状态 CSV | 其他教师得到 404；CSV 不含证据正文、反馈正文、综评或学生 UUID | 重交后导出跟随新当前修订 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 发布页反馈/评价覆盖 | 正式提交标题旁只显示当前修订已反馈 n/m 与已评价 n/m | 其他教师不可读；不返回综评、趋势或工作草稿；v1 不显示评价覆盖 | 重交后计数跟随新当前修订 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 发布页跟进标记 | 正式提交行显示待重交或重交中；班级进度在当前阶段有未正式提交容器时显示尚未正式提交 | 其他教师不可读；不返回工作草稿正文、综评或趋势 | 开始重交后待重交改为重交中；正式提交后尚未正式提交消失 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 工作台发布待办 | 教师工作台只显示各发布当前修订待反馈 n、待评价 n 与待重交 n | 其他教师工作台不含该发布；班级管理权已变更时不显示计数；v1 不显示待评价；不返回综评或草稿正文 | 开始重交后待重交归零；评价后待评价减少 | 只读查询，无幂等写入 | 不依赖 Agent 或模型 |
| 学校与教师管理 | 唯一 ADMIN 建校、改名、启停、重置邀请码，并登记待开通教师 | 教师、学生和其他账号无写入且不能枚举他校；停用后业务入口拒绝 | 同校工号冲突、学校已停用或资源不存在时拒绝 | 相同幂等请求返回原学校/教师且不泄露邀请码明文 | 不调用认证供应商；认证不可用时管理员仍可改学校状态 |
| 班级与学生名单导入 | 教师建班后上传「学号、姓名」名单，逐行预览后确认，新建账号并加入本班 | 他校教师、非本班教师、停用账号或停用学校在准备阶段即被拒绝且零写入 | 冲突行、班级版本变化或过期确认整单失败；已在本班的行只计入跳过 | 相同幂等请求重放同一导入结果，不重复建号或重复入班 | 不依赖 Agent 或模型；解析在服务端完成且不保存上传文件 |
| Agent 高影响动作 | 确认后执行一次 | 领域权限仍可拒绝 | 过期、参数或版本变化时拒绝 | 同一运行返回已保存结果；跨新运行安全失败且不重复 | 工具前中断零写入；已确认命令提交后不因后续流中断回滚或伪报失败 |

## 最小端到端场景

```gherkin
场景: 教师手工创建并发布不低于原版的跨学科任务书
  假如教师选择匹配学段和年级的主学科、至少一个不同的融合学科
  并且为每个已选学科填写贡献与不可替代性，选择合法作业类型、条件子类型、提交模式和 1–16 周周期
  并且填写真实背景、2–8 条可观察目标，每条引用 1–3 条匹配学段年级的已选学科核心素养
  并且填写 3–4 个连续阶段，每阶段都有核心动作、情境承接、学习支架、类型化提交证据、评价要点、课时建议和目标关联
  并且评价至少包含问题意识、证据质量、跨学科连接、方案表达及其优秀/良好/达标/待改进描述
  并且每条目标都至少被一个阶段承担并被一个量规维度评价
  当教师保存草稿
  那么系统追加完整的 schema v3 修订，且草稿 head 与该修订精确一致
  当教师预览并明确确认发布精确版本
  那么 Release snapshot 以 schema v3 冻结全部字段并生成对应的规范化 hash
  并且有权学生看到同一基本设置、背景、学习目标与课程依据、学科贡献、阶段行动、支架、证据与评价标准

场景: 管理员建立学校边界并登记待开通教师
  假如平台已有唯一管理员
  当管理员创建两所学校并在两校登记相同工号
  那么两名教师都成功且互不可见
  并且同校再次使用该工号被拒绝
  当管理员停用其中一所学校
  那么该校教师不能进入教学工作台
  并且既有活动、提交与评价 ID 与内容保持不变
  当该校教师在停用前已开始请求但尚未执行新的领域命令
  那么停用后的草稿、发布、成员、提交、附件、反馈与评价写入都被拒绝
  并且既有成功幂等结果与教学历史不被改写
  当非管理员调用建校命令
  那么请求被拒绝且不出现新学校
  当尝试把已有班级改到另一所学校
  那么数据库拒绝变更且既有成员关系保持同校

场景: 本地登录拒绝账号枚举并限制锁定期资源消耗
  假如本地认证中存在一个未锁定账号、一个已锁定账号和一个不存在的同格式 identifier
  当同一角色分别使用错误密码登录未锁定账号与不存在账号
  那么两次都返回相同的账号或密码错误结果，且都执行一次同参数 Argon2id 派生
  并且两次都不创建会话，也不泄露账号、工号、学号或学校是否存在
  当使用任意密码登录仍在锁定期内的同角色账号
  那么系统返回账号锁定，且不执行密码派生、不增加失败次数、不延长锁定截止时间
  当锁定截止时间到达后使用正确密码登录
  那么系统创建只在数据库保存令牌 SHA-256 的会话，并清零失败次数与锁定时间
  当使用不匹配的角色登录已锁定账号
  那么系统仍走不存在账号的等成本路径并返回相同的账号或密码错误结果，不泄露锁定状态

场景: 结构化内容兼容旧历史且不伪造字段
  假如既有草稿、修订或 Release snapshot 使用 schema v1
  当教师或学生读取既有资源
  那么系统继续展示原有六段内容，不伪造 v2 任务书字段，也不改变原 snapshot hash
  当教师读取既有未封存的 v1/v2 草稿
  那么既有编辑器按原 schema 提供兼容路径，不静默升级或改写历史
  并且新建入口另行产生 schema v3 修订

场景: 设计助手在教师确认理解后才创建结构化草稿
  假如模型可用且教师描述了跨学科活动
  当描述缺少一个会改变年级、任务、证据或评价结构的必要条件
  那么助手每轮只提出一个必要问题，且不调用 create_activity_draft
  当资料足以形成任务书
  那么助手先调用 search_knowledge 检索首版教育部官方白名单，并对相关结果调用 read_source_section 核对原文
  并且当前活动包含语料已收录学科时，create_activity_draft 提案至少引用两个不同官方来源
  并且 read_source_section 只能读取同一会话此前检索命中的章节，create_activity_draft 的每条引用都已在本次会话读过；跳过任一步均不展示确认、不写草稿
  并且引用的来源 ID、章节 ID、标题与站内链接逐字匹配服务端语料，客户端伪造片段或来源时请求失败关闭
  并且执行前展示任务理解摘要、教师已提供要求、模型假设、每个融合学科的必要贡献、知识/过程/情感目标各自对应的任务—证据—评价链，以及可点开的官方依据与采用理由
  并且这些说明明确标示为可编辑建议和设计依据，不宣称课程标准映射、自动合规或教育效果
  并且当提案工具因输入校验或 JSON 截断未能展示确认时，AgentRun 以 FAILED 结束，failure_code 带可诊断后缀，助手界面给出明确终态而非持续处理中
  当教师尚未确认或明确拒绝
  那么系统不创建 ActivityDraft、ActivityDraftRevision 或草稿动作审计
  当教师批准签名的精确方案
  那么工具只接受满足 D-061 合同的完整 schema v3 任务书，并通过共享 saveActivityDraft 命令追加一条 AGENT 修订
  并且写入成功后停止模型 loop，渲染精确站内预览链接；教师点击后才进入既有可编辑预览，该理解确认不能替代后续发布确认
  当客户端伪造签名、改变工具参数或重复提交不同参数
  那么服务端拒绝写入，不放宽 actor、AgentRun、幂等或 schema 校验
  当模型不可用或被禁用
  那么教师仍可通过普通表单创建、编辑、预览和发布完整 v3 活动，且不要求存在 AgentRun
  并且教师仍可在独立课程标准检索页查询同一官方白名单

场景: 教师通过不改变页面布局的悬浮入口使用独立 Agent 会话
  假如 AI 配置完整且当前用户经服务端确认是教师
  当教师打开工作台或任意非活动教师页面
  那么页面右下角显示唯一圆形 Agent 入口，且入口不进入主内容流、不改变页面宽度、grid、sticky rail 或手工控件
  当教师打开入口
  那么系统显示非模态独立会话面板，并可继续使用既有官方依据检索、结构化草稿提案与精确发布确认
  并且面板标题合并职责说明，边界说明随对话记录滚动，输入框与提交按钮共用同一描边容器且保持贴底可见
  并且创建草稿和发布仍分别等待原有签名 approval 与领域授权，面板不获得额外权限
  当教师关闭面板后重新打开或在教师页面间客户端导航
  那么同一内存消息和待处理审批仍保留
  当教师刷新、离开教师工作区或直接打开深链
  那么系统不从 URL、localStorage、cookie 或数据库恢复消息、审批或长期记忆
  当视口为桌面、平板或 320–390px 窄屏
  那么面板内消息独立滚动、输入与关闭可达，页面不产生横向溢出，既有 ConfirmDialog 仍保持自己的模态语义
  当 AI 被禁用、配置不完整、用户未登录、用户是学生或教师上下文不可建立
  那么系统不渲染悬浮入口、不查询助手班级、不构造模型 provider
  并且教师手工创建、编辑、预览与发布闭环和学生页面布局保持不变

场景: 教师 Agent 安全识别当前页面并查询本人工作区摘要
  假如 AI 配置完整且当前教师拥有两个班级、若干草稿和发布
  当教师从工作台打开 Agent 并询问当前页面、班级、草稿、发布或待办
  那么客户端只提交白名单页面类型和可选 UUID，不提交 query string、页面正文、任意 URL 或 DOM
  并且系统通过同一教师工作台授权查询返回本人资源的名称、最小摘要和 canonical `/teacher/...` 链接
  并且发布待办只包含待反馈、待评价与待重交计数，不包含学生姓名、提交 ID、证据、反馈、评价或草稿正文
  当教师点击结果中的链接
  那么 Next 客户端导航保留同一内存会话，下一次发送读取新的当前页面类型；模型本身不能自动跳转
  当客户端提交其他教师的动态资源 UUID，或伪造历史中的标题、数量、ID 或 href
  那么服务端不反射该路径，不把资源放入结果或模型历史，并将无权动态上下文降级为不可用
  当教师失去目标班级管理权
  那么对应提交入口与待办不再返回，但不泄露资源是否仍存在
  当教师指着自己的一份草稿要助手看看设计
  那么助手先读回该草稿当前修订的完整结构化 v2/v3 任务书，再按逆向设计与情境叙事标准逐条指出问题并给出可直接替换的文字
  并且结果只包含该草稿的状态、版本、更新时间、是否已发布和 canonical 编辑/预览链接，不包含 Release 标识、学生、提交、反馈或评价
  并且助手不改写教师已有草稿；教师要落地修改仍走草稿页表单或另建一份草稿
  当教师提交其他教师的草稿 UUID 或伪造标识
  那么服务端不发出草稿查询，结果与草稿不存在完全一致
  当草稿在两轮之间被改写、删除或转手
  那么客户端历史中的旧正文按当前授权重算后不进入模型
  当该草稿是 schema v1 快照
  那么只返回标题与链接，正文不进模型，助手请教师打开草稿页处理
  当草稿读取遭遇基础设施失败
  那么请求在建立 AgentRun 之前就近失败，不留下悬空运行
  当教师说"就按你说的改"并要求把修改落到草稿上
  那么助手只能改写它本轮已读到的同一版本，并逐条声明改动了哪几个任务书区域及理由
  并且服务端按 v2 字段归属计算真实差异，声明与差异必须完全相等，越界改动、虚报区域与零改动一律失败且零写入
  并且系统展示这些声明并等待教师确认，教师确认后才写入新版本，原版本保留为历史修订
  当助手提出改写一份它没读过、版本不符或不属于该教师的草稿
  那么系统自动拒绝，不向教师展示任何描述改写其草稿的确认卡
  当教师在别处已经改过这份草稿
  那么改写因版本不符失败，草稿原内容一字不变
  当教师拒绝确认，或同一请求中再次尝试改写
  那么零写入，且不重复提出同一次改写
  当 AI 被禁用、查询失败或模型失败
  那么不新增业务写入、页面列、遮罩或第二个入口，既有手工工作台、导航、草稿与发布闭环仍可使用

场景: 过程性任务按冻结阶段顺序执行
  假如教师发布新的结构化 v2/v3 过程性任务，快照包含三个连续阶段及各自证据要求
  并且学生是目标班级当前成员
  当学生打开发布实例
  那么只能开始阶段 1，并看到该阶段在冻结快照中的名称、情境、行动、支架、证据与评价要点
  当学生保存非空文字、READY 附件或快照内至少一个证据检查点并正式提交阶段 1
  那么系统追加阶段 1 的不可变正式修订，并幂等创建或复用阶段 2 工作草稿
  并且教师进度视图显示该学生完成 1/3，但不读取阶段 2 草稿正文或附件元数据
  当同一提交请求重复到达
  那么不增加第二条阶段 1 修订，也不创建第二份阶段 2 草稿

场景: 混合提交在全部阶段后进入整项终稿
  假如新的结构化 v2/v3 Release 采用 mixed 且执行协议为 1
  当学生依次正式提交全部阶段
  那么最后阶段提交在同一事务准备唯一的 `phaseIndex = 0` 整项终稿草稿
  并且整项终稿可以使用既有重交、附件与教师反馈闭环
  当任一前置阶段尚无正式修订
  那么学生不能创建、保存或正式提交后续阶段或整项终稿

场景: 阶段检查点必须来自发布定义
  假如当前阶段的冻结快照只定义证据 1 和证据 2
  当学生保存重复、越界或属于其他阶段的证据索引
  那么服务端拒绝写入且不产生阶段草稿、修订或下一阶段
  当学生只确认快照内的证据 2 并正式提交
  那么正式修订固化该检查点，即使文字为空也不覆盖或伪造证据正文

场景: 阶段历史、关闭和角色隔离
  假如学生已经提交阶段 1 并开始阶段 2
  当发布教师查看阶段 1 并保存反馈
  那么反馈只绑定阶段 1 的精确正式修订，阶段 2 的继续执行不改写该历史
  当其他学生、非发布教师或已经退出班级的学生尝试读取或写入该资源
  那么服务端按资源归属或当前成员关系拒绝且不泄露存在性
  当发布实例关闭
  那么学生仍可读取自己的全部阶段历史，但不能再保存、提交或开始下一版

场景: 历史发布不被阶段协议追溯改写
  假如 Release 在 D-031 前已经发布，或其 executionVersion 为 0
  当学生或教师继续读取并完成原整项提交闭环
  那么系统仍只使用 `phaseIndex = 0`，不伪造阶段进度、不改变 snapshot/hash，也不拆分已有 Submission 历史

场景: 教师为发布实例建立带角色的作业小组
  假如发布教师仍管理目标班级，且甲、乙、丙都是当前学生成员并且尚无该 Release 的个人 Submission
  当教师明确确认建立“校园调查组”，为甲、乙设置角色标签，并建立另一个包含丙的小组
  那么两个 ReleaseGroup 只属于当前 Release，同一学生不能重复入组，角色与成员可在提交开始前读取和修改
  当非发布教师、学生或已经不再管理班级的发布教师尝试配置
  那么服务端拒绝且不返回可用于枚举其他班级学生的资料

场景: 同组成员共享阶段提交与反馈
  假如甲、乙属于同一 ReleaseGroup，任务采用三个过程性阶段
  当甲保存阶段 1 的共享工作草稿
  那么乙打开同一 Release 和阶段时看到同一工作草稿版本，而组外学生看不到该 Submission 的存在性
  当乙基于最新版本正式提交阶段 1
  那么系统只追加一条小组阶段 1 修订，并幂等准备唯一的小组阶段 2 草稿
  并且教师小组进度显示完成 1/3，但不读取阶段 2 草稿正文或附件元数据
  当教师对该修订确认反馈
  那么甲、乙看到同一反馈，组外学生仍不能读取

场景: 小组附件保留上传者并服从共享授权
  假如甲、乙属于同一小组且共享草稿版本为 2
  当甲上传一个通过验证的附件并保存到共享草稿
  那么附件记录实际上传学生为甲，乙和有权教师可经授权下载，其他小组学生不能读取对象或元数据
  当乙使用陈旧的草稿版本同时保存或移除附件
  那么系统拒绝覆盖并要求刷新，不产生第二份正式历史

场景: 提交开始后冻结小组归属
  假如某小组已经创建任一阶段 Submission，或某学生已有当前 Release 的个人 Submission
  当教师尝试改名、调整角色、增删该小组成员、删除该小组，或把已有个人提交的学生迁入小组
  那么系统拒绝且既有工作草稿、正式修订、附件与反馈主体保持不变
  当尚无 Submission 的另一个小组被修改或删除
  那么有权教师可明确确认该变更，且相同幂等请求不重复执行

场景: 小组闭环不依赖 AI 或对象存储
  假如 AI 被禁用且附件存储暂不可用
  当同组学生用非空文字或发布快照内检查点依次完成阶段
  那么共享提交、教师进度、反馈和关闭后只读仍然可用，不要求 AgentRun 或附件

场景: 教师用名单码批量加入既有学生账号
  假如 operator 已为两个既有 STUDENT 账号配置唯一名单码
  并且当前教师仍管理目标班级
  当教师粘贴名单码并预览
  那么系统只返回精确匹配结果、重复项、已在班项和无法匹配项，不提供全局学生目录或姓名模糊搜索
  当全部目标可加入且教师确认绑定班级版本、名单码与学生集合的成员变更
  那么同一事务追加当前成员区间、执行确认意图、记录成功审计与幂等结果，并让班级版本前进一版

场景: 教师结束并重新加入班级成员
  假如学生当前属于教师管理的班级
  当教师预览影响并明确确认结束成员关系
  那么系统只把当前区间的 endedAt 固化为服务端时间，不删除历史
  当教师随后用同一学生名单码再次确认加入
  那么系统追加新的当前区间，原区间保持不变
  并且学生对旧活动、本人提交与反馈的历史读取继续服从既有时间重叠规则

场景: 成员变更重新授权、处理并发并安全重试
  假如成员变更确认绑定班级版本 4 与精确学生集合
  当教师已不再管理班级、名单码改绑、学生角色改变、成员状态变化或班级已经前进为版本 5
  那么系统不改变成员关系并要求重新预览
  当同一教师使用相同幂等键和相同参数重试已成功变更
  那么系统返回原结果且不新增成员区间
  当相同幂等键用于不同参数
  那么系统返回幂等冲突

场景: 名单管理不把认证工作放进业务事务
  假如既有学生账号和名单码已经由 operator 写入应用数据库
  当有权教师读取班级名单、预览精确名单码并确认加入或结束成员关系
  那么有权教师仍可读取班级名单、预览精确名单码并确认加入或结束成员关系
  并且系统只使用已解析的本地 actor，不在数据库事务中执行密码派生或会话创建

场景: 学生把已验证格式的私有附件固化进正式修订
  假如当前成员学生已经保存一份含非空文字的工作草稿
  当学生附加不超过 5 个白名单文件且每个不超过 20 MiB
  那么 Vercel 后端只以短期 OIDC 签发绑定精确类型、大小和唯一对象 key 的私有上传地址，自托管后端只经重新授权的服务器接收路由写入 release 目录外的持久磁盘，且两者都不向浏览器返回长期读写密钥
  当对象元数据匹配但文件头验证尚未完成
  那么工作草稿保留附件，正式提交被拒绝
  当对象元数据与 JPEG、PNG、WebP、PDF、DOC 或 DOCX 的声明和文件头一致
  并且学生用最新工作副本版本正式提交
  那么同一事务把全部 READY 附件关联复制到不可变正式修订后再消费工作副本
  当学生开始重交
  那么新工作草稿复制当前正式修订的文字与附件关联，旧修订不变

场景: 附件读取仍按业务资源授权
  假如附件对象保存在已配置的 Private Blob 或自托管持久磁盘且业务记录含 storage key
  当其他学生、其他教师或未授权调用者提交附件 ID 或 storage key
  那么系统返回不存在且不签发下载 URL
  当附件所有者学生，或发布教师且仍管理目标班级，请求有权读取的附件
  那么服务端重新授权后代理私有对象流，按已保存媒体类型允许图片/PDF inline 并让 Word 保持下载，不返回永久对象 URL 或 storage key

场景: 附件服务失败不破坏文字闭环
  假如 Private Blob 配额/OIDC、自托管磁盘或对象读取暂时不可用
  当学生继续保存并正式提交有效文字证据且工作草稿没有未完成附件
  那么纯文字正式修订仍可创建
  并且附件视觉转写、OCR 或文档解析都不是文字提交的依赖

场景: 当前账号可辨识并可直接切换
  假如已预配置的教师或学生完成登录
  当进入自己的工作台或任一已授权业务页面
  那么顶部持续显示服务端用户名称、业务角色和退出登录入口
  当教师进入学生根工作台或学生进入教师根工作台
  那么页面不查询或显示目标工作台业务资源
  并且说明当前账号角色，提供正确工作台入口和退出登录入口
  当同一账号访问不属于自己的具体草稿、发布或提交路径
  那么系统仍返回不存在，避免枚举资源

场景: 未认证访问不能旁路本地登录
  假如调用者没有有效 cdas_session
  当访问 /admin、/teacher、/student 或任一具体业务资源
  那么系统重定向到对应角色登录页或返回资源级不存在
  并且 development、Preview、E2E 与 staging 都不提供点击进入预配置账号的旁路

场景: 教师确认后发布精确草稿版本
  假如教师拥有 ready 状态的草稿版本 7
  并且该教师管理目标班级
  当发布预览绑定草稿版本、班级、截止时间和参数摘要
  并且教师在第一方界面确认并执行发布
  那么系统只创建一个不可变发布实例
  并且封存原草稿
  并且审计中的人类 actor 是该教师

场景: 发布确认后草稿发生变化
  假如发布确认绑定草稿版本 7
  当草稿已经变为版本 8
  并且客户端执行原发布动作
  那么系统不创建发布实例
  并且要求重新预览和确认

场景: 数据库只接受完整且同事务的发布事实
  假如任意写入绕过领域命令直接操作数据库
  当 Release 没有唯一的已执行发布意图，或草稿没有在同一事务封存
  那么数据库拒绝提交
  当发布意图的截止时间不是带 offset 的 ISO 时间点，或包含数据库无法精确保存的亚毫秒精度
  那么数据库拒绝该发布意图
  当快照不是意图绑定版本的精确修订，包含额外或缺失字段，或内容摘要不匹配规范化 UTF-8 正文
  那么数据库拒绝提交
  当发布意图已经执行或草稿已经封存，但同一事务没有产生唯一 Release 与快照
  那么数据库也拒绝提交

场景: 草稿当前内容必须有精确修订历史
  假如教师正在编辑草稿版本 3
  当保存完整内容并成功提交事务
  那么草稿 head 前进为版本 4
  并且追加一条正文完全一致的不可变修订 4
  当任意写入只改 head、跳号或伪造不一致修订
  那么数据库拒绝提交

场景: 草稿来源与 Agent provenance 不可混淆
  假如普通 UI 已用幂等 key K 保存手工修订
  当 Agent 用同一教师和 K 重试
  那么系统返回幂等冲突而不是把手工修订报告成 Agent 修订
  当 AgentRun 属于其他教师、已经失败或已经取消
  那么 Agent 草稿保存与发布预备均被拒绝
  当 AgentRun 已成功结束且请求不是原幂等请求的精确重放
  那么系统拒绝创建另一条草稿修订或发布意图

场景: Agent 模型中断边界服从已提交的业务结果
  假如模型在任何工具执行前中断
  那么系统不创建草稿或 Release
  并且 AgentRun 记录为失败或取消
  当教师已经通过签名审批确认精确发布参数
  并且 publish command 成功提交唯一 Release
  那么同一数据库事务把 AgentRun 从运行中记录为成功
  并且后续 provider step、HTTP 回应或客户端连接中断不能回滚 Release 或把该运行改写为失败
  当该 AgentRun 已先失败或取消
  那么发布事务回滚且不创建 Release
  当同一审批以新的 AgentRun 原样重放
  那么系统安全拒绝且不创建第二个 Release

场景: 确认后的参数不能整组替换
  假如教师确认了草稿 A、班级 A、版本 7 和参数摘要
  当任意调用试图同时替换 payload、摘要、目标和预期版本
  那么数据库拒绝修改 ActionIntent
  并且原确认不能发布草稿 B 或班级 B

场景: 发布预备并发幂等
  假如教师拥有 ready 草稿
  当同一教师用相同幂等键和相同参数并发准备发布意图
  那么系统只创建一条 ActionIntent
  并且两次调用返回同一预备结果
  并且不创建 Release
  当相同键用于不同截止时间
  那么系统返回幂等冲突

场景: 发布请求安全重试
  假如幂等键已经成功创建发布实例
  当同一教师使用相同键和相同参数重试
  那么系统返回同一个发布实例
  并且数据库没有第二个发布实例
  当相同键用于不同参数
  那么系统返回幂等冲突

场景: 发布教师明确确认后关闭 Release
  假如发布教师仍管理目标班级
  并且 Release 处于 active 状态
  当教师在第一方界面预览并确认绑定该 Release、当前状态与参数摘要的关闭意图
  并且执行关闭命令
  那么 Release 变为 closed
  并且系统记录确认、成功审计和幂等结果
  并且学生不能再保存、开始重交或正式提交
  但原本可见的学生仍可读取活动、自己的提交和反馈
  并且有权教师仍可查看提交并保存最终反馈

场景: 关闭 Release 重新授权并安全重试
  假如关闭确认已绑定 active 的 Release
  当非发布教师、已不管理目标班级的发布教师，或参数、状态已变化的请求执行关闭
  那么系统不改变 Release 状态
  当同一教师以相同幂等键和相同参数重试已成功的关闭
  那么系统返回同一个 closed Release
  并且不新增第二条关闭审计

场景: 数据库不接受没有确认事实的直接关闭
  假如 Release 仍处于 active 状态
  当任意写入不绑定唯一且已执行的关闭 ActionIntent，直接把状态改为 closed
  那么数据库拒绝提交
  当关闭意图已执行但同一事务没有把精确 Release 关闭，或关闭时间、actor、参数与摘要不一致
  那么数据库也拒绝提交

场景: 正式提交至少包含一项有效证据
  假如学生的工作修订只有空白文本
  当学生正式提交
  那么系统拒绝并返回缺少证据
  当学生提供非空文本
  那么系统创建不可变的正式提交修订

场景: 不可见 Unicode 字符不构成文本证据
  假如工作副本只包含普通空白、NBSP、NEXT LINE、ZERO WIDTH SPACE 或独立变体选择符
  当学生正式提交
  那么系统返回缺少证据
  并且工作副本保留、正式修订不增加
  当文本超过 20000 个 Unicode code point
  那么保存输入被拒绝且不截断原文

场景: 正式提交绑定已保存工作副本
  假如学生预览并准备提交工作副本 X 的版本 3
  当另一个保存已将 X 更新为版本 4
  那么版本 3 的正式提交请求被拒绝
  并且系统不会混合或丢失两个版本的正文

场景: 重交不会覆盖历史
  假如学生已有正式修订 1
  当学生显式开始新修订并再次提交
  那么系统创建正式修订 2
  并且修订 1、其证据和反馈保持不变

场景: 历史成员只能读取既有工作草稿与正式历史
  假如学生在活动开放期间是班级成员
  并且已经保存工作草稿或正式修订
  当该成员关系结束但曾与活动开放期重叠
  那么学生仍可读取活动、自己的工作草稿、正式修订与反馈
  但保存工作草稿、开始重交与正式提交命令都返回不存在
  并且页面不显示任何对应写入入口
  并且既有工作草稿与追加式正式历史不被修改

场景: 重复开始重交不重置工作内容
  假如学生已经从正式修订 1 开始了工作副本 X
  并且已经修改并保存 X
  当开始重交的同一请求重复到达
  那么系统返回 X 的当前元数据
  并且不会重新复制修订 1 覆盖学生的修改

场景: 保存与正式提交并发时不丢数据
  假如保存与正式提交同时针对工作副本 X 的版本 3
  当其中一个请求先成功
  那么另一个请求必须因版本或工作副本变化而失败
  并且正式修订只能来自一个完整、已保存的精确版本

场景: 教师不能评价已过期的页面内容
  假如教师确认的是学生修订 1
  当学生在执行反馈前提交了修订 2
  那么原反馈命令不写入反馈
  并且要求教师查看并确认修订 2

场景: 教师确认结构化形成性反馈并由学生响应
  假如学生已经正式提交某一阶段修订 1
  当发布教师选择“按反馈修改并重交”、选择“基础支持”并填写非空反馈正文
  并且教师确认绑定该 Submission、修订 1、当前反馈版本、下一步与支架层级的精确参数
  那么系统在同一事务追加一条 TeacherFeedbackRevision，正文、下一步、支架层级、已执行 ActionIntent、审计与幂等结果精确一致
  并且学生能在该阶段修订 1 下看到教师已确认的正文、“按反馈修改并重交”和“基础支持”
  当学生通过既有入口开始重交、修改并正式提交修订 2
  那么修订 1、其结构化反馈和证据保持不变，修订 2 成为新的当前正式修订
  并且教师必须针对修订 2 重新准备反馈，不能复用修订 1 的确认

场景: 形成性支架不改变共同任务或阶段状态
  假如教师对个人或小组提交选择“继续后续阶段”和“挑战拓展”
  当反馈保存并由有权学生读取
  那么发布快照、阶段索引、既有后续阶段和其他学生的任务均不改变
  并且该选择不是分数、最终评价、自动解锁或长期学生画像
  当其他学生、组外学生或非管理教师读取该 Submission
  那么系统返回不存在且不泄露反馈正文、下一步或支架层级

场景: 旧反馈历史与 AI 失败保持可用
  假如迁移前反馈只有正文且没有 D-034 结构化字段
  当有权教师或学生读取历史
  那么系统原样显示正文，并明确这是未指定下一步与支架层级的旧历史，不补造决定
  当 AI 建议不可用
  那么教师仍可手工选择下一步和支架层级、确认反馈，学生仍可按既有流程重交

场景: AI 形成性反馈建议只能绑定精确正式修订与反馈版本
  假如发布教师为当前正式修订成功取得一份形成性反馈建议
  并且同一事务已把建议 AgentRun 结束为成功，并追加绑定该 SubmissionRevision 与当前反馈版本的成功审计
  当教师修改建议并准备最终反馈确认
  那么系统允许把同一 AgentRun 绑定到本次不可变反馈修订，并继续等待教师独立确认
  当客户端改用同教师的其他成功 AgentRun、上一反馈版本的建议 Run，或上一正式修订的建议 Run
  那么准备命令因缺少同一正式修订与反馈版本的成功建议审计而拒绝，不创建 ActionIntent 或反馈历史
  当修复前已经存在一条错误绑定其他成功 Run 的确认意图
  那么最终保存命令仍重新核对精确建议审计并拒绝执行，不把错误来源写进不可变反馈历史
  当匹配建议的相同准备幂等请求重复到达
  那么系统返回原 ActionIntent，不新增第二条确认意图
  当教师不使用 AI 建议
  那么原手写准备、确认与保存路径继续记录 MANUAL 来源且不要求 AgentRun

场景: AI 形成性反馈只使用重新授权后真正读到的附件
  假如当前正式修订含一张 READY 图片、一份有文本的 DOCX，以及一份 PDF 或不可解析的旧 DOC
  当发布教师且当前班级管理员点击「让助手起草这一版反馈」
  那么系统先以当前教师逐件复用正式附件下载授权，再在数据库事务外转写图片或抽取 DOCX 文本；PDF 只标记为需查看原件
  并且主起草模型只收到 attachmentId、受限内容、读取状态与截断说明，不收到文件名、storage key、对象 URL 或原始字节
  并且反馈使用 READABLE 附件中至少一个具体可核验事实；PDF、旧 DOC 或单件读取失败只提示教师核对原件，不根据类型与存在性猜测
  当附件在读取前已不再属于同一当前正式修订，或教师失去班级管理权
  那么整次建议按资源级不存在失败并结束 AgentRun，不产生反馈、确认意图或建议正文审计

场景: 文档解析在学生提供的压缩内容前保持资源边界
  假如当前正式修订含一份小于上传上限、但 DOCX 条目会异常膨胀的附件，或任意 PDF 附件
  当发布教师请求 AI 形成性反馈或量规评价建议
  那么 DOCX 在进入 Mammoth 前按实际展开量失败关闭，PDF 在进入通用解析器前直接标记不可读
  并且该附件只作为不可读证据提示教师核对原件，不进入主起草模型，也不阻塞手写反馈、手写评价或既有附件下载

场景: 教师确认证据绑定量规评价
  假如学生已经正式提交某一阶段修订 1，且发布快照是结构化 schema v2/v3
  当发布教师为全部冻结量规维度给出等级或证据不足，并为每个等级引用本版证据
  并且填写非空综评，在独立面板确认「确认并保存量规评价」
  那么系统在同一事务追加一条 TeacherEvaluationRevision，维度结果、综评、已执行 ActionIntent、审计与幂等结果精确一致
  并且形成性反馈的 CONTINUE/REVISE 决定保持不变
  并且学生或同组成员能看到已确认评价；其他学生或其他教师得到资源级不存在

场景: AI 只为当前正式修订起草可编辑量规建议
  假如发布教师仍管理目标班级，当前正式修订存在且发布快照是结构化 schema v2/v3
  当教师在现有评价编辑器点击「让助手起草这一版评价」
  那么服务端先复用反馈工作区查询重新验证资源归属与当前正式修订
  并且只把冻结量规、当前修订可见文字、已确认检查点，以及逐件复用下载授权后成功得到的受限附件转写发送给事务外的模型调用
  并且文件名、storage key、原始附件字节、旧反馈、旧评价与工作草稿不进入主起草模型输入
  当模型返回合法建议
  那么建议按冻结顺序覆盖全部维度，只引用本版文字、已确认检查点或本次成功读取的当前正式附件 ID；无法判断的维度标记证据不足且不带引用
  并且同一事务把 AgentRun 结束为成功，追加只绑定本版正式修订与当前评价版本且不含建议正文的成功审计
  并且建议与综评直接回填现有可编辑表单，页面说明未经教师确认不会保存
  并且此时没有 ActionIntent、TeacherEvaluation 或 TeacherEvaluationRevision
  当教师修改任意建议并通过既有独立确认面板保存
  那么最终不可变评价修订绑定该成功 AgentRun 并标记为 AI_ASSISTED
  并且该来源只说明本版曾由 AI 起草，不表示教师采纳了模型原文
  当客户端把同教师的其他成功 AgentRun 填入评价表单
  那么准备命令因缺少同一正式修订与评价版本的建议审计而拒绝，不创建确认意图
  当教师放弃已生成建议
  那么不产生评价或确认意图，已经成功的 AgentRun 不被回拨
  当调用者是其他教师或学生、发布教师已失去班级管理权、页面对应的修订已不是当前版、快照是 v1 或没有正式修订
  那么系统不返回建议并保持资源级不存在或量规不可用，不产生评价历史
  当模型失败、超时、输出增删改名维度、引用未成功读取或不属于本版的附件，或产生其他非法 schema
  那么 AgentRun 以失败结束，就近错误保留现有手写输入，且不产生确认意图或评价历史
  当 AI_PROVIDER_DISABLED=1 或模型配置不完整
  那么入口不渲染、provider 不构造、AgentRun 不创建，教师仍可完整手写、确认并保存量规评价

场景: v1 快照、证据不足与陈旧确认不能写入评价
  假如发布快照是 schema v1
  当教师尝试准备量规评价
  那么系统拒绝写入，且不从 feedbackCriteria 发明维度
  假如快照是 v2 且某一维度标记证据不足
  那么该维度不得携带等级或引用
  当学生在确认后提交修订 2
  那么原评价确认不写入，教师必须针对修订 2 重新准备

场景: 教师提交列表只显示当前修订的评价状态
  假如发布快照是结构化 schema v2/v3 且当前正式修订尚无评价
  当有权教师打开该 Release 的正式提交列表
  那么系统显示「待评价」，不展示综评或维度结果
  当该修订已确认评价版本 1
  那么列表显示「已评价 v1」，入口为「查看反馈与评价」
  假如发布快照是 schema v1
  那么列表显示「无量规」，且不发明评价维度
  当其他教师打开同一列表
  那么系统返回不存在

场景: 学生活动列表只显示当前修订是否已有评价
  假如有权学生的当前正式修订已确认量规评价
  当该学生打开自己的活动列表
  那么系统显示「已有评价」和「当前版已有量规评价」，不展示综评或维度结果
  当其他学生打开自己的列表
  那么看不到该活动

场景: 教师发布页只显示当前修订的反馈与评价覆盖
  假如发布快照是结构化 schema v2/v3，且三份当前正式修订中仅一份已确认反馈与评价
  当有权教师打开该 Release 的正式提交列表
  那么系统显示「已反馈 1/3」和「已评价 1/3」，不展示综评或维度结果
  假如发布快照是 schema v1
  那么系统可以显示反馈覆盖，但不显示评价覆盖
  当其他教师打开同一页
  那么系统返回不存在

场景: 教师发布页只显示待重交与尚未正式提交
  假如当前正式修订的形成性下一步是按反馈修改并重交，且尚无工作副本
  当有权教师打开该 Release 的正式提交列表
  那么系统显示「待重交」，不展示工作草稿正文或综评
  当学生已开始重交草稿但尚未正式提交
  那么列表改为显示「重交中」
  假如班级进度中当前阶段已有提交容器但 latestRevisionNumber 为 0
  那么系统显示「尚未正式提交」
  当其他教师打开同一页
  那么系统返回不存在

场景: 教师工作台只显示各发布的待反馈、待评价与待重交计数
  假如三份当前正式修订中一份已确认反馈且待重交并已评价，两份尚无反馈也尚无评价
  当有权教师打开工作台
  那么该发布显示「待反馈 2」「待评价 2」和「待重交 1」，不展示工作草稿正文或综评
  当学生已开始重交草稿
  那么「待重交」计数归零
  假如发布快照是 schema v1
  那么工作台不显示待评价
  当其他教师打开自己的工作台
  那么看不到该发布

场景: 教师工作台把评阅队列与班级分开，草稿不出现在工作台
  假如教师同时拥有待反馈发布、已发布活动和编辑中草稿
  当有权教师打开工作台
  那么待办显示待反馈、待评价与待重交计数，班级卡片列出该班已发布活动
  并且工作台不列出「我的草稿」
  当教师打开活动设计
  那么只列出编辑中与可预览草稿，已封存的发布源草稿不出现

场景: 教师顶栏面包屑按真实页面层次返回
  假如教师在活动设计页
  当教师点击顶栏「教师工作台」
  那么进入工作台
  假如教师在某发布的正式提交列表
  当教师点击顶栏班级名
  那么进入该班成员页
  并且当前活动标题是当前页、不可点

场景: 学生活动列表只显示待重交与重交中
  假如当前正式修订的形成性下一步是按反馈修改并重交，且尚无工作副本
  当有权学生打开自己的活动列表
  那么系统显示「待重交」，不展示反馈正文或综评
  当学生已开始重交草稿但尚未正式提交
  那么列表改为显示「重交中」
  当其他学生打开自己的列表
  那么看不到该活动

场景: 教师可导出当前正式修订评阅名册
  假如有权教师打开该 Release 的正式提交列表
  当教师下载「导出评阅名册」
  那么文件是 CSV，含对象名与当前修订的反馈、评价、跟进标记，不含证据正文、反馈正文或综评
  当其他教师打开同一导出路径
  那么系统返回不存在

场景: AI 不可用时核心闭环仍然成立
  假如模型服务不可用
  当教师通过普通界面创建草稿、预览并发布
  并且学生提交文本证据
  并且教师手写并确认最终反馈
  那么学生可以查看反馈
  并且整个过程不要求存在 AgentRun

场景: 受保护 staging 的合成真实账号验收不扩大业务权限
  假如同一次受保护 GitHub run 已生成并校验 staging GO 证据
  并且该证据以健康证明密钥绑定 run、部署源码、数据库、本地认证配置、合成教师、两个学生与一名无关教师、GitHub Environment secret 中的 base URL、Vercel project name 及有效的 Vercel Automation Bypass secret
  并且 Vercel Preview 在未提供显式部署标识时以其 Git commit SHA 作为构建期部署标识
  当额外的“合成写入、本地认证会话、保留不清理”人工证明均为 true
  并且写入前重新验证当前远端 health proof 仍与该 run 精确一致
  并且四个本地合成账号均存在、为 ACTIVE、所属学校为 ACTIVE 且密码验证成功
  并且 base URL 必须是该 project 的精确 HTTPS `.vercel.app` Preview 根地址，不能由普通 Environment Variable 替换为自定义域或其他主机
  并且只有本合成验收 workflow 将 `STAGING_DEPLOYMENT_PROTECTION_REQUIRED` 固定为 `1`；共享 staging verifier 在该值缺失时只验证通用公开 HTTPS 根地址，既不要求 project 也不读取 bypass secret
  并且 Vercel Deployment Protection 保持启用；每次发送 bypass 前，独立无 cookie、无 bypass 的 health 请求必须得到 Vercel SSO `302`、`server: Vercel`、`x-vercel-id` 与精确绑定 health URL 的 64 位 nonce challenge
  并且两次 health 请求与四个全新浏览器上下文只对精确的 staging scheme、host 和有效 port 通过 `x-vercel-protection-bypass` 请求头访问
  并且 CDN、重定向与任何其他 origin 均不接收 bypass 或 set-bypass-cookie 请求头，bypass secret 不进入 URL、截图、artifact 或验收记录
  那么 acceptance-only operator 只能以唯一 `cdas-staging-*` namespace 先预配置教师与主学生及其本地凭据，再追加或精确重入同班第三学生，并为无关教师只建立不含班级或成员关系的 AppUser；该 operator 不扩大产品成员管理权限
  并且班级恰有两个活跃学生成员，且两个学生本地身份均不同
  并且真实浏览器必须经现有第一方 UI 完成手工草稿、发布、文本提交、教师反馈、量规评价、学生查看与关闭
  并且第三学生能看到同一 Release，但主学生的 evidence、feedback、evaluation 与显示名称均不泄漏；直接打开教师页面取得的主学生 submission URL 返回 404
  并且无关教师直接打开该教师的 Release 与主学生 Submission URL 均返回资源级 404，且不泄漏学生 evidence、feedback、evaluation 或显示名称
  当学生保留关闭前的重交页面并在关闭后提交保存
  那么既有 Server Action 与领域命令拒绝该写入
  并且刷新后页面不显示写入口、工作副本与正式历史仍可读取
  并且最终 verifier 只以显式 read-only transaction 查询该 namespace
  并且 verifier 证明第三学生在目标 Release 下没有 Submission、SubmissionRevision 或 TeacherFeedback 历史
  并且任何缺失、配置绑定不匹配、浏览器失败或证据 schema 不完整都为 NO_GO
  当同 marker 已经留下任意业务草稿
  那么重跑在追加新的业务历史前拒绝，并要求新的 run attempt 使用新 marker

场景: 受保护 staging 的首个 Agent 场景以真实模型启动同一 Release 的完整教学闭环
  假如受保护 Environment 已批准唯一合成写入、本地认证会话、模型费用、identity reservation 与历史保留
  并且同一次 run 的 GO 证据绑定当前源码、AI-enabled deployment、隔离数据库、本地认证配置、DeepSeek API key、模型、approval secret、Vercel project 与 automation bypass
  并且 Vercel Deployment Protection 保持启用，automation bypass 只发送到绑定 Preview 的精确 origin，不发送给 DeepSeek、重定向或其他主机
  并且 DeepSeek V4 使用 non-thinking 模式；已有草稿会话中的明确发布请求只向模型开放并指定 publish_activity_release，仍必须等待签名的人类确认
  当 runner 在首页最终 scheme、host 与有效 port 精确等于已绑定 staging origin 后，使用预留教师凭据提交登录表单
  并且每次关键导航后重新校验 origin，密码在使用后从环境和表单输入清除
  并且教师在第一方助手提交固定合成主题与 marker 标题；模型生成的 schema v3 任务书必须满足 D-061 的基本设置、真实情境、可观察目标与核心素养、学科贡献、连续阶段、类型化证据与四档量规合同，但不把生成文本逐字照抄 prompt 当成成功条件
  那么模型通过共享 saveActivityDraft 命令创建唯一 READY_FOR_PREVIEW 版本 1 与 AGENT revision
  并且客户端只渲染与工具返回 draft ID 一致的精确站内预览链接，教师点击后才导航
  并且共享 teacher layout 在内存保留同一官方 AI SDK message session
  当教师返回普通编辑页人工修改摘要并保存为 READY_FOR_PREVIEW 版本 2
  那么版本 1 AGENT revision 与版本 2 MANUAL revision 都不可改写，且版本 2 把摘要替换为固定人工修订值；其余教师可见结构保持一致，兼容投影按三维目标、阶段证据与评价维度规范化
  当教师逐段核对版本 2 预览并明确要求把版本 2 发布到 marker 班级且不设置截止时间
  那么模型只能提出带精确版本 2、班级与 null dueAt 的签名发布 approval
  并且在教师点击确认前没有 ActionIntent 或 Release
  当教师确认
  那么系统仍通过 prepare → UI decide → publish 领域命令原子创建唯一版本 2 SEALED head、EXECUTED ActionIntent、ACTIVE Release 与不可变 snapshot
  并且四个同教师、同模型、同浏览器时间窗的 AgentRun 恰好分别对应无业务写入的草稿提案、教师确认后的草稿写入、无业务写入的发布提议和教师确认后的发布执行
  并且该教师在 marker 精确标题下只能存在这一份草稿，包括未发布草稿
  并且两次 save、prepare、decide、publish audit 与两次 save、prepare、publish 幂等记录的 actor、来源、run、intent、版本和结果资源精确一致
  当预留主学生通过第一方学生界面打开该 Release，保存非空固定合成文本并明确正式提交
  那么该 Release 只产生该学生的一份 Submission 与不可变版本 1 SubmissionRevision
  并且学生直接访问教师 Release URL 返回资源级 404
  当发布教师通过第一方界面查看该 Submission 并明确确认固定合成反馈
  那么系统产生一份 MANUAL TeacherFeedbackRevision、EXECUTED feedback intent 与 UI provenance audit，且不增加 AgentRun
  并且主学生重新打开活动能看到该反馈
  当预留其他教师直接访问目标 Release 与 Submission URL
  那么两个资源均返回 404，且不泄漏主学生显示名、evidence 或 feedback
  当预留其他学生打开同一 Release
  那么活动可见，但其工作区为空且主学生显示名、evidence 与 feedback 均不可见；目标 Submission URL 返回 404
  当主学生在开始重交后保留陈旧页面，发布教师明确确认关闭活动，再由该陈旧页面保存
  那么 Release 进入 CLOSED，关闭 intent 与 UI audit 成功，陈旧保存由服务端以 RELEASE_NOT_ACTIVE 拒绝
  并且主学生刷新后仍能读取原正式 evidence 与 feedback，但输入不可编辑且所有写按钮消失
  并且发布教师直接访问学生活动 URL 返回资源级 404
  并且只读 verifier 在同一 marker 与同一 Release 上同时证明 Agent 发布 provenance、主学生提交、教师反馈、关闭、陈旧写拒绝、其他学生零历史与其他教师零目标操作
  当直接打开或刷新预览页
  那么系统不从 URL、localStorage 或业务数据库恢复对话或 approval
  并且既有手工发布路径仍可使用
  当 run_real_model 未获批准、任一 attestation 缺失、health binding 改变、origin 改变、同标题多出草稿、时间窗多出 AgentRun，或任一最终证据缺键、缺检查项、缺截图或 hash 非法
  那么工作流必须 NO_GO，且不得用清理、覆盖或重跑同 marker 修复 append-only 历史

场景: schema v3 贯通新建、发布、阶段执行与量规评价且保留旧历史
  假如教师从手工表单或设计助手创建一份新活动
  那么保存的 canonical content 为 schema v3，兼容标量列只由同一正文派生
  并且每条核心素养引用属于已选学科并匹配学段与年级，每条目标都被至少一个阶段和一个量规维度覆盖
  当引用未知核心素养、未选学科、错误学段或错误年级
  那么共享保存命令拒绝且不产生 ActivityDraft、ActivityDraftRevision 或成功幂等结果
  当教师保存后重新打开编辑页和预览页
  那么系统精确读回同一份 v3 任务书，展示基本设置、背景、目标与课程依据、学科贡献、阶段和量规，不投影为 v1/v2
  当教师确认发布 v3 phased 或 mixed 草稿
  那么 Release snapshot 与源 revision 精确相等且 hash 对应同一 v3 JSON，executionVersion 为 1
  并且学生按冻结阶段顺序保存和正式提交，mixed 终稿只在全部阶段有正式修订后开放
  当发布教师评阅当前正式修订
  那么形成性反馈、量规评价、评价建议、列表状态和过程诊断均使用冻结 v3 阶段与量规
  当教师通过 Agent 修改一份既有未封存 v2 草稿
  那么工具保持 schema v2 并追加新修订；跨 schema 修改失败且不写入
  并且既有 v1/v2 修订、发布快照与 hash 始终不迁移、不回填、不重算

场景: 教师自助建班并按 Excel 名单导入学生
  假如教师已在本校开通账号且还没有任何班级
  当教师新建一个班级
  那么该班级属于教师所在学校并由该教师管理
  当教师上传一份包含新学号、已有学号、他班学号和格式错误行的名单
  那么预览逐行显示新建、复用、已在本班、跨班冲突与格式错误，并只允许导入前两类
  并且上传前先验证当前教师仍管理本校班级，浏览器不能绕过上传与预览自行提交名单条目
  并且空行后的物理行号保持准确，未来生效的成员关系显示为冲突
  当教师确认导入
  那么系统为新学号创建本校学生账号与本地凭据，并把可导入的学生加入本班
  并且新账号可用「cdas + 学号」登录，且首次登录被要求修改密码
  并且既有账号的姓名与密码没有被改写，冲突行没有产生任何写入
```
