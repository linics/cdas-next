# CDAS Next 决策记录

本文件只记录已经决定并会影响项目边界的事项。新决策追加记录，不静默改写历史理由。

## D-001：新项目使用独立仓库

- 状态：已接受
- 日期：2026-08-18
- 决策：CDAS Next 位于与旧项目平级的独立仓库，不放入旧仓库子目录，不以长期重构分支实现。
- 理由：隔离旧模型、旧合同、旧依赖和 AI 编码工具的误复用。
- 旧项目参考：`https://github.com/linics/cdas.git`，固定提交 `6e3e9cbb31e3017b78f75df28179c2eef218fd35`。

## D-002：旧代码默认不复用

- 状态：已接受
- 日期：2026-08-18
- 决策：允许迁移用户场景、失败案例、静态学科数据、样例和测试思想；业务代码、数据库结构、API 合同和页面代码默认不复制。
- 例外：任何直接复用必须单独记录其解决的问题、替代方案和复用理由。

## D-003：第一阶段只实现一条闭环

- 状态：已接受
- 日期：2026-08-18
- 决策：教师创建草稿、发布给一个班级、学生提交证据、教师反馈、学生查看反馈。
- 后果：RAG、多 Agent、小组、多阶段、互评、自评和自动评分延期。

## D-004：发布实例与可编辑草稿分离

- 状态：已接受
- 日期：2026-08-18
- 决策：发布创建不可变 ActivityRelease 快照；学生提交不引用可变草稿。
- 理由：避免教师修改草稿导致历史提交和评价语义漂移。

## D-005：Agent 通过业务工具工作

- 状态：已接受
- 日期：2026-08-18
- 决策：普通 UI 与 Agent 共用相同领域服务。Agent 不直接操作数据库，高影响动作必须确认并审计。

## D-006：不承诺旧系统兼容或数据迁移

- 状态：已接受
- 日期：2026-08-18
- 决策：第一阶段不兼容旧 `/api/v2`，不读取旧数据库。若未来确认存在需要保留的真实数据，另行设计一次性、可验证的数据导入程序。

## D-007：技术栈在最小验证后确定

- 状态：已接受
- 日期：2026-08-18
- 决策：当前不因已有代码自动沿用旧技术栈，也不因流行度直接采用 Agent 框架。
- 候选验证方向：React Agent UI + AG-UI；Python 方向验证 PydanticAI，复杂持久流程出现前不引入 LangGraph 或独立工作流平台。
- 选择标准：能否稳定完成 `AGENT.md` 中唯一的第一阶段 Agent 验证任务，并保持权限、确认、审计和失败降级清楚。
- 后续：技术选型已由 D-010 收束；本条保留当时的验证依据，不再表示当前候选状态。

## D-008：发布、提交与反馈使用不可变修订

- 状态：已接受
- 日期：2026-08-18
- 决策：一份草稿最多发布一次，发布后封存；学生的每次正式提交形成不可变 SubmissionRevision；教师反馈绑定具体提交修订，修改反馈时追加 TeacherFeedbackRevision。
- 理由：禁止静默覆盖学习证据或评价历史，让重交、反馈修改、并发确认和审计具有明确语义。
- 后果：再次发布必须派生新草稿；教师确认反馈后若学生已经重交，原确认失效并要求重新查看。

## D-009：成员关系和高影响动作保留可验证历史

- 状态：已接受
- 日期：2026-08-18
- 决策：班级成员关系使用有效区间而非删除；发布、关闭和最终反馈先创建绑定精确参数与资源版本的 ActionIntent，再由第一方界面确认；执行时仍重新授权，并在同一事务保存幂等结果与动作审计。
- 理由：角色检查不能回答历史可见性，也不能防止确认后参数变化、重复请求或响应丢失导致的重复执行。

## D-010：第一阶段采用单一 TypeScript 模块化单体

- 状态：已接受
- 日期：2026-08-18
- 决策：第一阶段使用 Next.js 16 App Router 与严格 TypeScript；Clerk 负责认证；PostgreSQL 与 Prisma 7 负责持久化、事务和迁移；Vercel AI SDK 负责结构化 Agent UI 与工具确认。生产数据库优先验证 Neon，附件优先验证 AWS S3 私有桶与 GuardDuty Malware Protection。
- 理由：当前领域事务重而 Agent 场景薄，拆分 React 与 Python 会增加身份传递、CORS、接口生成和部署故障面，却没有确定的 Python 原生工作负载。上述组件都提供官方 Next.js 或 PostgreSQL 集成，可避免自研认证、迁移、连接池、聊天流和确认 UI。
- 验证结果：官方 Supabase Next.js 示例虽然集成度高，但本次临时构建得到 Next.js 16.3.1 搭配 `eslint-config-next` 15.3.1、浮动 `latest` 依赖，且 `@supabase/ssr` 仍为 beta；不将该模板原样作为生产基线。
- 约束：Clerk 只提供身份；角色、班级成员关系和资源授权仍保存在应用数据库并由领域命令验证。AI SDK 的 approval 只承载人机交互，服务端 ActionIntent 才是可信确认事实。
- 延后：不引入 FastAPI、PydanticAI、CopilotKit、AG-UI、LangGraph、Temporal 或通用任务平台；出现确定的跨运行时或持久长任务后重新决策。

## D-011：依赖供应链采用显式白名单与可审计覆盖

- 状态：已接受
- 日期：2026-08-18
- 决策：应用依赖固定精确版本；pnpm 安装脚本使用 `allowBuilds` 白名单；生产依赖审计作为独立验证；已知漏洞只能通过显式、带版本的 workspace override 临时修复，并重跑 Prisma 校验、迁移、测试和生产构建。
- 当前例外：Prisma 7.9.1 的配置包精确依赖存在栈耗尽公告的 `deepmerge-ts` 7.1.5，workspace 暂时覆盖为已修复的 8.0.1。Prisma 官方依赖升级后应移除此覆盖并重新审计。
- 理由：既不等待已知高危项进入基线，也不通过复制或修改第三方源码制造私有分叉。

## D-012：提交先完成文本纵向切片，附件单独扩展

- 状态：已接受
- 日期：2026-08-18
- 决策：Submission 使用稳定容器、单一工作副本和不可变正式修订；第一段实现只保存文本证据，不预建通用 Evidence JSON、空 Attachment 表、上传状态机或对象存储适配层。文本统一 NFC 与换行，最多 20,000 个 Unicode code point；纯 Unicode 空白、格式控制字符或独立变体选择符不构成证据。
- 理由：文本已能独立闭合保存、正式提交、迟交和重交历史；附件还需要真实 S3 区域、预签名上传、类型/大小、所有权和恶意文件扫描验证。提前抽象只会把未验证假设固化进领域模型。
- 扩展缝隙：后续增加私有 Attachment、工作副本关联与 append-only 正式修订关联；上传/扫描在事务外完成，正式提交事务只引用已完成安全检查的资产。

## D-013：最终反馈绑定提交修订并通过确认意图追加

- 状态：已接受
- 日期：2026-08-18
- 决策：每个 SubmissionRevision 最多有一个稳定 TeacherFeedback 容器；教师每次确认保存都追加不可变 TeacherFeedbackRevision，并以容器版本做 CAS。最终写入必须由绑定精确提交修订、反馈版本和规范化正文的 ActionIntent 支持，AI 建议只记录为 provenance，不直接成为最终反馈。
- 理由：学生重交与教师确认可能并发；把反馈直接挂在可变 Submission 或覆盖当前正文，会让评价对象、修改历史和人类责任无法证明。按提交修订分隔后，旧反馈可保留，新修订必须重新查看和确认。
- 降级：手写反馈使用同一命令但不需要 AgentRun 或模型服务；`AI_PROVIDER_DISABLED=1` 时核心闭环仍然成立。

## D-014：草稿采用可变 head 与不可变连续修订

- 状态：已接受
- 日期：2026-08-18
- 决策：ActivityDraft 保存当前可编辑 head；每次成功保存以预期版本做 CAS，并追加正文完全一致、版本连续的 ActivityDraftRevision。普通 UI 与 Agent 共用命令，但 revision source、AgentRun provenance 与幂等来源必须一致。发布预备只创建 ActionIntent，第一方确认后的发布事务才允许把 READY head 同版本封存。
- 理由：只保留可变草稿无法撤销或解释 Agent 修改；只保留事件流又会给第一阶段读取和表单带来无必要复杂度。head + 数据库校验的不可变修订同时提供简单读取、并发保护和可审查历史。
- 约束：新草稿从版本 1 开始；正文更新必须版本加一；FAILED/CANCELLED 或其他教师的 AgentRun 不得作为来源；SEALED 草稿永久不可修改或删除。

## D-015：第一阶段完成标准以文本证据闭环为准

- 状态：已接受
- 日期：2026-08-20
- 决策：第一阶段可交付闭环只要求非空文本证据；附件上传、对象存储、文件扫描与 attachment-only 提交作为后续独立纵向切片，不计入当前六步闭环的完成门槛。
- 理由：`PRODUCT.md` 的“文本或附件”允许文本路径独立闭环，D-012 已明确先完成文本切片。继续把附件写成当前能力会让规范与运行态冲突；现在预建通用 Evidence JSON、空 Attachment 表或存储适配层只会固化尚未通过真实 S3 和扫描验证的假设。
- 后果：当前正式提交必须包含有效文本。后续附件切片必须保留私有存储、所有权、类型与大小检查、恶意文件扫描完成状态、事务外上传，以及不可变正式修订关联，不能通过把 `textEvidence` 改成可空来冒充支持。

## D-016：第一阶段班级成员由受控 operator 初始化

- 状态：已接受
- 日期：2026-08-20
- 决策：第一阶段产品 UI 和 Agent 不提供教师自助增删成员；真实 Clerk 用户先存在，再由受控、幂等的 operator CLI 建立应用用户、班级和当前成员映射。
- 理由：六步教学闭环需要真实成员事实，但不需要同步实现 SIS、邀请、密码或批量成员管理。把初始化 CLI 当成教师业务工具会绕过人类确认、资源审计与历史区间合同。
- 后果：CLI 不得覆盖既有角色、显示名称、班级管理员、班级名称或成员历史；教师自助成员管理若进入范围，必须新增明确场景、ActionIntent、服务端命令、审计与权限反例。

## D-017：时间以 UTC instant 保存并按使用者装置时区显示

- 状态：已接受
- 日期：2026-08-20
- 决策：数据库与服务端合同只传带 offset 的 ISO 时间点；教师输入的 `datetime-local` 在浏览器用当前 IANA 时区转换为精确 instant，所有教师与学生页面再用浏览器 `Intl` 按当前装置时区显示。
- 理由：固定台北时区会让异地使用者误读截止与确认时间；把无 offset 的本地时间交给 Server Action 又无法证明使用者意图。浏览器原生时区数据库同时处理 DST gap 与重复时刻，不需要自建时区换算层。
- 约束：Server Action 拒绝无 offset、重复或未知字段；SSR 首屏使用稳定 UTC fallback，hydration 后再显示装置时区，不通过内联脚本改 DOM。`datetime-local` 的 DST gap 必须拒绝；重复时刻沿用浏览器原生较早 offset 并由测试固定。

## D-018：首个助手场景复用 AI SDK 官方消息流与签名审批

- 状态：已接受
- 日期：2026-08-20
- 决策：活动助手使用当前 AI SDK 的 `useChat + streamText`、严格工具 schema、签名 `toolApproval` 和 AI Gateway；不自建聊天传输、审批 token 或通用 Agent 工作流。草稿与发布分别复用既有 `saveActivityDraft` 和 prepare → decide → publish 命令链。
- 理由：当前只有一个短工具场景，官方消息流已经提供工具 part、暂停、审批响应与验签。另建聊天协议或工作流引擎只会增加第二套状态机；只相信 SDK 审批又不能承担资源版本、确认人和重新授权，所以 ActionIntent 继续作为业务信任边界。
- 失败边界：配置不全或 `AI_PROVIDER_DISABLED=1` 时不渲染助手、不构造 provider、不创建 AgentRun。模型在工具前中断不产生业务写入。写命令成功提交时立即固定 AgentRun 为 SUCCEEDED；普通工具 step 用 `stopWhen` 结束，审批续传用官方 `prepareStep` 中止后续 provider signal。手工六步闭环始终独立可用。
- 当前限制：真实模型供应商、地区与日志条款仍需外部验收；确认执行后若整个 HTTP 响应丢失，以新 AgentRun 原样重建会安全失败为幂等冲突，跨运行恢复不得通过放松 provenance 或 ActionIntent 绑定来实现。

## D-019：生产构建暂时使用 Next.js 官方 Webpack 路径

- 状态：已接受
- 日期：2026-08-20
- 决策：`pnpm build` 固定为 `next build --webpack`；开发服务器暂不改动。不要为当前 Turbopack 停滞添加自定义 loader、私有补丁或依赖分叉。
- 证据：在相同源码与环境中，Next.js 16.3.1 默认 Turbopack build 多次停在 optimized production build 且没有诊断；官方 `--webpack` build 完成编译、TypeScript、静态页面生成和 trace。Next.js 本地文档明确把 `--webpack` 列为受支持的 opt-in 路径。
- 后续：升级 Next.js 或 AI SDK 后先在独立分支重跑默认 build；只有默认路径稳定通过全套验证后才移除本选项。

## D-020：Agent 业务写入与成功 provenance 原子提交

- 状态：已接受
- 日期：2026-08-20
- 决策：Agent 成功保存活动草稿或发布 Release 时，领域命令在同一 PostgreSQL 事务内提交业务结果、ActionAudit、IdempotencyRecord 与对应 AgentRun 的 `RUNNING → SUCCEEDED`。UI 路径不得携带 AgentRun；新 Agent 写入只接受 RUNNING run，SUCCEEDED run 只允许原幂等结果的精确重放；AgentRun 已失败、取消、缺失或属于其他教师时，业务事务整体回滚。
- 理由：在命令提交后再由流式回调单独更新 AgentRun，会留下“Release 已存在但运行永久停在 RUNNING”的进程崩溃窗口。进程内重试或日志无法证明审计事实与业务事实一致；把终态 CAS 放进现有领域事务可以直接复用 PostgreSQL 原子性，不需要自建 outbox 或工作流引擎。
- 后果：handler 的 finish callback 仍负责无业务写入的普通完成、模型失败与取消；写工具返回后对同一成功终态的调用只是幂等确认。发布幂等摘要同时绑定 UI/AGENT 来源，跨来源重放安全失败。数据库禁止 AgentRun 终态回拨、身份改写、删除或通过 `SET NULL` 擦除修订与审计 provenance。

## D-021：发布与关闭事实由数据库双向绑定确认意图

- 状态：已接受
- 日期：2026-08-20
- 决策：每个 ActivityRelease 以唯一外键绑定创建它的已执行发布 ActionIntent；发布意图执行、草稿封存、Release 和精确快照必须在同一事务完整出现。Release 关闭同样固化唯一关闭 ActionIntent，且意图执行与状态迁移必须同事务完成。正向资源写入与反向意图/封存状态都由延迟数据库约束验证。
- 摘要：schema-v1 活动内容继续以 `canonicalize@4` 的固定字段字节计算 SHA-256；数据库只实现这个封闭七字段 schema 的等价 serializer，并使用 PostgreSQL 17 核心 `sha256(bytea)` 复算。它不是通用 JCS 实现，不引入 `pgcrypto` 或第二套发布协议；未来 schema v2 必须新增明确分支与跨层 golden vector。
- 理由：只在应用命令中消费确认仍允许后续 Prisma 直写意外制造无确认 Release、未封存快照、假摘要或无确认关闭。外键、唯一性和双向延迟约束能证明最终历史的完整形状，同时保留现有领域命令、ActionIntent 与 Serializable 事务作为唯一写路径。
- 迁移：既有 Release 只从唯一成功发布/关闭审计回填；缺失、歧义或内容不一致的历史直接中止迁移，不伪造 ActionIntent 或改写 append-only 事实。

## D-022：外部重构需求进入分阶段路线但不扩大当前合同

- 状态：已接受
- 日期：2026-08-23
- 输入：《中小学跨学科任务设计与评价一体化平台 PRD（研创赛重构版 v1.0）》与《中小学跨学科任务平台新旧版本对比简报》。
- 决策：接纳单教师负责、结构化跨学科设计、语义化阶段、过程证据、形成性反馈、差异化、证据驱动评价、过程诊断和复用等长期产品方向，并统一记录在 `ROADMAP.md`。这些方向不是第一阶段交付承诺，不改变 D-003、D-010、D-012、D-015、D-016 或当前 `ACCEPTANCE.md`。当前合同中的 Release 关闭尾段来自此前已经接受的 D-009 与 D-021，不是本次外部需求输入造成的范围扩张；D-003 的教师—学生教学闭环仍是第一阶段主线。
- 进入条件：每个后续纵向切片必须先明确用户场景、非目标、资源级授权、append-only 历史、外部依赖边界和正常/越权/并发/重复/依赖失败验收场景，再以新的已接受决策进入实现。
- 技术边界：外部材料中的 React + Vite + FastAPI、RAG、异步评价和多学校结构仅作为被评估的方案或能力需求，不覆盖 D-010。当前单一 Next.js 模块化单体继续有效；不预建第二后端、工作流引擎、向量库或多 Agent 层。
- 评价边界：AI 可以建议、解释和指出证据不足，但教师仍是发布者、反馈者和最终评价责任人；AI 与教师差异记录只作为审计或研究素材，不自动证明有效性。
- 后果：后续会扩展产品宽度，但当前团队继续先完成第一阶段真实外部验收；路线条目不会通过占位页面、数据库空字段或未经确认的 Agent 工具提前进入产品。

## D-023：首个助手场景只在共享 activities layout 内存续接

- 状态：已接受
- 日期：2026-08-23
- 决策：`/teacher/activities` 的共享客户端 layout 持有唯一官方 AI SDK `useChat` session，使 `create_activity_draft` 成功后的客户端导航可以在精确预览页继续原消息与签名 approval。session 不写入 URL、localStorage、cookie 或业务数据库；直接进入、刷新预览或导航离开 activities subtree 都不会恢复该对话。
- 理由：首个场景必须先看到服务端重新授权读取的不可变修订预览，再由同一消息历史让模型提出精确发布工具和官方签名 approval。把消息或 approval 放进 URL 会泄漏并允许篡改；为一个短场景增加服务端聊天存储会引入新的保留、读取授权与恢复协议，而当前 ActionIntent、audit 与幂等账本已经承担业务事实。
- 后果：预览页同时保留既有手工发布面板；助手 session 丢失时教师可以继续手工闭环，但必须重新从新建页开始新的 Agent 会话，不能拼接旧 approval。受保护的首次外部验收用真实浏览器证明 new → 草稿提案确认 → preview → 普通编辑页人工保存版本 2 → preview 的内存连续性，并以只读账本验证 AGENT 版本 1、MANUAL 版本 2、四次 AgentRun（草稿提案、草稿执行、发布提议、发布执行）、教师确认和唯一 Release；随后以普通第一方 UI 让同一 Release 完成学生提交、教师反馈、学生读取、教师关闭和关闭后只读，不把这些动作扩展成 Agent 工具，也不引入第二后端、持久工作流或通用聊天系统。
- D-045 后续修订：session 已上提到教师 layout；草稿工具成功只渲染精确站内「查看预览」与「继续编辑」链接，不再自动跳转，由教师点击后进入页面。D-023 的签名审批、内存会话和无持久恢复边界不变。

## D-024：首个助手试行直连 DeepSeek 官方 API

- 状态：已接受
- 日期：2026-08-23
- 决策：首个助手试行使用 AI SDK 的 OpenAI-compatible provider，服务端以 `DEEPSEEK_API_KEY` 直接调用 `https://api.deepseek.com`；试行模型固定通过 `AI_MODEL` 选择 DeepSeek 模型，仓库默认使用官方列出的 `deepseek-v4-flash`。不再把 Vercel AI Gateway 或其 BYOK 余额作为运行依赖。
- 理由：当前 Vercel 团队即使配置自有 DeepSeek key，Gateway 仍要求有效信用卡和 Gateway credit 才允许请求；DeepSeek 官方 API 已提供兼容接口与工具调用，直连能保留现有 AI SDK 消息流、签名审批和领域工具合同，同时让费用只由用户已有的 DeepSeek 账户承担。
- 约束：DeepSeek key 只存在于服务端环境变量，不进入客户端、日志或 artifact；模型调用仍在数据库事务外；配置不全或供应商失败时助手失败关闭，手工教学闭环继续可用。选择视觉模型不代表当前产品支持图片上传，附件仍受 D-012 与 D-015 约束。
- V4 工具模式：DeepSeek V4 默认 thinking，但官方 V4 兼容说明指出 thinking 请求不接受 `tool_choice`。首个教师助手固定发送 `thinking.type=disabled`；已有草稿会话中的明确发布请求用 AI SDK named tool choice 选择 `publish_activity_release`，输入仍由模型生成，签名确认与领域命令继续作为高影响动作边界。
- 后果：D-018 的 AI SDK 消息流、工具 schema、审批和业务失败边界继续有效，其中“AI Gateway”供应商路径由本决策替代。生产数据的地区、日志保留和供应商合规仍需外部审查。

## D-025：附件证据采用私有 S3、托管扫描与不可变修订关联

- 状态：已接受；存储供应商与扫描语义由 D-026 替代
- 日期：2026-08-24
- 用户场景：学生在已保存的文字工作草稿中补充图片、PDF 或 Word 证据；教师在反馈页面读取正式修订的附件；学生重交时沿用上一正式版附件并可显式调整。
- 决策：每个工作草稿最多关联 5 个附件，单文件最多 20 MiB；首个切片只接受 JPEG、PNG、WebP、PDF、DOC 与 DOCX。文件进入 AWS S3 私有桶，五分钟预签名 PUT 绑定精确 Content-Type、Content-Length、SSE-S3 与 `If-None-Match: *`，随机且唯一的对象 key 不允许覆盖既有对象。GuardDuty Malware Protection 通过对象标签返回扫描结果；只有 `GuardDutyMalwareScanStatus=NO_THREATS_FOUND` 映射为 `READY`，威胁、不可支持、拒绝访问或扫描失败均映射为 `REJECTED`，无已知终态时保持 `SCAN_PENDING`。
- 事务与历史：预签名、对象元数据读取、标签读取和下载签名都在数据库事务之外。数据库保存所有者、声明元数据、私有 storage key、上传/扫描时间和单向状态；工作副本关联可变且共享工作副本版本令牌，正式提交事务只把同一工作副本中的 `READY` 资产复制成 append-only `SubmissionRevisionAttachment`，随后才移除工作副本关联。重交从当前正式修订复制附件关联，不改写旧版。
- 授权：storage key 和预签名 URL 都不是读取权限。学生只可操作和下载自己的附件；教师只可下载自己发布且仍管理班级的正式修订附件。每次签名与状态转换前都由服务端按当前资源关系重新授权；其他学生、其他教师和历史成员写入均拒绝。
- 降级与非目标：D-015 的非空文字正式提交门槛继续有效；对象存储或扫描不可用时，学生仍能完成纯文字闭环。附件单独提交、PPT/Excel/CSV、视频、OCR、文档解析、多模态模型与附件 Agent 工具不进入本切片。AWS 测试资源和 GuardDuty 费用须在明确启用后再做真实 staging 验收。

## D-026：开发阶段附件改用 Hobby 上的 Vercel Private Blob 与 OIDC

- 状态：已接受；自托管后端与预览 disposition 由 D-055 扩展
- 日期：2026-08-24
- 决策：D-025 的数据库所有权、工作副本、`READY` 门槛、append-only 正式修订关联和单文件限制继续有效；对象存储改为新加坡区域的 Vercel Private Blob。创建前必须自动确认目标项目当前 Production 与 Preview target 均为 Hobby，创建界面或接口不得要求付款方式、Pro Trial 或升级。若出现任何账单要求立即停止；Hobby 配额耗尽时允许附件不可用，不允许转成付费或追加用量。
- 身份与上传：项目仅连接 Preview，使用 Vercel 自动提供、短期轮换的 OIDC 与 `BLOB_STORE_ID`；禁止创建或配置长期 `BLOB_READ_WRITE_TOKEN`。浏览器上传前由 Route Handler 重新验证当前 Clerk/UI actor、学生所有权、active 工作副本、精确 storage key、媒体类型与最大字节数，再签发五分钟、不可覆盖且不追加随机后缀的私有预签名上传。
- 内容验证：Vercel Blob 不提供 GuardDuty 等价的恶意文件扫描，因此不得把上传完成描述为“病毒扫描通过”。沿用数据库 `SCAN_PENDING` 名称以避免无价值迁移，但其当前语义只是验证 Private Blob 元数据与声明完全一致，并核对 JPEG、PNG、WebP、PDF、DOC 或 DOCX 的文件头；匹配才进入 `READY`，不匹配进入 `REJECTED`。更深的压缩包结构检查与恶意文件扫描属于后续独立能力。
- 下载与降级：每次下载先按业务资源关系重新授权，再由服务器读取并代理私有 Blob；本决策当时统一使用 `Content-Disposition: attachment`，D-055 后按保存媒体类型允许图片/PDF inline，二者都保留 `nosniff` 与 `private, no-store`，且不得把永久 Blob URL 或 OIDC 信息返回浏览器。Blob、OIDC 或配额不可用时只暂停附件，移除未完成附件后纯文字保存与正式提交继续运行。
- 理由：Private Blob 与现有 Vercel Preview 同一管理面，Hobby 明确在免费额度内使用且超额后暂停而不产生额外费用。OIDC 避免长期存储读写密钥；显式缩小“内容验证”声明则避免把文件头检查伪称为恶意文件扫描。

## D-027：教师成员管理使用预配置名单码与确认式历史变更

- 状态：已接受
- 日期：2026-08-24
- 用户场景：学校或 operator 已经建立学生 Clerk 账号与 AppUser 映射后，班级管理员教师需要把一批既有学生加入自己管理的班级、查看当前与历史名单、结束当前成员关系，并在学生再次进入该班时恢复成员资格。
- 身份匹配：operator 可为 STUDENT 配置全局唯一、规范化的 `rosterKey`；教师只可按精确名单码批量预览，不提供全局学生目录、姓名模糊搜索或 Clerk ID 输入。名单码不是认证凭证，不能创建登录会话、读取学生证据或绕过教师当前班级管理权。
- 确认与事务：批量加入和单人结束都先形成绑定班级 ID、班级版本、精确学生/成员集合、操作类型和参数摘要的 ActionIntent。教师在第一方 UI 确认后，执行命令重新验证 actor、当前 manager、学生角色、名单码、成员状态和班级版本；成员区间写入、班级版本递增、意图执行、幂等结果和成功审计在同一 Serializable 事务提交。
- 历史：加入或恢复一律追加新的 ClassroomMembership 区间；结束只允许把当前区间的 `endedAt` 从空值固化为服务端时间。既有区间不得删除、改写学生、班级、开始时间或已经设置的结束时间；历史活动与提交读取继续按既有区间重叠规则判断。
- 边界：首个切片只管理已存在的 AppUser，不调用 Clerk 管理 API，因此外部身份服务不可用不阻断名单管理。教师不能创建账号、设置密码、发邀请或修改学生资料；CSV/Excel 文件解析、多租户、跨校目录、班级创建、小组和 Agent 成员工具不进入本切片。
- 理由：名单码给教师一个可批量、可报告错误且不暴露 Clerk 内部 ID 的精确引用，同时把身份开通与业务成员关系分开。复用现有 ActionIntent、IdempotencyRecord、ActionAudit 和成员区间约束即可形成完整纵向切片，不需要第二后端或身份同步服务。

## D-028：合成验收用独立第四身份证明教师资源隔离

- 状态：已接受
- 日期：2026-08-24
- 决策：受保护合成验收增加一名 Clerk test 教师，并只为其建立独立 `AppUser`，不创建或授予目标班级、成员关系或业务资源。真实浏览器必须以该身份直接访问目标 Release 与 Submission 的精确 URL，并得到资源级 404。
- 理由：仅用学生账号或不存在的 AppUser 会分别证明角色拒绝或未开通拒绝，不能证明“另一教师”通过认证且具备教师角色后仍受资源所有权约束。第四身份只扩展验收证据，不改变产品角色、成员或授权模型。

## D-029：真实模型验收不关闭 Vercel Preview 保护

- 状态：已接受
- 日期：2026-08-24
- 决策：`staging-agent-acceptance` 与 AI-enabled Preview 继续启用 Vercel Deployment Protection。受保护 workflow 将 project、精确 Preview URL、保护模式与 automation bypass 一并绑定到 Agent gate；浏览器只向该 origin 添加 bypass header，跨 origin 请求不携带。
- 理由：真实模型验收需要浏览器访问 Preview，但不能为一次测试降低共享开发部署保护。origin-scoped bypass 复用现有合成验收边界，也避免把 secret 放入 URL、截图或 artifact。

## D-030：结构化跨学科活动采用内容 schema v2 并兼容 v1 历史

- 状态：已接受
- 日期：2026-08-24
- 用户场景：教师需要设计一份学生能够直接执行的跨学科任务书，而不只是填写标题和一段说明。原版 CTS/CDAS 的作业设计器、学生任务书、AI prompt 和“校园节水行动”样例构成本切片的能力与中文语义下限；CDAS Next 可以重组交互和增强结构，但不得删减这条基线。
- 基本设置：schema v2 保留原版的探究主题、任务描述、学段、年级、主学科、融合学科、作业类型、条件子类型、探究深度、提交模式、1–16 周周期和可选跨学科概念。学科使用原版小学/初中可用范围的稳定代码目录；主学科必须符合学段，融合学科至少一个、去重且不能包含主学科。作业类型继续使用 `practical`（实践性作业：体验、观察、参与真实情境）、`inquiry`（探究性作业：调查、实验、论证科学问题）、`project`（项目式作业：设计、制作、迭代解决现实问题），不另造更薄的四类目录。跨学科概念保留 `matter_energy`（物质与能量：形态转化与守恒）、`structure_function`（结构与功能：设计与适应性）、`system_model`（系统与模型：简化研究工具）、`stability_change`（稳定与变化：相对平衡与绝对发展），首版可选择 0–2 个。截止时间继续由发布参数 `dueAt` 冻结，不在可变草稿中维护第二份日期。
- 学习设计：schema v2 分开保存面向学生的背景设定，知识与技能、过程与方法、情感态度三类目标，以及 3–4 个连续任务阶段。每阶段只有一个核心动作，并明确阶段名称、情境承接、学习支架、一个或多个带类型的提交证据、评价要点和课时建议。证据类型使用 `text`、`document`、`image`、`video`、`confirm`、`link` 稳定目录。默认任务链应能表达“观察/调查 → 统计/分析 → 建议/公开表达”，不能退化成泛泛的“开展活动”。
- 评价：schema v2 保存可编辑评价维度及 `excellent`、`good`、`pass`、`improve` 四档描述；默认至少覆盖问题意识、证据质量、跨学科连接和方案表达。它们作为学生任务书中的成功标准和教师人工反馈依据，本切片不做自动评分。
- 版本与历史：`ActivityDraft` head 与每条 `ActivityDraftRevision` 显式保存内容 schema 版本和全部结构化设计。既有 v1 草稿、修订和 Release snapshot 不回填、不改 hash，继续按 v1 读取；新的手工或 Agent 保存产生完整 v2 修订，旧未封存草稿只有在教师补齐后才升级。新发布从精确 v2 修订生成 v2 snapshot；v1 与 v2 的规范化正文和 SHA-256 分别验证。
- 手工与助手：手工创建、编辑、学生预览和发布不依赖模型。`create_activity_draft` 生成同一 v2 结构并继续调用共享 `saveActivityDraft`；AI 结果必须先成为教师可编辑草稿，不能直接发布。模型失败不降低手工任务书能力。
- 边界：本切片不建立阶段依赖/回溯引擎、小组协作、RAG、课程标准图谱、AI 自动评价、学生 AI 或教案文件导出。`submissionMode` 在本切片先作为任务书与发布快照语义；过程性检查点真正形成多次正式提交属于后续阶段执行切片。主学科与融合学科也不自动证明融合质量。
- 理由：原版已经形成完整的“情境—目标—行动—证据—评价”产品语义。v2 应把这套语义真正纳入不可变历史和学生发布快照，而不是仅增加几个展示字段；兼容读与新写收紧可同时保护历史和提高新活动质量。

## D-031：阶段执行复用提交历史，并由发布版本固定执行语义

- 状态：已接受
- 日期：2026-08-25
- 用户场景：采用“过程性提交”或“混合提交”的结构化任务书发布后，学生需要按任务书中的连续阶段完成证据检查点，保存本阶段草稿并正式提交；教师需要在不读取学生草稿正文的前提下查看每名学生的阶段进度，并能对任一阶段的正式修订使用既有反馈闭环。原版 CTS/CDAS 已实现的“第一阶段草稿 → 每阶段独立提交 → 检查点来自任务定义 → 提交后幂等准备下一阶段”构成本切片的能力和中文语义下限。
- 发布边界：`ActivityRelease.executionVersion` 冻结发布采用的执行协议。历史 Release 迁移后保持 `0`，继续使用一份整项 Submission，不回填阶段状态、不改变 snapshot 或 hash。新发布的 v2 `once` 任务使用 `0`；新发布的 v2 `phased` 与 `mixed` 任务使用顺序阶段协议 `1`。执行协议不能在发布后修改。
- 阶段身份：协议 1 不复制或改写发布内容，而以不可变 snapshot 中 1 起始的 `phaseIndex` 标识阶段，以 1 起始的 `evidenceIndex` 标识该阶段证据要求。索引必须落在冻结快照范围内；名称、行动、情境、支架、证据类型、评价要点和课时建议始终从同一 snapshot 读取。由此保护既有 schema v2 历史，暂不预建通用工作流或伪造新的 StageType。
- 提交容器：Submission 从“Release × 学生”扩展为“Release × 学生 × phaseIndex”的稳定容器。`phaseIndex = 0` 表示整项终稿，`1..N` 表示阶段检查点。每个容器继续复用现有可变 WorkingCopy、不可变 SubmissionRevision、附件和反馈历史；阶段身份、Release 与提交主体一经创建不得改写。
- 顺序规则：协议 1 只允许从阶段 1 开始；阶段 n 的前一阶段必须已有正式修订，学生不能跳阶段。正式提交阶段 n 后，在同一事务内查找或创建下一阶段工作草稿，并在幂等重试时返回同一个结果。`phased` 完成最后阶段即完成任务；`mixed` 完成最后阶段后准备 `phaseIndex = 0` 的整项终稿；`once` 始终只使用 `phaseIndex = 0`。本切片不要求教师批准才解锁，也不实现条件分支、任意依赖图、退回或回溯引擎。
- 证据检查点：工作草稿和正式修订保存学生明确确认的 `completedEvidenceIndexes`。服务端每次保存和提交都按冻结 snapshot 验证，拒绝未定义、重复或越界条目。正式提交至少需要非空文字、一个已就绪附件或一个已确认检查点；附件仍只接受 D-025/D-026 已支持的格式，`video`、`confirm` 与 `link` 在本切片仅作为任务书要求和学生检查点声明，不伪称平台已经验证外部内容。
- 授权与可见性：只有 Release 班级的当前学生成员能写自己的阶段容器；Release 关闭后全部阶段只读。学生只能读取自己的草稿和历史。发布教师且仍为班级管理员时，可读取阶段正式修订和进度、继续使用既有反馈命令，但不能读取未正式提交的草稿正文。所有 UI 与未来 Agent 工具必须复用同一命令，不得直连 Prisma。
- 教师进度：教师发布页按当前班级成员展示阶段完成数、当前可执行阶段和最终状态；草稿最多只贡献“已开始”状态，不返回草稿正文或附件元数据。教师从某一阶段正式提交进入既有反馈页时，页面必须显示来自冻结快照的阶段名称与证据要求，避免把阶段修订误认为整项终稿。
- 理由：让每个阶段拥有独立 Submission 可以直接复用已经验证的乐观并发、幂等、附件、追加式修订和反馈边界，同时保留阶段完成后仍可反馈上一阶段的能力。显式 executionVersion 避免把已有 v2 Release 和整项提交强行改成新语义；线性推进达到原版真实能力，不把路线候选词汇误建成统一状态机。

## D-032：发布实例内的小组共享同一阶段提交

- 状态：已接受
- 日期：2026-08-25
- 用户场景：发布教师需要按某个 Release 的当前班级名单建立作业小组、设置成员角色，并查看小组阶段进度；同组学生需要读取和修改同一份阶段工作草稿、共享正式修订、附件和教师反馈。原版 CTS/CDAS 已实现的“作业内建组与角色 → 同组同阶段复用一份 Submission → 仅成员访问 → 教师按组查看进度 → 产生提交后锁定成员和删除”构成本切片下限。
- 小组边界：`ReleaseGroup` 只属于一个 Release；`ReleaseGroupMember` 绑定该 Release 班级中的学生并保存教师填写的角色标签。同一学生在同一 Release 最多属于一个小组。小组不是 ClassroomMembership、长期班级分组或 Clerk 组织，也不传播到其他 Release。
- 配置规则：只有发布教师且仍为目标班级管理员时，才能通过第一方界面明确确认创建、修改或删除小组。成员必须是目标班级当前学生；已有个人 Submission 的学生不能再迁入小组。小组一旦拥有任一阶段 Submission 容器，其名称、成员、角色与删除全部锁定；已有历史不能通过拆组或换人重归属。没有提交的小组仍可调整，命令必须幂等并留下动作审计。
- 提交主体：Submission 的稳定主体扩展为“个人学生或 ReleaseGroup，二者恰好一个”。个人历史迁移后保持原主体与 `phaseIndex`；小组模式按“Release × Group × phaseIndex”唯一。阶段顺序、WorkingCopy、不可变 Revision、检查点、附件、反馈与 D-031 幂等进入下一阶段全部复用，不另建第二套小组提交流水线。
- 授权与并发：学生仍须是目标班级当前成员才能写；若属于 ReleaseGroup，则只解析并写该组的 Submission。所有持久小组成员都能读取同一工作草稿与正式历史，其他小组、未分组学生和非管理教师不能读取存在性。共享草稿仍使用一个乐观版本；并发成员不能静默覆盖，陈旧保存要求刷新后重试。本切片不实现实时共同编辑、自动合并、组内评论或贡献度分析。
- 附件与反馈：小组成员可把自己的附件加入共享工作草稿；附件仍记录实际上传学生，下载授权扩展到同组成员和有权教师。正式修订复制附件和检查点后保持不可变。教师反馈仍绑定精确 SubmissionRevision，同组成员读取同一反馈，不复制多份个人反馈。
- 教师进度：发布页按小组展示成员与角色、已完成阶段数、当前阶段和正式修订入口；未分组学生继续显示个人进度。教师视图不读取小组工作草稿正文或附件元数据。组名筛选只在当前 Release 的已授权集合中工作，不提供跨班级或跨 Release 学生搜索。
- 边界：本切片不做实时协同编辑、个人贡献统计、任务分工状态机、同伴互评、跨作业固定分组或组间资源共享。AI 与 Blob 不可用时，小组仍可用文字或快照内检查点完成阶段闭环。
- 理由：规范化 Release 小组和互斥提交主体可以在数据库与领域命令中明确执行成员边界，同时完整复用已经通过验收的阶段历史。提交开始后冻结小组配置，避免成员变化追溯改写旧修订、附件和反馈的主体。

## D-033：设计助手采用结构化提案确认，不引入通用 Agent 运行时

- 状态：已接受
- 日期：2026-08-25；2026-08-27 竞赛冲刺补充首版官方依据检索
- 用户场景：教师只有一段初步跨学科活动构想时，需要助手识别真正会改变设计的缺口；资料足够后，教师应先看到助手如何理解任务、采用了哪些假设、为什么需要各融合学科，以及知识/过程/情感目标如何连接学生任务、证据和评价，再决定是否创建可编辑草稿。
- 调研依据：Vercel AI SDK、OpenAI Agents SDK、LangGraph、PydanticAI 与 Mastra 的官方设计共同强调结构化工具、明确停止、人工批准、可观测运行和失败边界；OpenHands、Cline、SWE-agent 与 Goose 的官方实现进一步证明“先计划后行动、进度可见、产物可审查”有助于控制高自由度任务。CDAS 只采纳这些控制原则，不复制编码 Agent 的终端、浏览器、文件系统、多 Agent、长期记忆或通用工作流能力。
- 交互合同：资料不足时每轮最多提出一个会改变年级、学科组合、真实任务、成果、证据或评价结构的必要问题，不为可选润色阻塞创建。资料充分后，助手先通过只读 `search_knowledge` 与 `read_source_section` 核对官方依据；命中当前语料已收录学科时，`create_activity_draft` 的签名 approval 除结构化理解摘要、教师已提供要求、明确假设、每个融合学科的必要贡献、知识/过程/情感三条目标—任务—证据—评价链和完整 schema v2 提案外，还须展示至少两个不同官方来源的规范引用与采用理由。教师批准前不执行草稿写入。拒绝后不重试、不产生草稿，教师可继续补充或改用手工表单。
- 写入与历史：理解确认是可逆 L1 草稿写入前的交互审批，不建立 ActionIntent，也不等于发布确认。批准后的工具仍调用 `saveActivityDraft`，由当前教师、RUNNING AgentRun、schema、幂等和共享领域命令决定能否提交；成功时既有 ActivityDraftRevision、ActionAudit、IdempotencyRecord 与 AgentRun provenance 原子形成，随后停止模型 loop。理解摘要和诊断只存在于当前内存消息，不另建长期实体；教师编辑后的正式草稿历史仍以不可变 revision 为准。
- 诊断边界：跨学科必要性、一致性说明和官方来源引用是模型对当前提案的可解释建议与可追查依据，不是课程标准映射、发布硬门槛、自动合规、自动评分或教育效果结论。结构化 schema 只保证每个融合学科有对应贡献和证据、三类目标各有任务—证据—评价链；教师可以拒绝、编辑或完全不用。引用只存在于当前内存消息，不进入草稿或发布快照。AI 不可用、输出非法或审批失败时，完整手工 schema v2 创建、编辑、预览和发布路径不变；教师仍可在独立页面直接检索同一官方白名单。
- 技术选择：继续使用当前 Next.js 模块化单体、Vercel AI SDK `useChat + streamText + signed toolApproval`、DeepSeek provider 和共享领域命令。首版语料固定为教育部课程方案及语文、数学、物理、信息科技 2022 年课程标准的已圈定章节，由构建脚本生成稳定章节 ID、层级位置和 SHA-256 的只读静态索引，并以确定性词法检索运行；不使用 embedding、pgvector、新 Prisma 表、通用 RAG 框架或第二后端。UbD、C-POTE、T/CAET、教材、教师案例和 AI 历史产物不进入首版。AI SDK 消息状态直接表达检索、读原文、“澄清／方案待确认／草稿已创建／发布待确认”，仍不增加数据库工作流状态机、LangGraph/Mastra/OpenAI Agents SDK/PydanticAI runtime、持久 checkpoint、长期记忆或多 Agent。
- D-058 后续修订：生产语料扩展为同一教育部发布页下的课程方案与 14 门 2022 年课程标准；综合实践活动仍不提供独立课标。命中任一已收录学科时，两条不同来源的引用门槛不变。
- 验收：覆盖必要澄清零写入、繁简体稳定检索、零命中、来源与章节白名单、客户端伪造检索结果/引用失败关闭、命中覆盖学科时两个不同官方来源、结构化提案可读、未批准或拒绝零写入、签名批准只写一份草稿、非法 schema 失败关闭、写后停止、发布仍独立确认、AI 禁用时手工路径与独立检索可用。首版检索不改变业务写入和既有四次 AgentRun 的闭环形状。

## D-034：结构化形成性反馈冻结教师的下一步与差异化支架决定

- 状态：已接受
- 日期：2026-08-25
- 用户场景：教师查看某名学生或小组的精确阶段正式修订后，需要在反馈中明确学生接下来应继续后续阶段，还是回到本阶段修改重交，并选择基础支持、标准任务或挑战拓展的支架强度；学生需要看到这项已确认决定并沿现有重交流程响应，同时保留反馈前后的全部证据。
- 原版下限：原版 CTS/CDAS 已有按阶段展示任务、支架与证据、教师填写阶段评价、学生读取反馈和继续下一阶段；没有可靠证据证明其已交付差异化分配、反馈后重交历史或关键审核节点。CDAS Next 继续保留这些阶段体验，并以既有不可变 SubmissionRevision、TeacherFeedbackRevision、ActionIntent 和资源授权提供更强历史，不把原版未完成能力伪称为基线。
- 数据合同：`TeacherFeedbackRevision` 新增可空的 `nextStep` 与 `supportLevel`，只为兼容迁移前历史保持可空；D-034 新建反馈一律通过 schema v2 payload 同时写入 `CONTINUE | REVISE` 和 `FOUNDATION | STANDARD | CHALLENGE`。字段、正文、精确 SubmissionRevision、反馈预期版本、可选建议 AgentRun 与 payload hash 必须在同一个已执行 ActionIntent 中确认，并由数据库插入约束核对。修改反馈只追加新版本，不改写旧决定或旧正文。
- 业务语义：下一步与支架层级是教师确认的形成性教学建议，不是评分或阶段状态。`CONTINUE` 不构成终评；`REVISE` 不删除、回拨或锁住 D-031 已创建的后续阶段。发布快照中的共同核心目标、任务和评价标准保持不变，差异化只发生在这条反馈对该个人或小组修订的支架强度与行动建议中。
- 授权与交互：只有发布教师且仍管理目标班级时可以准备并保存；学生本人或小组成员只能读取自己有权访问的正式反馈，其他学生、组外学生和其他教师得到资源级不存在。确认面板必须同时展示正文、下一步、支架层级、目标提交版本和参数摘要。学生看到 `REVISE` 时使用既有“开始重交”入口，生成新 SubmissionRevision 后旧确认不能用于新版本。
- Agent 与降级：现有可选 AI 建议仍只能填充待确认参数，不能自动保存或发布；本切片不新增第二个 Agent 场景。AI 不可用时教师手写正文并选择两个结构化字段的流程完整可用。
- 非目标：自动形成性反馈、教师批准后才解锁阶段的强制闸门、自动评分、量规维度—证据绑定、长期学生画像、自评互评、跨提交的持久差异化计划、通用工作流和第二后端。
- 验收：覆盖个人与小组正常读取、`REVISE → 开始重交 → 新正式修订`、`CONTINUE` 不改变阶段状态、旧历史兼容、其他学生/教师隔离、学生重交与反馈版本并发使确认失效、幂等重放和 AI 失败手工降级。2026-08-25 的开发期受保护合成验收在提交 `6612ebd968303280fe11963ced1c8f2ff66fe5ac`、GitHub Actions run `32792068879` 上以 `AI_PROVIDER_DISABLED=1` 完整通过浏览器闭环和只读数据库核对。

## D-035：证据驱动量规评价独立于形成性反馈

- 状态：已接受
- 日期：2026-08-25
- 用户场景：教师查看某名学生或小组的当前正式修订后，需要按发布快照中冻结的四档量规给出终审式判断，并把每个维度绑定到本版证据，或明确标记证据不足；学生需要看到已确认评价，同时保留形成性 CONTINUE/REVISE 反馈。
- 聚合边界：新建 `TeacherEvaluation` 与 `TeacherEvaluationRevision`，1:1 绑定 `SubmissionRevision`，不混入 `TeacherFeedback`。形成性下一步与支架层级继续只存在于反馈；量规评价不改变阶段状态，也不是自动分数。
- 数据合同：评价 payload `schemaVersion` 为 1。维度身份是快照顺序中 1 起始的 `dimensionIndex` 加上精确 `dimensionName`。必须覆盖冻结 v2 `rubricDimensions` 的全部 4–8 项，不得增删或改名。LEVEL 必须带 `excellent|good|pass|improve` 和 1–5 条不重复本版引用；`INSUFFICIENT_EVIDENCE` 不得带等级或引用。综评遵循与反馈相同的 NFC/可见文字规则，最多 10,000 个 code point。
- 证据授权：`{ kind: "text" }` 仅当本版文字有可见内容；`{ kind: "attachment", attachmentId }` 仅限本版 READY 附件；`{ kind: "checkpoint", evidenceIndex }` 必须落在本版 `completedEvidenceIndexes`。v1 snapshot 没有四档量规，命令与 UI 均不开放评价，也不得从 `feedbackCriteria` 发明维度。
- 授权与交互：只有发布教师且仍管理目标班级可以准备并保存；关闭后仍允许评价。学生本人或小组成员只读自己的已确认评价；其他学生、组外学生和其他教师得到资源级不存在。教师提交页使用第二个独立确认面板，文案为「确认并保存量规评价」，不得与反馈确认合并。
- 命令与历史：复制 `prepare_teacher_feedback_intent` / `save_teacher_feedback` 的 Serializable、幂等、审计与十分钟确认边界。修改只追加评价修订。来源本切片固定 MANUAL，`suggestionAgentRunId` 为空。
- Agent 与降级：本切片不新增 Agent 工具。AI 不可用时手写评价完整可用。AI 建议与教师终审差异历史延后。
- 非目标：自动评分、把量规分数写成阶段状态、v1 回填量规、Agent 评价工具、把评价混入反馈确认。
- 验收：覆盖全部维度、证据不足、v1 拒绝、隔离、陈旧确认、幂等和 AI 关闭手写路径；同一变更扩展 staging 浏览器与只读核对。2026-08-26 的开发期受保护合成验收在提交 `d1c9386fd66fbb08d29415cec4c2989dacbe6a6f`、GitHub Actions run `32901425620` 上以 `AI_PROVIDER_DISABLED=1` 完整通过浏览器闭环和只读数据库核对。

## D-036：教师提交列表只读展示当前修订的量规评价状态

- 状态：已接受
- 日期：2026-08-25
- 用户场景：发布教师在 Release 正式提交列表上需要看到每份当前正式修订是否已有量规评价，而不打开每一份提交、也不读取综评或维度结果。
- 数据合同：`getTeacherReleaseSubmissions` 为当前正式修订增加 `evaluation: { currentVersion } | null`，以及发布级 `rubricAvailable`（快照 schema v2 为 true）。列表不得返回评价综评、outcomes、引用或学生工作草稿。
- 展示：v2 显示「待评价」或「已评价 vN」；v1 显示「无量规」，不得发明维度。入口文案改为「查看反馈与评价」。班级进度行仍只反映阶段完成，不混入评价诊断。
- 授权：沿用既有发布者且当前班级管理者列表授权；其他教师继续资源级不存在。不新增命令、表或 Agent 工具。
- 非目标：过程看板趋势、导出、案例库、学校管理、AI 评价建议、把评价状态写成阶段完成条件。
- 验收：列表对 v2 当前修订区分待评价/已评价版本；v1 显示无量规且不泄露评价正文；隔离与只读属性保持不变。2026-08-26 的开发期受保护合成验收在提交 `d1c9386fd66fbb08d29415cec4c2989dacbe6a6f`、GitHub Actions run `32901425620` 上以 `AI_PROVIDER_DISABLED=1` 证明教师列表显示「已评价 v1」。

## D-037：学生活动列表只读展示当前修订是否已有量规评价

- 状态：已接受
- 日期：2026-08-25
- 用户场景：学生在自己的活动列表上需要知道当前正式修订是否已有教师确认的量规评价，而不把综评或维度结果带进首页。
- 数据合同：`listStudentReleases` 增加 `hasCurrentEvaluation`。仅当当前正式修订存在 TeacherEvaluation 时为 true；无正式修订或 v1 快照不得为 true。列表不得返回综评、outcomes 或评价版本号以外的细节。
- 展示：有评价时进度显示「当前版已有量规评价」，并归入「已有评价」分组；无评价时保持既有待提交/已提交/已有反馈分组。不在学生首页对每项 v1 活动标注「无量规」。
- 授权：沿用既有学生列表可见性；其他学生继续看不到无权活动。不新增命令、表或 Agent 工具。
- 非目标：学生首页展示维度分数、过程看板、评价与反馈合并分组、把评价当成阶段完成条件。
- 验收：有权学生在列表看到当前修订已有评价；列表不泄露综评；无权学生列表不含该活动。2026-08-26 的开发期受保护合成验收在提交 `d1c9386fd66fbb08d29415cec4c2989dacbe6a6f`、GitHub Actions run `32901425620` 上以 `AI_PROVIDER_DISABLED=1` 证明学生列表显示「已有评价」且不泄露综评。

## D-038：教师发布页只读展示当前修订的反馈与评价覆盖计数

- 状态：已接受
- 日期：2026-08-26
- 用户场景：发布教师打开 Release 正式提交页时，需要一眼看到当前正式修订中有多少已反馈、多少已评价，而不打开每一份提交，也不读取综评或维度结果。
- 数据合同：`getTeacherReleaseSubmissions` 增加 `reviewCoverage: { currentRevisionCount, feedbackCount, evaluationCount }`，只从当前正式修订已有的反馈/评价存在性计数。不得返回综评、outcomes、趋势时间序列或工作草稿。
- 展示：正式提交记录标题旁显示「已反馈 n/m」；schema v2 另显示「已评价 n/m」，v1 不显示评价覆盖以免发明量规。班级进度行仍只反映阶段完成。
- 授权：沿用既有发布者且当前班级管理者列表授权；其他教师继续资源级不存在。不新增命令、表或 Agent 工具。
- 非目标：跨发布趋势、导出、案例库、学校租户、轻量管理员、学生首页覆盖计数、把覆盖计数写成阶段完成条件。
- 验收：计数只含当前正式修订；三份提交中仅一份已评价时显示「已评价 1/3」；其他教师打开同一页得到不存在；不泄露综评。2026-08-26 的开发期受保护合成验收在提交 `8e902a0e233064442041a69fe449292bfd9d1cce`、GitHub Actions run `32937628880` 上以 `AI_PROVIDER_DISABLED=1` 证明教师发布页显示「已反馈 1/3」和「已评价 1/3」。

## D-039：教师发布页只读展示待重交与尚未正式提交

- 状态：已接受
- 日期：2026-08-26
- 用户场景：发布教师打开 Release 正式提交页时，需要看到谁被要求按反馈重交却还没交、谁已开始重交草稿、谁卡在当前阶段尚未正式提交，而不打开每一份提交，也不读取草稿正文或综评。
- 数据合同：`getTeacherReleaseSubmissions` 为每份当前正式修订增加 `followUp: "AWAITING_RESUBMISSION" | "RESUBMISSION_IN_PROGRESS" | null`：当前反馈下一步为 `REVISE` 且无工作副本时为待重交，有工作副本时为重交中；其他情况为 null。班级进度增加 `awaitingFormalRevision`：未完成且当前阶段容器 `latestRevisionNumber === 0` 时为 true。不得返回工作草稿正文、反馈正文、综评、outcomes 或 `nextStep` 以外的形成性细节。
- 展示：正式提交行在现有「正式修订 n」旁显示「待重交」或「重交中」；标题旁在待重交数大于 0 时显示「待重交 n」。班级进度在 `awaitingFormalRevision` 时显示「尚未正式提交」，仍不把评价诊断混入阶段完成。
- 授权：沿用既有发布者且当前班级管理者列表授权；其他教师继续资源级不存在。不新增命令、表或 Agent 工具。
- 非目标：跨发布趋势、量规薄弱项聚合、导出、案例库、学生首页跟进标记、把跟进标记写成阶段完成条件。
- 验收：REVISE 且无工作副本时显示待重交；开始重交后改为重交中；当前阶段仅有未正式提交容器时显示尚未正式提交；其他教师打开同一页得到不存在；不泄露草稿正文或综评。2026-08-26 的开发期受保护合成验收在提交 `6c76b2b7f6c432c572627a9f38d756184acbc9a5`、GitHub Actions run `32943934421` 上以 `AI_PROVIDER_DISABLED=1` 证明教师发布页显示「待重交 1」。

## D-040：教师工作台只读展示各发布的待反馈与待重交计数

- 状态：已接受
- 日期：2026-08-26
- 用户场景：发布教师打开工作台时，需要看到哪些 Release 还有待反馈或待重交，而不必点进每一份正式提交页，也不读取草稿正文或综评。
- 数据合同：`getTeacherActivityDashboard` 为仍可查看提交的发布增加 `attention: { pendingFeedbackCount, awaitingResubmissionCount }`，计数规则与 D-038/D-039 的当前正式修订一致。班级管理权已变更时 `attention` 为 null。不得返回学生名单、工作草稿正文、反馈正文、综评或趋势。
- 展示：「我的发布」在目标班级旁仅在计数大于 0 时显示「待反馈 n」和/或「待重交 n」。不把这些计数写成阶段完成条件，也不在工作台展开提交行。
- 授权：沿用既有发布者列表；`attention` 仅在当前仍管理目标班级时出现。不新增命令、表或 Agent 工具。
- 非目标：跨发布趋势、量规薄弱项、导出、案例库、学生首页、工作台待评价计数。
- 验收：三份当前修订中一份已反馈且待重交、两份待反馈时，工作台显示「待反馈 2 · 待重交 1」；开始重交后待重交计数归零；其他教师工作台不含该发布。2026-08-26 的开发期受保护合成验收在提交 `0bbbc119b7f509ae128ce894a4f309b8a12464f4`、GitHub Actions run `32963244909` 上以 `AI_PROVIDER_DISABLED=1` 证明教师工作台显示「待反馈 2」和「待重交 1」。

## D-041：教师工作台只读展示各发布的待评价计数

- 状态：已接受
- 日期：2026-08-26
- 用户场景：发布教师打开工作台时，需要看到哪些 Release 还有当前正式修订尚未量规评价，而不必点进每一份正式提交页，也不读取综评或维度结果。
- 数据合同：`getTeacherActivityDashboard` 的 `attention` 增加 `pendingEvaluationCount`。仅 schema v2 且当前正式修订尚无 TeacherEvaluation 时计入；v1 快照该计数为 0。班级管理权已变更时 `attention` 仍为 null。不得返回学生名单、工作草稿正文、综评、outcomes 或评价版本号。
- 展示：「我的发布」在既有待反馈/待重交旁，仅在计数大于 0 时显示「待评价 n」。v1 不显示待评价。不把该计数写成阶段完成条件，也不在工作台展开提交行。
- 授权：沿用既有发布者列表；`attention` 仅在当前仍管理目标班级时出现。不新增命令、表或 Agent 工具。
- 非目标：跨发布趋势、量规薄弱项聚合、导出、案例库、学生首页、工作台已评价覆盖 n/m。
- 验收：三份当前修订中一份已评价时工作台显示「待评价 2」；v1 不显示待评价；其他教师工作台不含该发布。2026-08-26 的开发期受保护合成验收在提交 `89f6dfdab3c12d176352a1d0374f1f4915cef4c5`、GitHub Actions run `32966494396` 上以 `AI_PROVIDER_DISABLED=1` 证明教师工作台显示「待评价 2」。

## D-042：学生活动列表只读展示待重交与重交中

- 状态：已接受
- 日期：2026-08-26
- 用户场景：学生打开自己的活动列表时，需要立刻看到哪些活动被要求按反馈重交、哪些已经开始重交草稿，而不打开每一项，也不读取反馈正文或综评。
- 数据合同：`listStudentReleases` 增加 `followUp: "AWAITING_RESUBMISSION" | "RESUBMISSION_IN_PROGRESS" | null`，规则与 D-039 相同。不得返回 `nextStep`、反馈正文、综评或工作草稿正文。
- 展示：待重交单独成组，状态与进度显示「待重交」或「重交中」。有量规评价时仍在进度中保留「当前版已有量规评价」。不把跟进标记写成阶段完成条件。
- 授权：沿用既有学生列表可见性；其他学生继续看不到无权活动。不新增命令、表或 Agent 工具。
- 非目标：跨发布趋势、导出、案例库、学生首页量规薄弱项、把待重交写成阶段闸门。
- 验收：REVISE 且无工作副本时列表显示待重交；开始重交后改为重交中；无权学生列表不含该活动；不泄露反馈正文或综评。2026-08-26 的开发期受保护合成验收在提交 `82010d17fbb9e12394fc75685f46e47790be4600`、GitHub Actions run `32971142476` 上以 `AI_PROVIDER_DISABLED=1` 证明学生活动列表显示「待重交」且保留「当前版已有量规评价」。

## D-043：教师导出同一 Release 当前正式修订评阅名册

- 状态：已接受
- 日期：2026-08-26
- 用户场景：发布教师且仍管理目标班级，需要把当前正式修订的评阅状态带到课表或成绩册，而不打开每一份提交，也不导出证据正文或综评。
- 数据合同：`GET /teacher/releases/[releaseId]/submissions/export` 复用 `getTeacherReleaseSubmissions` 的同一授权与同一当前正式修订集合。CSV 只含班级、对象（小组名或学生显示名）、阶段、正式修订号、迟交、反馈状态、评价状态与跟进状态。不得含学生 UUID、提交 ID、证据正文、反馈正文、综评、outcomes 或附件内容。
- 展示：正式提交页提供「导出评阅名册」下载链接。尚无正式提交时仍可下载仅含表头的文件。schema v1 评价列固定为「无量规」。
- 授权：沿用既有发布者且当前班级管理者列表授权；未登录、其他教师与学生继续得到资源级不存在。不新增命令、表、审计或 Agent 工具。
- 非目标：跨发布趋势、量规薄弱项聚合、案例库、学校租户、轻量管理员、导出证据/附件/评价正文。
- 验收：有权教师下载的 CSV 含对象名与待重交/已反馈等列表已有标记，且不含证据、反馈或综评正文；其他教师访问同一导出路径得到不存在。2026-08-26 的开发期受保护合成验收在提交 `82010d17fbb9e12394fc75685f46e47790be4600`、GitHub Actions run `32971142476` 上以 `AI_PROVIDER_DISABLED=1` 证明有权教师可下载评阅名册 CSV，其他教师访问同一导出路径得到不存在。

## D-044：AI 只起草当前正式修订的量规建议，教师仍逐维终审

- 状态：已接受；附件证据边界由 D-055 扩展
- 日期：2026-08-28
- 用户场景：发布教师且仍管理目标班级，在评阅某名学生或小组的当前正式修订时，希望先得到一份按冻结量规逐维引用证据的评价草稿，再在现有表单中逐项修改并独立确认；教师不希望模型直接保存终评，也不能因模型不可用失去手写评价能力。
- 读取与授权：建议动作是第一方评阅页上的只读模型动作，不是聊天工具。它必须先复用 `getTeacherFeedbackWorkspace` 的资源级授权和当前正式修订查询，组装冻结 schema v2 量规、当前修订的可见文字与已确认检查点。D-055 后，每个当前正式附件还须逐件复用下载授权，只有不含文件名/storage key 的受限转写可进入主模型。旧反馈、旧评价与工作草稿不得送给模型。其他教师、学生、已不再管理班级的发布者、v1 快照和没有正式修订的资源都不能起草。
- 输出合同：模型必须按冻结顺序覆盖全部 4–8 个维度；每个维度只能给出四档之一并引用本版文字、已确认检查点或 D-055 本次成功读取的正式附件 ID，或者明确标记 `INSUFFICIENT_EVIDENCE` 且不带引用。输出经严格 schema 与现有评价领域合同再次验证后才返回页面；增删改名、越界引用、重复引用、引用不可读附件或根据附件存在性猜测均失败关闭。
- 交互与写入：入口嵌入现有 `EvaluationComposer`，作为次操作直接回填原有可编辑控件，并固定说明“这是 AI 建议，未经你确认不会保存”。模型成功时不创建 ActionIntent、TeacherEvaluation 或 TeacherEvaluationRevision；教师修改后仍走既有 prepare → `ConfirmDialog` → decide → save 链。最终修订绑定建议 `AgentRun` 并标记 `AI_ASSISTED`，只表示该表单曾由 AI 起草，不表示教师逐字采纳或模型作出最终判断。
- provenance 与保留：通过 schema 和领域约束的建议在同一事务把对应 `AgentRun` 结束为 `SUCCEEDED`，并向既有 ActionAudit 追加一条只绑定当前 SubmissionRevision 与评价版本、不含 Prompt 或建议正文的成功审计；最终准备命令据此拒绝其他成功运行冒充来源。供应商失败、超时或非法输出结束为 `FAILED`，教师离开或不采纳已成功建议不回拨运行状态。Prompt、模型原始建议、教师修改差异和完整请求正文不长期保存；不新增表或字段。每次教师明确重新起草产生独立 AgentRun；按钮 pending 时不允许并发重复请求。
- 外部边界与降级：认证、资源查询、AgentRun 起止和模型调用分离；模型调用不在数据库事务中。`AI_PROVIDER_DISABLED=1` 或配置不完整时不渲染入口、不构造 provider、不创建 AgentRun，原手写评价表单与确认保存路径一个字不变。模型失败或输出非法时就近显示可恢复错误并保留现有表单输入。
- UI 合同：复用当前 Classical 重设计分支的真实 token、表单层级、inline notice、次按钮、sticky rail 与移动端单栏；不新增页面列、卡片、全局抽屉、聊天区或确认弹层。最终保存继续只使用现有 `ConfirmDialog`。旧 `design-system/cdas-next/MASTER.md` 与当前分支实现存在的视觉差异不在本切片内回溯修正。
- 非目标：自动评分、AI 直接写入、全局助手附件工具、OCR/旧 `.doc` 解析、v1 量规回填、阶段状态变更、Prompt/建议全文留存、人机差异统计和模型有效性结论。
- 验收：覆盖正常起草后人工修改并确认、文字和检查点引用、附件相关维度证据不足、v1/无正式修订拒绝、其他教师与失去班级管理权后的资源级不存在、学生重交后的陈旧页面、非法模型 schema、供应商失败、pending 防重复、教师放弃零评价写入，以及 AI 禁用时完整手写闭环。

## D-045：教师端以悬浮入口承载全局独立 Agent 会话

- 状态：已接受
- 日期：2026-08-28
- 用户场景：教师希望在工作台任意页面通过一个不占页面流的悬浮入口打开独立会话，持续交代活动设计职责、检索官方课程依据，并在需要时沿用已经接受的草稿创建与发布审批；页面本身的表格、表单、双栏与移动端单栏不能因助手常驻而改变。
- 呈现与会话：教师路由外层只在 AI 配置完整且当前用户经服务端确认是教师时挂载一个固定定位的圆形入口和非模态面板。面板不进入页面 `main`、grid 或 sticky rail，不改变内容宽度和滚动；桌面使用受限宽高的右下角会话窗，窄屏收敛为保留安全边距的底部面板。入口使用 disclosure 语义，支持明确关闭和 Escape，关闭不清空会话。`ActivityAssistantSessionProvider` 上提到 `/teacher` layout，使同一内存会话在教师端客户端导航间保留；刷新、离开教师路由或直接深链不从 URL、localStorage、cookie、数据库恢复任何消息、审批或长期记忆。
- 职责边界：本切片只把 D-023/D-033 已接受的 `search_knowledge`、`read_source_section`、`create_activity_draft` 与 `publish_activity_release` 搬进全局表面，不增加通用导航、页面 DOM 操作、工作台查询、评价建议、成员管理、关闭发布或其他工具。D-044 量规建议继续只在评阅表单内起草。以后每增加一项职责，都必须复用对应第一方已鉴权查询或领域命令，并独立更新决策与验收。
- 导航边界：写工具成功只返回并渲染服务端校验后的精确站内链接，模型和会话 provider 不调用 `router.push`；教师明确点击后才导航。面板与原生 `ConfirmDialog` 同时存在时，Escape 由 dialog 优先处理，不能被 Agent 的全局监听截获。
- 权限与写入：悬浮入口不授予额外权限。API 仍逐请求认证教师，工具执行仍逐资源检查；创建草稿继续先展示结构化提案并等待签名 L1 approval，发布继续展示精确版本、班级与截止时间并等待签名 L2 approval。手工创建、编辑、预览和发布不依赖面板；AI 禁用、配置不完整、身份不符或助手上下文不可建立时不渲染入口、不查询班级、不构造 provider。
- 实现边界：复用现有 AI SDK `useChat + streamText + signed toolApproval`、同一 route、AgentRun 生命周期、ActionIntent 和领域命令，不引入 CopilotKit、assistant-ui、ChatKit、第二后端、RAG、持久线程、通用 Agent runtime 或多 Agent。成熟产品只作为交互形态参考，不复制其运行时和视觉系统。
- UI 合同：复用当前 Classical token、边框、字阶、按钮和动效时长；悬浮层使用 `--z-overlay`，不使用 `showModal`、背景遮罩或让页面 inert，以免与既有 `ConfirmDialog` 冲突。面板内长消息独立滚动，输入区保持可见；审批内容在窄面板内改为单列，320–390px、768px 和桌面视口均不得产生页面横向溢出。学生端、未认证门页和 AI 关闭态不出现入口。
- 非目标：跨刷新恢复、多会话列表、历史会话、长期记忆、未读通知、语音、文件上传、学生助手、全局评价工具、模型自动跳转任意页面、复制第三方聊天组件样式或替换现有页面导航。
- 验收：AI 开启且教师已认证时，工作台与非活动教师页均显示唯一悬浮入口；打开后可在独立面板完成现有活动助手对话与签名审批，跨教师页导航消息保持，关闭再开消息仍在，刷新不恢复。面板不改变页面流、主内容宽度或手工控件；AI 关闭、配置不完整、学生、未登录与无法建立教师上下文时无入口且无助手班级查询。桌面、平板和窄屏均可打开、输入、滚动和关闭，不遮断既有确认弹层的权限或语义。

## D-046：教师全局 Agent 首批读取当前页面与本人工作区摘要

- 状态：已接受
- 日期：2026-08-28
- 用户场景：教师在任意教师页面打开全局 Agent 后，希望它知道自己当前位于哪类页面，并能回答“我有哪些班级、草稿、发布和待办”，同时给出可点击的精确站内入口；教师不希望模型自动跳转、读取其他教师资源、展开学生名单或改变现有页面布局。
- 当前页面合同：客户端每次请求只提交由白名单路径解析出的页面类型和可选 UUID，不提交 query string、页面正文、任意 URL 或 DOM。服务端严格校验该提示；静态教师页只映射预定义名称与链接，草稿、发布和班级动态页只有在既有授权工作区结果中存在同一资源时才返回 canonical href。提交评阅等本批未接入资源详情的页面只返回无资源标识的页面类型。未知、伪造或跨教师资源降级为不可用上下文，不反射客户端路径，也不作为授权依据。
- 只读工具合同：新增 `get_current_context`、`list_my_classrooms`、`list_my_activity_drafts` 与 `list_my_releases`。后三者复用 `getTeacherActivityDashboard` 的同一教师角色与资源过滤，分别只返回班级名称和当前人数、草稿标题/状态/版本/时间、发布标题/班级/状态/时间/提交进度及待反馈/待评价/待重交计数。不得返回学生姓名、提交 ID、证据、反馈、评价、草稿正文或成员明细。既有工作台查询明确允许 UI 与 AGENT 来源，但 actor 仍由服务端会话注入。
- 历史与导航边界：只读工具输出使用严格 schema 和服务端 canonical 映射；续轮客户端消息历史中的工作区结果必须能与当前授权工作区重新核对，不能用伪造标题、数量、ID 或 href 污染模型上下文。工具只返回固定 `/teacher/...` href，UI 使用现有 `<Link>` 渲染，模型、transport 和 provider 不调用 `router.push`，教师点击后才导航。
- 执行与降级：本批 L0 工具不创建 ActionIntent、业务修订或额外审批；普通只读对话仍按既有 AgentRun 生命周期记录运行结果。AI 禁用、配置不完整、认证失败、查询失败或模型失败时不扩大入口与数据权限，教师工作台、手工创建/编辑/预览/发布和现有页面导航照常可用。
- UI 合同：只在既有会话 transcript 内复用现有结果块、字阶、边框、间距与站内链接样式，不增加页面列、固定工具栏、背景遮罩、第二个悬浮入口或自动展开区域；结果列表在桌面、平板和 320–390px 窄屏内换行并独立滚动，不改变页面 `main`、grid、sticky rail 或 ConfirmDialog。
- 非目标：完整草稿正文、发布提交名册、学生或成员姓名、评价与反馈详情、过程诊断数据、知识页 query 上下文、任意 URL/DOM 导航、自动点击、持久会话、多 Agent、RAG、新数据库表，以及新的业务写工具。
- 验收：本人班级、草稿和发布按现有工作台顺序返回 canonical 链接，发布只在仍有班级管理权时返回提交页入口与待办；其他教师资源、伪造历史和伪造页面 UUID 不进入结果或模型历史。跨教师页面导航后的下一次请求携带新的白名单页面上下文；教师仍需点击链接才跳转。新增结果不改变悬浮面板或底层页面布局，AI 关闭时手工闭环不变。

## D-047：教师全局 Agent 按需读取本人某一份草稿的完整任务书

- 状态：已接受
- 日期：2026-08-28
- 用户场景：教师在教师端任意页面打开全局 Agent 后，指着自己的某一份草稿说"帮我看看这个三阶段是不是同一个故事"，希望助手读到那份草稿当前版本的完整任务书，再按既有逆向设计与情境叙事标准逐条指出问题并给出可直接替换的文字；教师不希望助手凭 D-046 的标题摘要猜内容，也不希望它绕过草稿页直接改写自己的作品。
- 读取与授权：新增 `get_activity_draft`，输入只有一个 `draftId`。请求先在 D-046 已授权的工作台结果中比对该 ID，不在其中的一律返回 `NOT_FOUND` 且不发出数据库查询；命中后才调用既有 `getTeacherActivityDraft`，由它再次证明 `ownerId` 即当前教师。该查询同时允许 UI 与 AGENT 来源，但 actor 仍由服务端会话注入，SYSTEM 及其他来源继续被拒。其他教师的草稿、伪造 UUID 与查询期间已失去的资源在模型看来完全一致，都是资源级不存在。
- 输出合同：只返回该草稿当前修订的 schema v2 内容、草稿状态、版本号、更新时间、是否已发布，以及 canonical 编辑与预览链接。schema v1 快照只返回标题与链接并标记 `LEGACY_SNAPSHOT`，正文不进模型——它的结构早于助手被指令的设计方法，D-044 也已拒绝对它起草。不返回 Release ID、班级、学生、提交、附件、反馈或评价数据，也不返回草稿的历史修订。
- 历史与导航边界：续轮客户端历史中的每一条草稿读取结果都按当前授权重算后才进入模型，输入之外的标题、正文、状态、链接一律丢弃；期间被改名、改写、删除或转手的草稿不能以旧貌留在对话里。同一请求内对同一草稿只作一次授权判定，历史重算与本轮工具调用共用该结果。工具只返回固定 `/teacher/activities/...` href，由 UI 用现有 `<Link>` 渲染，模型不调用 `router.push`。
- 执行与降级：本工具是 L0 只读，不创建 ActionIntent、草稿修订或审批，也不改写教师已有草稿；教师要落地修改仍走草稿页表单，或明确要求助手按新要求另建一份草稿。草稿读取遭遇基础设施失败时在建立 AgentRun 之前就近失败关闭，不留下悬空运行；AI 禁用、配置不完整或认证失败时不扩大入口与数据权限，手工工作台与草稿页照常可用。
- UI 合同：复用会话 transcript 内既有结果块、字阶、边框与站内链接样式，只多一行阶段数、量规维度数与更新时间的摘要；两个链接放进既有 inline actions 容器，窄面板内单列排布。不增加页面列、固定工具栏、遮罩、第二个悬浮入口或自动展开的正文区域，不改变页面 `main`、grid、sticky rail 或 ConfirmDialog。
- 非目标：改写或删除已有草稿、草稿历史修订与差异、发布提交名册、学生或成员姓名、评价与反馈详情、过程诊断数据、v1 正文、附件解析、跨教师草稿检索、把读取结果持久化成诊断实体。
- 验收：本人草稿返回当前版本完整任务书与 canonical 链接，助手据此给出逐条修订建议但不写入；其他教师的草稿与伪造 UUID 不触发查询且返回不存在；v1 快照只返回标题与链接；历史中被伪造的草稿正文按当前授权重算后不进入模型；草稿在两轮之间被删除或转手后不再以旧貌出现；基础设施失败时不建立 AgentRun；AI 关闭时草稿页手工闭环与页面布局不变。

## D-048：教师确认后 Agent 改写本人未封存草稿的一个新版本

- 状态：已接受
- 日期：2026-08-28
- 用户场景：教师读完助手对某一份草稿的诊断后说"就按你说的改第二阶段"，希望助手把修改直接写成这份草稿的新版本，而不必自己把整段文字誊回表单；同时教师要能在写入前看清到底改了哪几处，要能拒绝，也要确信原版本不会被抹掉、没被要求改的地方不会被顺手重写。
- 前置读取：`update_activity_draft` 只能改写本轮对话里已由 D-047 `get_activity_draft` 返回 FOUND 的草稿，且 `expectedVersion` 必须等于当时读到的版本。该"已读版本"账本由服务端按重算后的历史重建，伪造的历史读取不能解锁改写。执行前再读一次当前草稿：授权与版本在写入这一刻重新成立，不只是在模型读的时候成立。
- 改动声明与核对：输入必须逐条声明改动的任务书区域（基本设置、背景设定、三维目标、总体任务、任务链阶段、证据要求、评价标准）及理由。服务端按 v2 字段归属计算真实差异，声明集合与真实差异集合必须完全相等：漏报越界改动、虚报未动区域、以及零改动的空改写都失败关闭，不进入教师确认。阶段与量规的顺序变化计入改动。
- 确认与写入：审批闸门在展示给教师之前先核对上述账本，未读、越权或版本不符的改写被自动拒绝，教师根本不会看到一张描述"改写你的草稿"的确认卡。通过后走既有签名 L1 approval；教师确认才调用既有 `saveActivityDraft`，由它再次校验 ownerId、SEALED 与 `expectedVersion`。与 D-033 一致，草稿改写不建立 ActionIntent——它不是已发布制品。每次请求最多改写一份草稿；教师拒绝后不重试。
- 版本与历史：改写产生新的 `ActivityDraftRevision`，`source = AGENT` 并绑定本次 AgentRun，原版本作为历史修订完整保留，append-only 不变。改写后草稿状态统一为 `READY_FOR_PREVIEW`：教师刚确认过一份完整 v2 任务书。写入成功后账本推进到新版本，同一对话里再次改写必须先重新读取。
- 降级与边界：`saveActivityDraft` 的 STALE_VERSION、DRAFT_SEALED、NOT_FOUND、CONCURRENT_WRITE 与幂等规则一律沿用，不放宽；教师在别处编辑过这份草稿时改写失败且原内容一个字不变。AI 禁用或配置不完整时不渲染入口，草稿页手工编辑与版本历史不受影响。
- 非目标：改写他人草稿、改写已封存草稿、v1 快照、片段式补丁、自动应用不经确认、跨草稿批量改写、删除或回滚修订、改写发布快照、把改写记入 ActionIntent 或新建诊断实体。
- 验收：读过同一版本后按声明改写成功并留下新修订与历史版本；未读、版本不符、越权草稿在教师看到确认卡之前就被拒绝；越界改动、虚报区域与零改动失败且零写入；教师拒绝确认时零写入；同一请求第二次改写被拒；封存草稿与并发改版由既有命令失败关闭；AI 关闭时草稿页手工闭环与版本历史不变。

## D-049：把提案里模型不该复述的事实交回服务端，并让失败自陈原因

- 状态：已接受
- 日期：2026-08-29
- 背景：真实模型下 `create_activity_draft` 长期无法稳定走完。排查发现四类原因，全部与"要求模型复述服务端已知的事实"或"看不出失败原因"有关：模型把提案写成散文再问教师要不要建立草稿；工具参数里的中文用半角双引号导致 JSON 解析失败；`citationLabel`/`href`/`schemaVersion`/`integratedDisciplineCodes` 这些服务端已知的值被要求逐字重打；失败只报到字段路径，看不出是内容错、格式错还是被截断。
- 引用合同：`sourceReferences` 只收 `sourceId`、`sectionId` 与采用理由，上限由 8 收紧到 4。标题与站内链接由服务端按同一份语料查出，教师确认卡片改为渲染该轮 `read_source_section` 返回的规范措辞。引用仍必须存在于语料、覆盖学段与学科、不得重复、且必须是本轮已通读章节——校验一条不放宽，只是不再校验模型有没有把文字抄对。
- 补齐合同：`content.schemaVersion` 恒为 2，`content.integratedDisciplineCodes` 在模型未提供时按 `integratedDisciplineContributions` 推导。模型仍可提供，提供了就继续按原规则与贡献逐项核对；重复贡献单独报 `DISCIPLINE_REPEATED`，贡献学科改用学科目录枚举。补齐只消除重复填写，不放宽一致性。
- 失败可读性：领域校验的自定义问题带稳定 `params.reason`，`AgentRun.failureCode` 在字段路径后附加该原因（例如 `..._SOURCEREFERENCES_0_SOURCE_NOT_READ`）。原因只取自本仓库自己写的静态标记，不含模型文本。供应商因长度截断与模型写出非法 JSON 分别记为 `OUTPUT_TRUNCATED` 与 `JSON_PARSE`，两者修法不同，不再混为一谈。
- 提示词：明确"调用工具就是在请求确认"——工具本身会把参数渲染成确认卡片并暂停，因此不得先用散文复述提案再询问教师，否则教师看不到可确认的卡片。工具参数中的中文一律不用半角双引号，需要引用用「」；提示词自身的引号同步改为全角，不再示范会导致解析失败的写法。提案各字段只写一到两句，引用固定两条。
- 输出上限：`maxOutputTokens` 由 8_000 提到 16_000。观测显示截断并非当前主因，但提案信封（摘要、假设、贡献、三条链）确实使载荷接近旧上限，保留余量以免与内容错误混淆。
- 非目标：自动重试非法工具调用、更换模型供应商、放宽任何授权或一致性校验、把服务端补齐扩大到教师真正需要决定的字段（学段、年级、学科、阶段、量规仍必须由模型明确给出并由教师确认）。
- 验收：引用只给出标识仍能通过语料、学段、学科与通读校验，伪造章节仍被拒；省略 schemaVersion 与融合学科代码的提案可通过并被补齐，提供却与贡献不符的仍失败；重复贡献单独报错；失败码带上稳定原因；提示词不含半角双引号。真实模型验收在同一套合成数据上从长期无法通过变为可稳定通过，剩余失败均为单点内容错误且教师可就地重试。

## D-050：非法工具参数允许一次修复重试，边界一律不变

- 状态：已接受
- 日期：2026-08-29
- 用户场景：教师把活动要求说清楚后，助手偶尔会在一次大型结构化调用里写错一处（多写一条融合学科贡献、阶段数不对、标点破坏 JSON）。教师看到的只是"草稿未创建"，得自己再说一遍"请重新创建草稿"。D-049 已把模型不该复述的事实收回服务端，剩下的都是单点笔误；这类错误模型在被告知具体报错后通常能自己改对。
- 范围：只有 `create_activity_draft` 与 `update_activity_draft` 的参数因 schema 校验失败时，每个请求最多重试一次。工具不存在、审批被拒、领域命令失败、供应商失败都不重试。重试用同一模型、同一份工具 schema，把上一次参数与校验报错原样交回并要求只重发一次调用。
- 不放宽任何边界：重试使用只含 schema、不含 execute 的工具注册表，因此修复过程本身不可能执行任何工具、写任何数据。修复后的参数走与首次完全相同的校验：同一 zod schema、官方来源必须本轮通读、草稿改写必须已读到同一版本、签名审批照常暂停等待教师确认、领域命令再次证明 ownerId/SEALED/expectedVersion。修复只是让模型有一次改正笔误的机会，不是让它绕过任何一道关。
- 失败与可见性：修复失败、超时、被中断或再次非法时，与从未修复过完全一致地失败关闭——同一失败码、同一 AgentRun 终态、同一"草稿未创建"提示与手工降级路径。修复不产生额外 AgentRun、ActionIntent 或审计记录；它是同一次运行内的一次模型调用。
- 成本：修复只在失败路径上发生，正常路径不增加任何调用。每请求上限一次，不存在重试风暴。
- 非目标：多次重试、跨请求重试、对领域命令失败重试、自动采纳修复结果而不经教师确认、把修复扩展到发布或评价、更换模型。
- 验收：一次非法提案经修复后仍然作为待确认卡片呈现且零写入；修复过程不调用任何 execute；同一请求内第二次非法参数不再触发修复并按原路失败关闭；修复本身失败时与无修复行为一致。真实模型验收在同一套合成数据上连续通过。

## D-051：教师全局 Agent 读取某次发布的过程诊断聚合

- 状态：已接受
- 日期：2026-08-29
- 用户场景：教师在教师端任意页面问助手「这次发布学生卡在哪个阶段」「量规哪一维最弱」「让他们重交之后有没有进步」，希望直接得到这次发布的过程数据并据此讨论任务书哪里要改；教师不希望助手因此拿到学生名单，也不希望它把统计说成对某个学生或对课程质量的结论。
- 读取与授权：新增 `get_process_insights`，输入只有 `releaseId`。请求先在 D-046 已授权的工作台结果中比对该 ID，不在其中的一律返回 `NOT_FOUND` 且不发出查询；命中后复用第一方过程诊断页的同一个 `getTeacherInsights`，该查询按 `publisherId` 限定并只允许 UI 与 AGENT 来源，actor 仍由服务端会话注入。查询返回里没有这次发布时，同样降级为资源级不存在。
- 数据合同：只返回这次发布的标题、班级名、状态、过程诊断页链接，以及三组计数——各阶段桶的对象数与总对象数、冻结量规每个维度的五档分布与最弱维度标记、重交后评价上升/持平/下降的份数。不返回学生或小组姓名、成员数、提交 ID、维度序号、证据、反馈、综评、outcomes 或任务书正文。所有数字都是队列层面的计数，无法归因到个人。
- 解读边界：提示词要求把弱项当作任务书与教学安排的信号（证据要求是否含糊、支架是否足够、阶段是否断层）并给出可改之处，不得据此推断某个学生的表现，也不得输出课程质量或达标结论。教师要看具体是谁，由助手引导他打开提交页。
- 历史与导航边界：续轮历史中的诊断结果按当前授权重算后才进入模型，伪造的标题、计数或链接不能污染上下文。工具只返回固定 `/teacher/insights` 链接，由 UI 用现有 `<Link>` 渲染，教师点击才导航。
- 执行与降级：L0 只读，不创建 ActionIntent、业务修订或审批，普通对话仍按既有 AgentRun 生命周期记录。查询或模型失败时就近失败，过程诊断页与其余手工闭环不受影响。
- UI 合同：复用会话 transcript 内既有结果块、字阶、边框与站内链接样式，阶段分布与重交计数以短句呈现，不新增页面列、图表、遮罩或第二个入口。
- 非目标：学生或小组层名册、按人查看阶段与评价、提交与反馈明细、跨发布趋势、导出、把弱项写成阶段闸门或课程标准结论、按诊断结果自动改写草稿。
- 验收：本人发布返回与过程诊断页一致的阶段分布、量规分布、最弱维度与重交计数，并给出 canonical 链接；其他教师的发布与伪造 UUID 不触发查询且返回不存在；结果不含任何学生、小组、提交或正文字段；历史中伪造的诊断结果按当前授权重算；AI 关闭时过程诊断页与手工闭环不变。

## D-052：AI 起草形成性反馈，教师仍逐句终审

- 状态：已接受
- 日期：2026-08-29
- 用户场景：教师在评阅某名学生或小组的当前正式修订时，希望先拿到一段引用本版证据、指出下一步该做什么的反馈草稿，再在现有表单里改写并独立确认；教师不希望模型直接把反馈发给学生，也不能因模型不可用而失去手写反馈的能力。这是 D-044 明确留到之后的反馈侧对称切片。
- 读取与授权：起草是评阅页上的只读模型动作，不是聊天工具。它先复用 `getTeacherFeedbackWorkspace` 的资源级授权和当前正式修订查询，组装教师自己写的本阶段要求（名称、核心动作、评价要点、类型化证据说明）、本版可见文字和已确认检查点。D-052 最初不送附件内容；D-055 后每个当前正式附件须再复用下载授权，只有不含文件名/storage key 的受限转写可进入模型。旧反馈、旧评价与工作草稿仍不送。其他教师、已失去班级管理权的发布者与陈旧页面都不能起草。
- 输出合同：模型只返回 `body`、`nextStep`、`supportLevel` 三个字段。正文限 40–1200 字，须用第二人称先引用学生这一版真正写下的一件具体事，再指出一处最值得改进处并给出下一步做法。`nextStep` 只能是 CONTINUE 或 REVISE，`supportLevel` 只能是 FOUNDATION/STANDARD/CHALLENGE。返回后先过严格 schema，再用既有 `createTeacherFeedbackPayload` 走一遍领域合同，不通过就失败关闭，不进入表单。schema v1 快照仍可起草，但不虚构阶段要求：`phase` 为 null，检查点为空。
- 交互与写入：入口嵌入现有 `FeedbackComposer`，作为次操作回填正文与两个结构化选择，全部可改；固定说明这是 AI 建议、未经确认不会保存。起草成功不创建 ActionIntent 或反馈修订；教师修改后仍走既有 prepare → `ConfirmDialog` → decide → save 链。最终修订绑定该 `AgentRun` 并标记 `AI_ASSISTED`，只表示这一版曾由 AI 起草，不表示教师逐字采纳。
- provenance 与保留：通过校验的建议在同一事务把 `AgentRun` 结束为 `SUCCEEDED`，并追加一条只绑定当前 SubmissionRevision 与反馈版本、不含 Prompt 或建议正文的成功审计（新增 `complete-teacher-feedback-suggestion`，与评价侧同构）。供应商失败、超时或非法输出结束为 `FAILED`。Prompt、模型原文与教师改动差异不长期保存；不新增表或字段。
- 外部边界与降级：认证、资源查询、AgentRun 起止与模型调用分离，模型调用不在数据库事务中。`AI_PROVIDER_DISABLED=1` 或配置不完整时不渲染入口、不构造 provider、不创建 AgentRun，原手写反馈表单与确认保存路径一个字不变。
- 非目标：自动发送反馈、AI 直接写库、跨修订或跨学生批量起草、把建议写成阶段闸门、评价侧自动写入、把附件读取挂进全局助手。
- 验收：正常起草后人工修改并确认、引用本版文字与已确认检查点；D-055 后还须证明成功读取附件的具体事实可进入反馈，而文件名/storage key 不外泄、不可读附件不被猜测。v1 快照不虚构阶段要求，其他教师与失去班级管理权后返回资源级不存在，学生重交后的陈旧页面、非法模型输出、供应商失败和教师放弃均零写入，AI 禁用时完整手写闭环。

## D-053：起草提示词必须自报输出字段名

- 状态：已接受
- 日期：2026-08-29
- 背景：D-044 的评价建议与 D-052 的反馈建议都用 `Output.object` 声明 schema，但 DeepSeek 走的是 OpenAI 兼容的 `json_object` 响应格式，它只保证返回合法 JSON，**不按 schema 约束字段名**。模型因此只能从提示词里得知该用什么字段。真实模型验证发现：反馈建议会把正文写进 `draftFeedback`，评价建议会返回 `rubricDimensions` 数组并给每项加 `justification`——内容其实写得不错，但字段名全不对，校验一律失败。也就是说这两个起草功能对真实模型的成功率是 0，而单元测试因为把 `generateSuggestion` 整个替换掉，从来没有覆盖到这一段。
- 决定：两个起草提示词都必须逐字列出输出字段名与取值形状（评价侧还须写明 `citations` 的两种对象形式），并保留提示词中的 “JSON” 一词——供应商要求提示词出现该词才接受 `json_object`。提示词构造函数导出，并由合同测试断言字段名齐备，防止改写提示词时静默丢失。
- 边界不变：这只改变模型如何被告知输出形状。返回结果仍过同一 schema、同一领域合同、同一资源级授权与同一教师确认链；模型说错字段名时仍然失败关闭，不做任何字段名猜测或映射。
- 非目标：改用供应商的结构化输出模式、放宽 schema 以迁就模型、按相似度自动重命名字段、把模型原文直接落库。
- 验收：两个提示词都包含各自的全部输出字段名与 “JSON”；真实模型在同一套合成输入上连续返回可通过校验的建议；字段名从提示词中移除时合同测试失败。

## D-054：教师全局 Agent 读取某次发布的提交名册（匿名序号）

- 状态：已接受（项目所有者 2026-08-29 就全部 OPEN QUESTION 作出裁定，取匿名序号方案）。
- 日期：2026-08-29
- 用户场景：教师发布一次活动后带着整班的进度，最常问的是「这几十个里，哪几个我今天得先看」。D-046 的 `list_my_releases` 只给队列层待办计数，D-051 的 `get_process_insights` 只给阶段分布与量规分布，两者都说不出是哪几个对象；教师要么自己打开提交页逐行扫，要么下载 D-043 的 CSV。教师要的是一份可排序的优先级名册，不是对学生的判断，也不希望助手因此拿到提交正文、证据或反馈评价明细。
- 读取与授权：沿用 D-047／D-051 的两道门。第一道：`releaseId` 先在 D-046 已授权的 `getTeacherActivityDashboard` 结果中比对，且该发布的 `canViewSubmissions` 必须为真（即教师仍是发布者且仍管理目标班级）；未命中或为假一律返回资源级不存在，**不发出任何数据库查询**，其他教师的发布与伪造 UUID 因此不可区分。第二道：命中后复用第一方提交名册页同款查询 `getTeacherReleaseSubmissions`（`src/server/queries/submission-workspace.ts`），它自身要求 actor 同时等于 `release.publisherId` 与 `release.classroom.managerId`，否则抛 `SubmissionWorkspaceQueryError("NOT_FOUND")`。该查询自 `f5cbaf0` 起就已 `resolveCommandContext(..., ["UI", "AGENT"])`，本决策**不需要放宽任何来源**；但来源早已允许不等于职责已开放——开放这项职责的是本决策本身。actor 仍由服务端会话注入，客户端不能提交 actor 或班级。
- 数据合同（字段与必要性）：返回发布层的标题、班级名、状态、提交模式、阶段总数与名册页 canonical href；返回逐行的阶段位置（第 n 阶段及阶段名，或「整项提交」）、当前正式修订号、是否迟交、反馈状态（待反馈／已反馈 vN）、评价状态（待评价／已评价 vN／无量规）、跟进状态（待重交／重交中／无）；返回尚未正式提交的对象及其 `started`、`completedPhaseCount`、`currentPhaseIndex`、`awaitingFormalRevision`；返回 `reviewCoverage` 三个计数。逐字段必要性：阶段位置是优先级排序的第一依据（卡在第 1 阶段比已到第 3 阶段更急）；修订号与迟交区分「第一次交」与「重交多次仍未过」；反馈／评价状态就是待办本身，D-041 与 D-046 已在计数层暴露过同一事实；跟进状态说明球在谁手上，待重交不必催、重交中不必打扰；未提交对象是「谁还没交」唯一的答案来源，缺了它这个工具就答不了教师最常问的那句话；`reviewCoverage` 让模型直接说出剩余量而不必自己数。
- 数据合同（不返回）：`submissionId`、`studentId`、`groupId` 等裸 UUID 字段（无教学含义，且是可被用于拼接后续请求的标识；行级链接由服务端生成为 canonical href，`submissionId` 只作为该 href 的一部分出现，不单独成字段）、提交正文与证据、附件（含文件名与媒体类型）、反馈正文、评价综评与 outcomes、小组成员明细与角色标签、精确提交时间戳（迟交布尔已足够）。
- 数据合同 · 身份边界（**本决策的硬边界**）：**学生显示名、小组名以及任何其他显示名一律不进入模型**。每一行只用匿名序号呈现：`objectOrdinal` 从 1 开始，按第一方名册页同一排序（即 `progress` 既有顺序）在本次响应内由服务端分配；`objectKind` 区分 `STUDENT` 与 `GROUP`，UI 渲染为「对象 n」或「小组 n」。序号不写库、不做持久映射、不保证跨请求稳定：每次响应重新分配，提示词必须写明序号只在本次结果内有效，跨轮不得沿用。小组名同样排除——它是教师自由输入，无法保证不含学生姓名。教师需要知道是谁时点行级链接进入第一方页面，姓名只在那里出现。**将来若要放开姓名或小组名，必须另立决策；不得在实现中顺手增加字段。**
- 数据合同 · 被否决的备选（记录在案）：曾考虑沿用第一方页面与 D-043 CSV 导出相同的「对象」口径（小组名，否则学生 `displayName`）。否决理由：那会是本项目第一次把可识别的学生身份交给外部模型供应商，而「尚未决定」一节里「模型供应商、区域、日志保留和关闭模型时的降级行为」至今没有定案，等于把一条未决风险变成正在发生的事实。D-043 的类比不成立：CSV 下载到教师本机，姓名没有离开信任边界，模型调用会离开。匿名序号方案随时可以升级为含姓名，反向不可逆。
- 数据合同 · 规模与输入面：单次最多返回 60 个对象，超出时按第一方排序截断并返回 `truncated: true` 与对象总数，提示词要求助手如实说明「只列出了前 60 个」并引导教师打开名册页。v1 输入面保持最小：只有 `releaseId`，不提供筛选、分页或排序参数。
- 与 D-051 的边界：D-051 承诺的是它自己的输出只有队列层计数、无法归因到个人，那是**那个工具的数据合同**，不是整个 Agent 的全局承诺。本工具是第一次把个体层状态放进模型，因此按仓库既有做法（「以后每增加一项职责，都必须复用对应第一方已鉴权查询或领域命令，并独立更新决策与验收」）单独立项、单独验收。D-051 不被推翻：它的输出仍然只有计数，仍然无法归因，两个工具的数据合同各自独立成立，教师也可以只用其中一个。提示词必须加上解读边界：名册状态只是「谁需要教师优先看」的工作信号，不得据此对任何学生的能力、态度或表现下结论，不得推断原因，也不得排出「好坏名次」；要判断具体情况必须点进提交页看原始证据。
- 历史与导航边界：续轮客户端历史中的名册结果按当前授权整块重算（与 D-047、D-051 的 reader 同一模式，同一请求内对同一发布只作一次授权判定），伪造的姓名或序号、状态、计数、链接都不能留在模型上下文里；教师在两轮之间失去班级管理权、发布被删除或被移出工作区时，重算结果为资源级不存在。工具只返回服务端生成的 canonical `/teacher/releases/<id>/submissions` 与行级 `/teacher/submissions/<id>` href，由 UI 用现有 `<Link>` 渲染，模型不调用 `router.push`，教师点击才导航。
- 已关闭与归档发布：与第一方名册页一致，发布关闭或归档后教师仍可读取名册（关闭后教师仍需完成反馈与评价）。不额外开放 `get_release_summary`——D-046 的 `list_my_releases` 与 D-051 的 `get_process_insights` 已覆盖发布层摘要，再加一个工具只增加表面积而不增加能力。
- 执行与降级：L0 只读，不创建 ActionIntent、业务修订或审批；普通对话仍按既有 AgentRun 生命周期记录。查询失败或模型失败时就近失败关闭，不扩大入口与数据权限；`AI_PROVIDER_DISABLED=1` 时不渲染入口、不构造 provider，提交名册页、CSV 导出与手工评阅闭环一个字不变。
- UI 合同：只在既有会话 transcript 内复用现有结果块、字阶、边框、间距与站内链接样式；名册可能较长，渲染行数需设上限并保持独立滚动，不新增页面列、表格控件、遮罩、第二个悬浮入口或自动展开区域；桌面、平板与 320–390px 窄屏均不得产生页面横向溢出，不改变页面 `main`、grid、sticky rail 或 ConfirmDialog。
- 非目标：提交正文与证据内容、附件（含文件名）、反馈与评价明细、学生或小组的裸 UUID、跨发布聚合与趋势、按名册自动批量起草反馈或评价、把名册数据持久化成新实体或新表、排名与打分、据名册推断学生能力或原因、`suggest_activity_structure`。
- 验收：有权教师对本人发布返回与第一方名册页一致的对象集合与状态，全部以匿名序号呈现且响应中不含任何显示名；超过 60 个对象时截断并如实标注；其他教师与伪造 UUID 不触发查询即资源级不存在；教师失去班级管理权后同一 `releaseId` 立即变为不存在；发布已关闭时仍可读名册且行为与第一方页面一致；重复调用为纯只读、不写业务历史、不创建 ActionIntent；查询或模型失败时就近失败且不泄露资源是否存在；历史中被伪造的姓名／序号、状态与计数按当前授权重算后不进入模型；`AI_PROVIDER_DISABLED=1` 时提交名册页与导出的手工闭环及页面布局不变。

## D-055：附件在自托管可用、可预览，并可由第一方评阅建议受限读取

- 状态：已接受
- 日期：2026-08-29
- 用户场景：学生在中国 VPS 或 Vercel 上提交图片、PDF 或 Word 证据后，自己与有权教师应能在原工作区直接查看可安全呈现的内容；教师请求形成性反馈或量规评价建议时，模型应能使用当前正式修订中真正读到的附件证据，而不是永远把附件当作未知数量。核心文字/检查点闭环仍不能依赖附件或模型。
- 私有存储：保留既有 `AttachmentStorage` 编排与 `SubmissionAttachment` 业务记录。Vercel 继续使用 Private Blob；自托管在 `ATTACHMENT_STORAGE_ENABLED=1` 且给出 `ATTACHMENT_STORAGE_DIR` 时使用 release 目录外的持久磁盘，本机优先于同时配置的 Blob。服务端接收路由只写对象和媒体类型 sidecar，既有 finalize/内容验证命令仍是唯一状态转换入口。两种后端复用相同的文件签名校验；这不声称是恶意文件扫描，也不比原 Blob 实现降级。路径 containment 在持有文件句柄的存储实现中校验。
- 预览：授权下载路由只按数据库保存的媒体类型决定 disposition；JPEG/PNG/WebP 与 PDF 可 inline，DOC/DOCX 和未知类型保持 attachment。图片/PDF 在现有工作区以原生 dialog 叠加预览，下载入口始终保留，不把评阅双栏撑成长页面。响应统一带 `sandbox` CSP、`nosniff` 与 `private, no-store`；请求参数不能把不可预览类型改成 inline。当前不引入 PDF.js 高保真渲染或 DOCX 浏览器渲染。
- 评阅读取边界：只扩展 D-044/D-052 的第一方教师评阅页起草动作，不新增全局 `read_submission_attachment` 工具。输入附件只能来自 `getTeacherFeedbackWorkspace` 已确认的当前正式修订；随后每个 ID 由更严格的 `getAuthorizedCurrentRevisionAttachmentDownload` 同时绑定指定 Submission、指定正式修订、最新修订号、发布教师与当前班级管理员。附件解析结束后、主起草模型调用前还须重新读取受保护工作区，陈旧修订或反馈/评价版本变化均失败关闭；storage key、永久 URL、文件名和原始字节不进入主起草模型。客户端不能提交附件 ID 解锁额外读取。
- 内容处理：图片使用独立、无工具、固定提示词的视觉子调用转写可见事实，主起草模型仍使用既有文本模型；默认视觉模型为 `deepseek-v4-flash-vision-exp`，可用 `AI_ATTACHMENT_VISION_MODEL` 在同一 DeepSeek 白名单内替换。DOCX 在服务端抽取纯文本，每个附件最多传入前 12,000 个 Unicode code point 并明确标记截断；在交给 Mammoth 前必须核对 ZIP 条目、本地头与实际受限展开量。PDF.js 会在公开文本流产出前物化已解码页面内容，页数或输出计数不能形成可靠的进程资源边界，因此 PDF 在具备隔离解析器前一律标记不可读，只保留授权预览/下载；旧 `.doc` 同样暂不解析。不可读文档只提示教师核对原件，不进入主起草模型。本期不做 PDF 自动解析/OCR。附件中的内容始终按不可信学生证据处理，不能成为模型指令。
- 建议与引用：形成性反馈可以使用 `READABLE` 附件的具体事实，但仍只回填现有表单。量规建议只允许引用本次成功读取的附件 ID；读取失败、不支持或存储不可用的附件不进入领域命令的授权 ID 集合，模型误引时由既有 `createTeacherEvaluationPayload` 失败关闭。教师仍逐句/逐维编辑并走独立确认；起草本身不创建 ActionIntent、反馈或评价历史，最终来源与成功审计规则完全沿用 D-044/D-052。
- 外部边界与降级：对象读取、文本抽取、视觉调用与主模型调用都在数据库事务外。单个附件解析失败时以不可读说明继续起草，不根据存在性或类型猜测；资源授权在读取期间失效则整次建议按资源级不存在失败并结束 AgentRun。AI 或附件存储不可用时手写反馈/评价、文字提交、检查点提交与授权下载仍按各自可用性独立运行。
- 运维：自托管 provision 默认创建 `/opt/cdas-next/shared/attachments`，部署脚本把启用开关和目录合并进共享 `.env`，目录不参与 release 轮换。正式学生数据进入前仍需另行决定备份保留、磁盘监控、孤儿对象清理与恶意文件扫描；本决策不把这些未决定事项伪装成已完成。
- 非目标：全局 Agent 任意读附件、学生聊天读取、自动反馈或自动评分、未确认写入、扫描件 OCR、旧 `.doc` 解析、DOCX/PDF 高保真批注、格式白名单扩大、开放 URL/路径/storage key、恶意文件扫描和长期保存附件转写或模型原始建议。
- 验收：图片/PDF 只有有权学生或“发布者且当前班级管理员”可预览，Word/未知类型不能 inline；自托管真实走通上传、验证、正式提交、教师预览/下载且 release 轮换不删除对象；其他学生/教师与伪造 ID 得到资源级不存在。真实模型冒烟必须提交一张含独有可核验事实的图片，形成性反馈使用其中具体事实，量规建议至少产生一条 `kind: attachment` 且指向同一正式修订的引用；教师确认前零业务修订，确认后仍由既有 AI_ASSISTED provenance 精确绑定。

## D-056：封闭词表由领域目录生成，不在提示词里手写

- 状态：已接受
- 日期：2026-08-30
- 决策：凡是会导致模型载荷被拒、而模型又无法自行推断的规则，必须出现在助手指令里；
  学科、作业类型与子类型、探究深度、提交模式、跨学科概念等封闭词表一律从
  `src/domain/activity/activity-content.ts` 的目录生成，禁止在提示词里手写取值。
  可由其他字段推导的载荷字段（如 `content.integratedDisciplineCodes`）由服务端推导，
  且推导即权威，不再接受模型版本后校验一致性。
- 理由：两次真实失败都不是模型不听话，而是系统持有模型不知道的规则再据此拒绝它。
  一次是承诺推导却不推导，模型填了必填字段就被判不一致；一次是 `科学` 仅小学开设，
  目录正确拒绝了「七年级 + 科学」，但指令从未说明学段与学科的对应。
  两次失败对教师都只显示「草稿未创建」。
- 执行机制：`activity-assistant-handler.test.ts` 遍历每份目录，断言每个 code 都出现在指令里；
  补学科、删跨学科概念标签或改子类型配对时该测试先失败，提示词必须同步更新。
  词表被清空时指令必须改口（例如跨学科概念删除后要求 `crossDisciplinaryConceptCodes` 留空数组），
  该分支同样有测试覆盖。
- 影响：提示词中该段文字不再手工维护；新增学科无需改提示词，删除标签会强制改提示词。

## 尚未决定

以下生产选择仍需在真实账号和学生数据进入前完成小型验证或合规审查：

- Neon 是否满足目标地区的数据驻留、备份与恢复要求
- Vercel Blob 的正式数据保留/删除策略，以及真实学生文件进入前是否增加独立恶意文件扫描
- 本地账号找回、管理员重置、MFA 与大规模账号开通流程
- 模型供应商、区域、日志保留和关闭模型时的降级行为
- Vercel 部署区域、观测数据范围与故障恢复方案

尚未决定的事项不得以脚手架默认值偷偷成为既定架构。

## D-057：助手同时回答「这个产品能做什么、在哪做」

- 状态：已接受
- 日期：2026-08-31
- 用户场景：教师在会话里问「你能不能帮我评判学生交上来的作业，包括附件」。助手照实
  回答自己的 12 个工具读不到提交正文、附件与评价，因此说不能给具体学生写评语——
  这对会话是准确的，对产品是错的：D-044/D-052/D-055 正是为此建的，评阅台上就有
  「让助手起草这一版反馈」和「让助手起草这一版评价」，起草时还会读当前正式修订的附件。
  教师得到的结论不是「换个地方做」，而是「这个产品做不到」。
- 决策：助手指令里除工具用法外，还必须包含产品职责地图——教师端与学生端每个页面的职责、
  附件限额与格式规则、以及会话做不了但产品做得到的常见转介。**遇到这类问题必须把教师
  指到能做的页面去，不能只说「我不能」。** 没写进地图的功能一律说目前没有，不得用
  「应该可以」搪塞，也不得编造页面或按钮。
- 不做的事：不新增任何工具，不放开任何数据，不给会话任何新的读取权限。评阅起草仍然只是
  第一方评阅页上的动作（D-044/D-055 的边界不变）。本期也不引入检索：地图是封闭的、
  随路由变动，规模适合直接生成进指令；将来若要在更丰富的帮助内容上加检索层，
  这份地图仍是它必须对齐的事实来源，不会因此推倒。
- 生成而非手写：教师端页面来自 `src/domain/assistant/teacher-product-surfaces.ts`，
  页面种类对齐 `teacherAgentPageKindSchema`；附件限额、可交格式、可预览格式与
  起草器可读格式全部由 `src/domain/submission/attachment-policy.ts` 推导。
  `staticContexts` 的固定页面标签与路径改为读同一份地图，不再各留一份。
- 执行机制：`activity-assistant-handler.test.ts` 遍历 `teacherAgentPageKindSchema`
  的每个页面种类与格式目录，缺一即失败；`submission-attachment-reader.test.ts` 断言
  目录里的 `assistantReading` 与读取器实际分支一致，避免助手承诺一种它其实读不了的附件。
  这正是 D-055 缺少的东西：能力上线、提示词没跟上，而没有任何测试会发现。

## D-058：官方课标语料扩展为课程方案与 14 门 2022 年课程标准

- 状态：已接受
- 日期：2026-09-01
- 用户场景：教师设计跨学科任务时，需要按活动实际学科检索教育部 2022 年义务教育课程标准原文，而不是只覆盖演示用的语文、数学、物理和信息科技。
- 决策：生产语料采用已核对的来源与章节白名单：教育部课程方案，以及道德与法治、语文、历史、英语、地理、科学、物理、生物学、信息科技、体育与健康、艺术、劳动、数学、化学共 14 门课程标准。综合实践活动可参与任务设计，但不提供独立课标语料，因此不触发“必须引用两个官方来源”的门槛。
- 构建：Word 原件仍来自并列旧项目 `cdas/storage/raw/curriculum_standards/`。新增学科用现有 Pandoc 3.9.0.2 `--track-changes=all -t gfm` 管线机械重抽；已在主线的五份 Markdown 保持原抽取，以免无谓改写既有章节 ID。`manifest.json` 继续是唯一章节准入清单；清洗器只去封面/目录/页眉页脚，不做章节取舍。
- 检索：语料扩容后，确定性词法检索改为先按来源各取最高分命中，再补第二命中，并对不少于 4 字的查询短语加权，避免高频“跨学科”把课程方案的 10% 课时条款挤出结果。仍不使用 embedding、向量库、新 Prisma 表或第二后端。
- 非目标：不吸收 PR #15 的 python-docx 提取器、学校组织、本地认证、任务书 schema 演进或蓝色换肤。UbD、C-POTE、T/CAET、教材、教师样例和 AI 历史产物仍不进入语料。
- 验收：15 个来源全部索引已圈定顶级章节；繁简体检索稳定；零命中明确返回；课程方案“跨学科主题学习 课时”在扩容后仍可被检索到；化学等新增学科可按学科过滤命中；综合实践活动不算已覆盖学科；命中已收录学科的提案仍须两个不同官方来源。

## D-059：学校组织边界与平台管理员端

- 状态：已接受
- 日期：2026-09-01
- 用户场景：平台唯一管理员需要建校、改名、启停学校、重置教师邀请码，并按学校查看、登记和启停教师。既有教师与学生继续用当前认证供应商上课；新登记教师只有业务身份，停在待开通，不能登录。
- 决策：新增 `School`、`ADMIN`、账号/学校启停状态和 `Classroom.schoolId`。数据库约束与触发器保证学校代码不可改、用户学校不可改、班级负责人与成员必须同校、全平台只有一名 ADMIN。管理员命令只读写学校、教师和聚合计数，不读活动、提交、反馈或评价。`/admin` 不装 Agent。邀请码只存 SHA-256，明文只在创建或重置结果中出现一次。管理员登记教师写入 `AppUser` 与 `TeacherProvisioning(PENDING)`，`authSubject` 为 `pending:<provisioningId>`，不调用认证供应商、不写密码。遗留回填使用 RFC 4122 UUID `c0de0000-0000-4000-8000-00000000c0de` 与合法代码 `SCHARCHX`。
- 授权：每个管理员命令/查询证明当前 actor 为 ACTIVE 的 ADMIN。教师与学生业务入口在 `getCurrentActor` 与关键查询中拒绝 DISABLED 账号或所属 DISABLED 学校，表现为不能进入工作台或资源级不存在。教学历史表不写 `schoolId`。
- 非目标：本地认证、登录页、会话、Clerk 用户创建或 ticket、邀请码自助注册、教师自助改资料、Excel 导入、任务书演进、蓝色换肤、管理员读写教学对象。
- 验收：回填后既有教学资源 ID 不变；同校工号唯一、跨校可复用；跨校班级/成员被数据库拒绝；停用学校或教师后业务入口拒绝且历史只读完整；重置邀请码后旧码失效且失败响应不泄露校名；ADMIN 单例；非管理员不能建校或枚举学校；相同幂等请求不双写；Agent 无学校管理工具。

## D-060：认证回归应用数据库并停用外部身份供应商

- 状态：已接受
- 日期：2026-09-01
- 用户场景：小规模学校部署需要在单一 CDAS Next 应用与 PostgreSQL 内完成管理员、教师和学生登录，不再依赖外部身份供应商的账号、ticket、回调或客户端脚本；既有教学授权和历史必须原样保持。
- 决策：新增一对一 `LocalCredential` 和可多条的 `AuthSession`。密码使用版本化 Argon2id envelope（memory 19456 KiB、iterations 2、parallelism 2、16-byte salt、32-byte tag），密码策略为 10–128 个 Unicode 字符且至少含字母和数字。未知 identifier 使用同参数固定 dummy hash，避免快速 miss 泄露账号；连续 5 次失败锁定 15 分钟，锁定检查在密码派生前执行。
- 会话：成功登录生成 32-byte 随机 token，浏览器持有 base64url 原值，数据库仅存 SHA-256；默认 TTL 12 小时。`cdas_session` 使用 HttpOnly、SameSite=Lax、Path=/、Expires/Max-Age，并在 production 启用 Secure。登出先撤销数据库会话再清 cookie；改密清除锁定状态、撤销全部旧会话并签发新会话。教师和学生的 `mustChangePassword` 都必须执行。
- 迁移语义：管理员登记的教师继续使用既有 `AppUser`。完成本地开通时，在同一事务把 `authSubject` 从 `pending:<provisioningId>` 改为 `local:<同一 AppUser id>`、创建凭据并完成 provisioning；不新建替身用户，不改教学资源 ID，不复制或重写任何发布、提交、反馈、评价、审计或幂等历史。
- 授权：本地认证只识别 caller。会话解析后的所有 UI actions 和 Agent tools 继续调用同一服务端领域命令，并在每次调用检查 ACTIVE 账号、ACTIVE 学校、资源所有权或成员关系；LocalCredential 与 AuthSession 不保存教学权限。
- 原因：第一阶段部署规模不需要外部身份供应商的组织、社交登录或企业 SSO 能力；移除跨供应商账号、ticket 与运行时配置后，自托管、离线演示、合成门禁和故障边界更简单，同时避免认证供应商不可用阻断全部手工教学流程。
- 非目标：SSO/MFA、邮件找回或自助重置、无密码登录、外部目录同步、生产凭据导入、跨校账号合并、新的授权模型，以及为了迁移而改写 append-only 教学历史。合成门禁密码只存 GitHub Environment secrets，不进入仓库或 artifact。
